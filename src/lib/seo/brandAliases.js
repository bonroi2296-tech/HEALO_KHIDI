/**
 * healwith: 브랜드 이름을 «다르게 적어 검색하는 사람»에게도 잡히게 하는 별칭 목록.
 *
 * 왜 필요한가 (2026-09-10 PO): 우리 이름을 라틴 소문자 `healwith` 로만 신고해 두면
 *   「힐위드」로 치는 한국 사람, 「Хилвиз」로 치는 러시아·카자흐 사람은 우리를 못 찾는다.
 *   이름 검색은 유입 중 가장 «살 마음이 큰» 갈래라 놓치면 손해가 크다.
 *
 * ⚠️ 이건 «우리가 쓰는 표기»가 아니다 — 그 반대다.
 *   `src/lib/i18n/glossary.js` 의 `brand.healwith` 는 키릴 음차를 **avoid**(본문에 쓰지 마라)로
 *   못박아 뒀고 그 결정은 그대로 살아 있다. 등록 상표·도메인·앱스토어 등록명이 전부 소문자
 *   `healwith` 라, 러시아어 본문에 «Хилвиз» 라고 적으면 등록명과 어긋나고 번역기 티도 난다.
 *
 *   두 규칙이 부딪히는 게 아니라 **자리가 다르다**:
 *     · 본문·화면 글자        → 언제나 `healwith`      (glossary 가 관장)
 *     · 검색엔진에 알리는 별칭 → 아래 목록             (이 파일이 관장)
 *   `alternateName`(JSON-LD)·`keywords` 는 「사람들이 이렇게도 부른다」를 적는 칸이지
 *   「우리가 이렇게 쓴다」를 적는 칸이 아니다. 오탈자·음차를 넣는 것이 원래 용도다.
 *
 * 🛑 지우기 전에: 위 구분을 이해했는지 먼저 확인해라. glossary 만 보고
 *    «avoid 인데 왜 여기 있냐»로 지우면 브랜드 검색 유입이 통째로 사라진다.
 */

/** 정식 등록명. 본문·화면은 언제나 이것만 쓴다. */
export const BRAND_CANONICAL = "healwith";

/**
 * 사람들이 실제로 쳐 볼 만한 다른 적기. 정식명(BRAND_CANONICAL)은 뺀 «별칭»만 담는다.
 * 근거를 옆에 적어 둔다 — 근거 없이 늘리면 잡음만 는다.
 */
export const BRAND_ALIASES = [
  // 라틴 — 대소문자·띄어쓰기 변형. 검색창엔 이렇게들 친다.
  "Healwith",
  "HealWith",
  "heal with",
  // 주소를 그대로 검색하는 사람도 많다.
  "healwith.co.kr",
  // 한국어 — 상표 동시 출원·실사용 중인 병기 표기(components/brand/Logo.jsx 참조).
  "힐위드",
  "힐 위드",
  // 러시아어·카자흐어 음차 — glossary 의 avoid 목록과 «같은» 철자들이다.
  //   본문엔 못 쓰지만, 그렇게 «검색하는» 사람은 실재하므로 별칭으로는 반드시 넣는다.
  "Хилвиз",
  "Хилвис",
  "Хилвит",
  // 일본어 — 가타카나 음차(heal=ヒール, with=ウィズ). 표기 규칙대로 옮긴 것이고
  //   실제 검색량은 확인하지 못했다. 잡음이면 이 줄만 빼면 된다.
  "ヒールウィズ",
];

/** 정식명 + 별칭 전부. keywords 처럼 «이름 전부»가 필요한 자리에 쓴다. */
export const BRAND_NAME_FORMS = [BRAND_CANONICAL, ...BRAND_ALIASES];
