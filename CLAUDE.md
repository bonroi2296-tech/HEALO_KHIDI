# HEALO KHIDI — Claude Code 프로젝트 지침

> **이 문서는 「항상 적용되는 것」만 담는다.** 특정 상황에서만 필요한 규칙은 아래 트리거 표를 보고
> 그때 해당 문서를 읽는다. 규칙을 지운 게 아니라 **자리를 나눈 것**이다 (2026-07-28 재구성,
> 이동 전수 대조표 = `docs/rules/MIGRATION_MAP.md`).

---

## 🔀 트리거 표 — 이 말이 나오면 그 문서를 먼저 읽어라

| 이런 작업·말이 나오면 | 읽을 문서 |
|---|---|
| UI·화면·디자인·색·컴포넌트·카피 톤 | **`DESIGN.md`** (위반 시 PR 머지 거부) |
| 합치기 신청서(PR)·머지·자동머지·리뷰 게이트·완성도 감사·보고 주기 | `docs/rules/AUTOMERGE.md` |
| 배포·실서비스 반영·프리뷰·빌드 창구·production 브랜치·긴급 배포 | `docs/rules/DEPLOY.md` |
| 출시·QA·검증·DB/RLS·데이터 흐름·인증 권한·소프트 삭제·돈 숫자 단정 | `docs/rules/SELF_QA.md` |
| 버그·누락·오류·반성문·재발·가드 룰·문서 건강검진·사용자 제보 | `docs/rules/BUG_ROUTINE.md` |
| AI 품질·환각·안전 위반·응답시간 P95·flag·품질 지표 | `docs/rules/AI_QUALITY.md` |
| 백오피스·사용설명서·매뉴얼·새 계층(role) 추가 | `docs/rules/MANUALS.md` |
| 라우트·페이지·URL 구조·어드민 메뉴·화면 위치 찾기 | `docs/rules/PROJECT_MAP.md` |
| 프리뷰·스크린샷·브라우저·로그인 뒤 화면 눈으로 보기 | `docs/rules/PREVIEW.md` |
| PPT·발표자료·제안서 슬라이드·피치덱 만들기 | **`docs/rules/PPT_STYLE.md`** (BeyondK 깔 고정 — 2026-07-31 PO 지시) |
| KHIDI·중간평가·성과지표·유치 건수·만족도·발표 | `docs/rules/KHIDI.md` · `docs/KHIDI_중간보고_베이스.md` |
| 병렬 세션·worktree·영역 배분·다른 세션에 일 떼주기 | `docs/PARALLEL_SESSIONS.md` |
| 보안 상세 체크리스트 | `docs/SECURITY_CHECKLIST.md` |
| 완성 판정 기준(Definition of Done) | `docs/DEFINITION_OF_DONE.md` |

### ⚠️ 신규 UI 작업 전 필수 확인

**`DESIGN.md` (프로젝트 루트) 먼저 읽기.** 기본 톤(teal, 단일 디자인) 표준·금지 룰·"AI가 만든 느낌" 회피 가이드 포함. (※ 예전 "legacy 모드"가 곧 이 "기본 톤" — 이름만 바꿈 2026-07-01. "premium"은 폐기된 옛 실험이지 업그레이드 아님.)
위반 시 PR 머지 거부.

---

## ⚠️ 새 세션 시작 시 필수 (맥락 인수인계)

**`docs/PROJECT_CONTEXT.md` 먼저 읽기.** 피벗 방향·핵심 전략 결정(왜)·현재 기능 상태·PO 결정 대기 항목·다음 작업까지 — 코드만 봐선 안 보이는 맥락 전부. 이걸 읽으면 이전 세션과 연속으로 일할 수 있다 (PO가 다시 설명 안 해도 됨).
남은 버그·개선점은 `docs/KNOWN_ISSUES.md`. **고정 규칙(이 문서)과 별개의 누적 PO 취향은 `docs/PO_PREFERENCES.md`** (세션 시작 시 훅이 「분류 대기실」을 자동으로 띄움 — 어기지 마라).

**세션 마칠 때 = `/handoff`** — 이번 세션 한 일·결정·다음 할 일을 `PROJECT_CONTEXT.md` 최상단에 정리(단일 SoR). 인수인계 파일을 새로 만들지 말 것(흩어지면 다음 세션 혼란). `/handoff`는 추가로 (A)미검증 항목을 다음 할 일 1번으로 승격, (G)이번 대화에서 드러난 PO 취향을 `PO_PREFERENCES.md`에 **분류 태그와 함께** 누적, (F)`npm run check:handoff`·`handoff:rotate`로 형식검사·자동보관까지 한다.

**중간 저장(세션 도중 — 2026-07-05 PO 승인, 세션 끝까지 기다리지 마라)** — 세션 진행 중 ①PO 결정(방향·정책·취향 확정) ②머지·배포 완료 ③중요한 발견·함정이 나오면 **그 즉시** `PROJECT_CONTEXT.md` 최상단 핸드오프의 해당 칸만 짧게 갱신하고 커밋한다(7칸 전체 재작성 불필요 — 해당 칸에 날짜 달고 1~2줄 추가면 충분). 이유: 세션이 도중에 죽으면(창 닫힘·서버 회수) 코드는 자동 커밋으로 남지만 **"왜 그렇게 결정했는지"는 통째로 증발**한다 — 결정 기억은 세션 종료가 아니라 결정 시점에 저장한다.

---

## 🗂 새 규칙이 생겼을 때 — 「쓸까 말까」가 아니라 「어디에 쓸까」 (2026-07-28 PO 결정)

**지침화하고 싶은 걸 참지 마라.** 참아야 할 건 「전부 한 통에 붓는 것」이다. 새 케이스가 생기면 **위에서부터 걸리는 첫 칸**에 넣는다.

| # | 판정 질문 | 넣을 곳 | 태그 |
|---|---|---|---|
| 1 | **기계가 잴 수 있나?** | 자동검사(CI)·훅. **문서엔 안 적는다** — 사람이 안 읽어도 안 뚫린다 | `[CI]` |
| 2 | **특정 상황에서만 쓰나?** | `docs/rules/*.md` 또는 스킬. **그 상황에서만 읽힌다** | `[문서:파일명]` |
| 3 | **말투·보고 방식인가?** | 말투 훅(`.claude/hooks/plain-korean.sh`) — 매 턴 재주입 | `[말투]` |
| 4 | **정말 매번·모든 작업인가?** | **이 문서**(`CLAUDE.md`). 여기만 상한을 둔다 | `[규칙]` |
| 5 | 어디에도 안 걸리면 | 사건 기록(`docs/POSTMORTEMS.md`). **규칙이 아니다** | `[기록]` |

- `docs/PO_PREFERENCES.md` 「분류 대기실」은 **최종 목적지가 아니라 대기실**이다. `/handoff`가 취향을 넣을 때 위 태그를 반드시 붙이고, 1·2·3·5번은 제 자리로 내려보낸 뒤 대기실에서 뺀다.
- 기계 검사: `npm run check:rules` — 태그 누락·대기실 초과·죽은 문서 참조·`CLAUDE.md` 상한을 CI가 막는다.
- **왜:** 2026-07-28 활성 취향을 75,756자 → 10,746자로 줄였는데 **반나절 만에 14,797자로 되찼다**(+38%). 되찬 11건을 분류해보니 매번 필요한 건 1건, 3건은 이미 있는 규칙의 재탕, 4건은 상황별, 3건은 말투였다. **분류 없이 한 통에 부으면 1건짜리 신호가 10건 잡음에 묻힌다.**

---

## 사용자 프로필

PO(프로덕트 오너) 혼자 운영. Bonroi 개인사업자, KHIDI(한국보건산업진흥원) 정부지원과제.

**소통 스타일:**
- 한국어 + 필수 영어 용어만 혼용, 짧고 직설적
- 결과물(URL, 배포, 시각적 확인) 우선 — 긴 설명 X
- **개발 용어는 쉽게 풀어 설명하되 원래 용어를 병기** — PO가 학습할 수 있게. 풀이 없이 용어만 쓰기 금지. (PO가 직접 요청한 방식)
  - 자주 쓰는 변환: 합치기 신청서(PR) / 자동 검사(CI) / 본판에 합침(머지) / 실서비스 반영(배포·deploy) / 작업본(브랜치) / 저장 올리기(커밋·푸시)
  - **내가 먼저 꺼낸 제안 문장 안의 원어도 그 자리에서 풀어라** — 본문·버튼은 쉽게 쓰다가 「덧붙이는 제안」에서 날것이 샌다. 제안은 PO가 판단해야 하는 문장이라 오히려 제일 쉬워야 한다.
- 기술 디테일은 물어볼 때만 설명
- "야 이거 ~~함" "~인디?" 같은 말투에 맞춰서 대응
- **쉽게 설명 + 「물어야 할 것만」 물어라 (PO가 직접 요청·여러 번 강조 — 어기지 마라)**: 비개발자가 바로 이해하게 비유·일상어로 풀어라. 질문은 **「어떻게 물을까」보다 「물어야 하나」가 먼저다** — 아래 🚦관문을 통과한 것만 묻고, **물기로 했으면 예외 없이** `AskUserQuestion` 도구로 "눌러서 고르는 버튼"을 띄워라.
  - 🚦 **묻기 전 관문 — 아래 셋 중 하나도 아니면 묻지 말고 그냥 실행.** ①돈이 나가나 ②되돌리기 어렵나(삭제·파괴·인증·암호화·PII·보안) ③PO만 아는 정보인가(사업 판단·취향·외부 사실). 셋 다 아니면 **추천안으로 실행하고 결과에 "이렇게 했음, 아니면 말해라" 한 줄**만 남겨라. **조사·진단 도중엔 묻지 마라** — 다 끝내고 한 번에.
  - ⛔ **이미 답을 받은 질문을 다시 묻지 마라 — PO가 「버그」라고 부른 그것 (2026-07-30 PO 제보: *"물어보는건 좋은데 대답을 해줘도 자꾸 버그처럼 같은 질문 하는거"*).** 버튼의 답은 도구 결과로 한 번 지나가고 끝이라, 대화가 길어지면 결정이 「없던 일」이 된다. **세는 걸로는 안 고쳐진다 — 답을 매 턴 눈앞에 다시 붙여야 안 잊는다.** → `scripts/check-ask-streak.mjs`가 매 턴 「이미 답한 것」 원장(질문 → 고른 답)을 재주입하고, 같은 질문 재탕·연타 2차례를 경고한다(매 턴 재주입 + 턴 종료 점검 양쪽). 확인 삼아 다시 묻기·말만 바꿔 다시 묻기 전부 위반.
  - **평문으로 "~할까요?"·"~할래요?"라고 묻기 금지** — 관문을 통과한 질문은 단순 예/아니오, "머지할까요?", "여기까지 할까요?", "이대로 진행?" 같은 것도 전부 버튼으로(예: "머지하기 / 더 작업 / 오늘 여기까지"). PO는 타이핑이 아니라 클릭으로 답하길 원함.
    - 근거(2026-07-20 실측): 한 세션에서 버튼 43개 = 질문 짓는 데만 **13분 48초**(평균 19.2초/개) 소모. 응답 지연의 최대 단일 원인이 "내가 던진 질문"이었음. 세션 길이·컨텍스트 크기는 무관(1170턴 돌려도 턴당 시간 1.03배)로 실측 반증됨.
    - **한 응답에 버튼은 최대 1개.** 묻기로 했으면 = 무조건 버튼(평문 질문·텍스트 A/B/C 나열 = 위반).
  - **선택지를 내밀 땐 양쪽의 이득·비용을 실측 숫자로 먼저** 적고 그다음 버튼 (상세: `docs/rules/AUTOMERGE.md`).

---

## ⚠️ 자율 안전선 — PO를 멈춰 세우는 건 2가지뿐

**①돈 나가는 것**(소액=알려만, 큰돈=확인) **②되돌리기 어려운 것**(데이터 삭제·파괴, 인증·암호화·PII·보안). 그 외는 어시스턴트가 알아서 + 자동검사로 닫는다. UI/화면 변경은 "무조건 확인" 아님.

- ⚠️ **자율이어도 안 변하는 것**: `DESIGN.md`(기본 톤·"AI 만든 느낌" 금지)·보안 핵심 규칙·`check:content`. 자율 범위가 넓어진 거지 품질 헌법이 풀린 게 아니다.
- 🛑 **DB 삭제·컬럼 drop·데이터 파괴는 여전히 PO 확인.** 가역적 추가(컬럼·테이블·인덱스)는 자동 OK.
- 자동머지 절차·리뷰 게이트·완성도 감사·보고 주기 상세 → **`docs/rules/AUTOMERGE.md`**

---

## ⚠️ 반복 확정된 작업 규칙 (취향 원장에서 승격)

> 세 번 이상 반복돼 「취향」이 아니라 「규칙」이 된 것들. 원문·경위는 `docs/PO_PREFERENCES.md` 「보관 C-1」.

1. **「마저 해」가 나오면 = 내가 중간에 끊고 있다는 뜻.** 중간 보고로 턴을 끝내지 말고 끝까지 하고 한 번에 보고.
2. **「그건 못 한다」고 말하기 전에 한 번 더 확인하라.** PO가 되물으면 대체로 PO가 맞았다.
3. **보고는 「PO 몫」과 「어시 몫」을 갈라서.** 섞으면 PO가 뭘 해야 하는지 못 찾는다.
4. **더미(가짜) 데이터 판단 기준은 「완성도」가 아니라 「피벗 전 도메인인가」.** 잘 만들어져 보인다고 되묻지 마라.
5. **PO에게 셸 명령어를 쥐여주지 마라.** 어시가 할 수 있는 건 어시가 한다. **일을 세션에 나눌 때도 같다 — 「내가 지금 할 수 있는 것」을 남의 세션 몫으로 적어두지 마라**(2026-07-29 PO: *"이건 니가 해도 되는거 아님?"*). 배분은 **내가 못 하는 것**(브라우저·권한·PO 판단)부터다.
6. **「나중에 다시 쓸지 모르니 보존」은 명분이 아니다.** 죽은 코드는 삭제.
7. **규칙은 「문서에 적었다」로 끝나지 않는다.** 기계로 잴 수 있으면 훅·자동검사(CI)로 박아라.
8. **반쪽 금지.** 새 역할·기능은 전체 흐름 끝까지 한 묶음으로.
9. **「재개하자」에 바로 실행부터 하지 마라 (2026-07-28 승격).** 재개·인수인계 첫 응답은 **표 한 장**(남은 일 / 상태 / 내가 할 것 vs PO 손 필요)으로 시작하고 그다음 착수. 긴 작업(빌드·Lighthouse) 전엔 "이거 돌린다, 3분" 한 줄. 근거: *"니 혼자 돌아가면 나는 니가 죽은건지 놀고 있는건지 일하는건지 어케 아냐"*.
10. **버그는 원인+재발방지까지 한 세트 / 시키기 전에 먼저 제안 / 검증 못 한 건 솔직히.** 상세 4단계 → `docs/rules/BUG_ROUTINE.md`.

---

## ⭐ 실측·재검증이 기본값 (매 작업 적용 — 상세·사례는 `docs/rules/SELF_QA.md`)

> PO 취향 10회+ 반복 확정 → 2026-07-15 고정규칙 승격. **"빌드 통과 ≠ 동작."**

1. 빌드·tsc·HTTP 200·404 안 남 = *화면이 그려지는지*일 뿐 **동작 아님** → 의미 있는 흐름은 실제 여정을 클릭/실호출/실DB로 검증.
2. PO가 **"완벽해?/오픈 확정?/이상하다·개판이야/된겨?"** 라고 되물으면 안심시키지 말고 **그 자리에서 실측·감사·점수표(근거)로** 답하라. 특히 내가 직전에 "다 됐다"고 한 직후일수록 방어 0·실측 100. 중간 신호(초록불·토스트·워크플로 success)로 답하면 나중에 거짓말이 된다.
3. **추측으로 단정·우기기 금지** — 화면 정체·테이블/컬럼 존재는 실제 파일·`information_schema`로 확인 후 말하고, 틀리면 빨리 인정.
4. **내 과거 문서·1차 분석도 재질문받으면 그 자리에서 재현·재검증**하고 틀리면 문서 정정.
5. **원인 지목 전 반증 검사** — "X가 원인이다"라고 말하기 **전에** *X가 진짜 원인이면 무엇이 참이어야 하는가*를 1줄 적고 그걸 먼저 확인하라. 통과 못 하면 X는 원인이 아니다. **큰 숫자를 찾은 것 ≠ 원인을 찾은 것.**
6. **검증 못 한 부분은 "검증 못 했음"이라고 솔직히 보고** — "됐어요" 남발 금지.
7. **PO 의 되물음은 「불안」이 아니라 「감사 지시」다 — 작업이 끝난 뒤에도 유효하다.** *"더 할 건 없어?"·"꼼꼼히 체크해봐"·"허점 없어?"* 가 오면 안심시키지 말고 **①범위를 정하고 ②항목표를 만들어 대조하고 ③내가 방금 쓴 코드도 남의 코드처럼 다시 읽어라.** (2026-07-28 실측: 하루에 세 번 물었고 **세 번 다 실제 결함이 나왔다** — 그중 둘은 내가 방금 만든 것, 하나는 광고비가 걸린 것. 한 번이라도 "네 다 됐습니다"로 답했으면 전부 실서비스로 나갔다.)
8. **「내가 심은 해석」을 문서에 단정형으로 쓰지 마라.** 측정·분석 설계는 **「~라는 뜻」이 아니라 「~일 수 있다(단서)」**로 적고, **정반대 해석이 가능한 경우를 같이** 적어라. 안 그러면 다음 사람이 **가설을 사실로 읽고 엉뚱한 걸 고친다**(2026-07-28 PO 지적으로 실제 2곳 교정).

---

## ⚠️ KHIDI 중간평가 (2026-08-27) — 모든 작업의 상시 기준

**8/27 중간평가에서 70점 이상이어야 잔금(30%)을 받는다.** 모든 개발·운영 작업은 이 평가를 염두에 두고 진행 (8월에 급조 금지).
**공식 성과지표**: 외국인환자 유치 **12건** / 사전상담·사후관리 **120건** / 만족도 **90점**. 정량 2개는 `/admin/khidi/conversion`가 자동 집계 = 그게 곧 점수.
평가항목·일정·작업 습관 상세 → **`docs/rules/KHIDI.md`**, 살아있는 준비 문서 → `docs/KHIDI_중간보고_베이스.md`

---

## 프로젝트 개요

**KHIDI HEALO** — 카자흐스탄/러시아/CIS 암환자 → 한국 종양 병원 매칭 의료관광 플랫폼.
라우트·주요 시스템 지형도 → **`docs/rules/PROJECT_MAP.md`**

**기술 스택:**
- Next.js 16 (App Router) + Supabase (PostgreSQL 17.6, RLS, pgvector)
- AI: `gemini-flash-latest` (자동 최신 별칭) — **PO 결정(2026-06-12): 최신 유지. AI가 임의로 구형 고정 금지.** 별칭 특성상 단가가 바뀔 수 있으므로 비용 통제는 Google 콘솔 spend cap + aiGuard(일일 상한)로. + 3-Tier RAG (`src/lib/chat/generateReply.ts`)
- 영상: **LiveKit** WebRTC (원격협진) — env 필수: `LIVEKIT_URL`·`LIVEKIT_API_KEY`·`LIVEKIT_API_SECRET` (NEXT_PUBLIC_LIVEKIT_URL은 선택)
- Auth: @supabase/ssr cookie-based SSR + Bearer token
- 암호화: AES-256-GCM (`src/lib/security/encryptionV2.ts`)
- Hosting: Vercel — 프로젝트 `healo-khidi` (메인), Team `bonrois-projects`
- 다국어: ko, en, ru, kz, zh, ja (6개 언어) — `src/lib/i18n/index.js`
- Supabase 프로젝트: `hvwwlkawaxabhtumjhrg`

---

## 빌드 & 배포 (핵심만 — 상세는 `docs/rules/DEPLOY.md`)

```bash
npx next build --webpack   # 필수: --webpack (Turbopack 금지 — 빌드 실패)
npm run dev                # dev 서버는 Turbopack 정상
```

- **「머지는 자유, 배포만 하루 한 번」** (2026-07-28 PO 결정). PR은 바로 머지해도 실서비스 빌드가 안 돈다 — KST 오후 3시 `Daily Deploy` 창구가 main을 `production` 브랜치로 밀어 그 한 건만 빌드한다.
- **머지했다 ≠ 실서비스에 나갔다.** 「배포됐다」고 말하기 전에 창구가 돌았는지 확인해라.
- 긴급 배포는 `[deploy]`, 프리뷰가 필요하면 `[preview]`를 커밋 제목에 (기본 프리뷰 OFF).
- OS: Windows 11 / Shell: **PowerShell 이 기본**, bash도 가능 — 문법 섞지 말 것.

---

## 보안 핵심 규칙

> 상세 체크리스트: `docs/SECURITY_CHECKLIST.md`

- **API 응답에 error.message 절대 노출 금지** → `"internal_error"` 코드형만
- **새 API 라우트** → 인증 헬퍼 필수 (`requireAdminAuth` / `requireConsultationAccess`)
- **권한 체크** → `app_metadata.role` 기준 (user_metadata 금지)
- **환자 PII** → `encryptStringNullable()` AES-256-GCM 후 `*_encrypted` 컬럼
- **공개 POST** → `checkRateLimit(ip, config)` from `src/lib/rateLimit`
- **서버 모듈** → `import "server-only"` (service_role 키 접근 시)

---

## 코드 컨벤션

- Path alias: `@/*` → `src/*`
- 한국어 주석 OK, 커밋 메시지 한국어
- `strict: false` (TypeScript) — 점진적 전환 중
