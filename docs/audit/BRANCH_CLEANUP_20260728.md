# 브랜치 정리 대장 — 2026-07-28

세션시작 훅의 「서랍에 갇힌 작업」 경보(#1062)를 켠 뒤, **본판(main)에 안 들어간 원격 브랜치 전수 대조**를 한 기록.

## 판정 방법 (다음에도 이대로)

1. **PR 상태**로 먼저 가른다 — `--no-merged` 는 쓰지 않는다(스쿼시 머지를 쓰면 알맹이가 다 들어가도 영원히 미머지로 보임).
2. **줄 단위 대조** — 브랜치판의 각 줄이 본판 같은 파일(문서는 `docs/` 전체 + 아카이브)에 있는지 파이썬 `in` 으로 확인. ⚠️`cut -c` 로 잘라 grep 하면 UTF-8 문자 중간이 끊겨 **「본판에 없음」 오판**이 난다.
3. 줄이 안 맞아도 **다시 쓴 것**일 수 있으므로, 브랜치에만 있는 줄의 **핵심 낱말을 본판 전체에서 재검색**해 「내용이 살아있나」로 최종 판정한다.
4. 지우기 전 **태그로 박제** → 언제든 복원 가능.

## 결과 — 6개 전부 「잔재」(살릴 내용 0), 원격 브랜치 삭제

복원: `git checkout -b <이름> branch-archive-20260728/<이름>` (또는 `git show <태그>:<파일>`)

| 브랜치 | 태그 SHA | PR | 왜 잔재인가 (실측 근거) |
|---|---|---|---|
| `chore/handoff-cms-session` | `2fbca19a` | #937 머지 | 유일하게 다른 5줄이 **뒤로 가는 내용** — `⬜ 애플 $99 결제`·`⬜ 구글 $25 결제` 가 본판엔 이미 `✅ 완료(7/24·7/27)`. iOS 푸시 SPM 배선 메모도 이미 끝난 일. |
| `feat/split-meeting-type` | `01f4d18c` | #1045 머지 | 12개 파일 **전부 브랜치에만 있는 줄 0**. 머지 뒤 남은 껍데기. |
| `feat/center-menu-rag` | `11efda77` | #1002 머지 | 다른 줄은 `check-content-consistency.mjs`(본판이 더 최신 — `[인라인사전]`·`[PDF React정합]`·`[유령키]` 가드 다 있음)와 `generateReply.ts` 의 `thinkingBudget:0` 뿐. **후자는 본판이 일부러 지운 것**(#1016 — 별칭 세대교체로 매 호출 400 나던 파라미터). 되살리면 AI 가 다시 죽는다. |
| `docs/handoff-db-outage-diagnosis` | `7d863d86` | #973 닫힘 | DB 장애 지식은 본판 `PROJECT_CONTEXT` 「부하를 만든 주체 실측」 + 아카이브 「DB 무응답 장애 관측·진단 오진 정정」 + `KNOWN_ISSUES` 131행(E2E 가 프로덕션 DB 를 뭄)에 이미 있음. 나머지는 낡은 a11y 리포트·낡은 스캐너·`⬜ 결제` 잔재. |
| `docs/consult-session-memo` | `15c46407` | #981 닫힘 | 7/24 화상상담 세션 핸드오프인데, **아카이브의 「2026-07-24 밤 — 미머지 #956 회수」 블록이 같은 내용을 더 최신으로 담고 있음**(#956 → #977 로 대체 경위까지). 코드는 #977 로 본판에 있음. |
| `worktree-consult-live-fixes` | `33c87f6e` | #956 닫힘 | 위와 같은 세션의 코드본. `isTonalFrame`·`startPipeline` 등 **전부 본판에 있음**. 사용설명서 차이분은 본판이 더 발전한 판(「화자 이름표」 → 「화자 구분(색·이름)」). `KNOWN_ISSUES` 차이분도 본판이 갱신판(사전상담 집계 0·rescue 정리 완료). |
| `docs/handoff-i18n-961` | `2de5b864` | #965 닫힘 | 코드는 #961 로 본판에 있고(`[i18n-스크립트]` 가드 존재), 사건 기록은 **`POSTMORTEMS.md` #115** 에 원인·수리·재발방지까지 다 있음. |

## 남긴 것 (건드리지 않음)

작업 중인 PR 4개 — `perf/tbt`(#1063) · `work/coordinator-cms-log`(#1065) · `work/seo-cleanup`(#1066) · `claude/daily-work-summary-3w71ns`(#1067). 전부 열린 PR + 당일 커밋 = 병렬 세션이 지금 쓰는 중.

⚠️ `docs/consult-session-memo` 는 로컬 워크트리 `.claude/worktrees/consult-live-fixes` 가 물고 있다 — 원격만 지웠고 로컬은 그대로 둠.
