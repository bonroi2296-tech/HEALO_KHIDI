# 완성도 감사 로그 (`/completeness-audit`)

> "완성 판정(Manager)"을 사람 눈에서 기계로 옮긴 루프의 기록이다. (2026-07-15, OKKY "Codex 72시간 사이클" 칼럼에서 착안)
> 채점표 = `src/lib/completeness/rubric.js` (사람용 `docs/DEFINITION_OF_DONE.md`). 형식·루프 구조는 `.claude/skills/completeness-audit/SKILL.md` 참고.
> 새 엔트리는 이 안내문 바로 아래에 추가.

## 2026-09-07 완성도 감사 (범위: diff — 백오피스 리뉴얼 7단계 PR #1669, 합치기 관문 · 로컬 세션 종료 뒤 클라우드 세션이 수행)

- **스캔**: semantic 4유형(DoD-2 성공 근거 · DoD-3 문서 드리프트 · DoD-6 cron/KPI 가정 · DoD-7 시각 완결) + 독립 리뷰(작성 맥락 없는 별도 에이전트) / **발견 4건(감사 1 · 리뷰 3)**
- **수정**:
  - `DoD-3` **파생 문서가 원본을 안 따라갔다.** `ADMIN_RENEWAL_PLAN.md` 는 상담·채팅을 형태③→① 로 전환했다고 적었는데, 그 문서가 「결과가 바뀌면 따라 갱신」하라고 지정한 합본 `BACKOFFICE_HIERARCHY_REDESIGN.md` 형태 표는 여전히 「③ consultations · chat」이었다 → 표 갱신. 같은 부류 전수: `PROJECT_MAP.md`(재수출이라 여전히 참) · `src/lib/manuals`(「AI 채팅에서 검수」 참) · `KNOWN_ISSUES` 「/coordinator/chat 읽기전용 뷰」(2026-06-29 해결 기록) — 추가 드리프트 없음.
  - `DoD-8 반쪽` **(독립 리뷰 CONFIRMED) 어드민 채팅을 코디 화면으로 합치면서 어드민에만 있던 표시 3개가 사라졌다** — 환자 현지 시각 배지(심야 경고) · 메신저 채널 배지(Telegram/WhatsApp) · 메시지 «미전달» 표시(왓츠앱 24시간 창 만료). 관리자가 만료된 창에 정정을 보내도 «미전달»이 안 보이는 상황 → 셋 다 합친 화면으로 옮기고 6개 언어 키 11개 추가. 원인: 두 화면을 «기능 목록»이 아니라 «검수 단추 유무»로만 비교했다.
  - `DoD-8 반쪽` (리뷰 PLAUSIBLE) `?inquiry=` 딥링크 문의가 선택 목록(최근 50건)에 없으면 **모달이 조용히 비어서 열렸다** → 못 찾으면 경고 토스트(6개 언어). 성공 뒤 딥링크 값이 남아 다음 「새 상담」에 같은 문의가 다시 붙던 것도 정리.
  - (리뷰 nit) 딥링크 검사기의 재수출 추적이 하위 `[id]/page` 를 먼저 만나면 놓치던 것 → 폴더 바로 아래 `page.*` 만 보고 확장자 있는 경로도 폴더로 정규화.
  - `DoD-2` 「됐다」 근거 재확인(코드로): 재수출 두 파일 `"use client"` 유지 · `useDeepLinkParam`/`NUMERIC_ID`/`AdminNav` 명명 수출 실재 · 검사기 selftest 8종 + 본검사 · typecheck · vitest 1,863건 통과. 본판(#1668) 합친 뒤 변경 파일 14개 그대로(충돌은 변경관리 대장 1곳, 코드 0).
- **보류(검증 못 함)**: `DoD-7` **화면 실측은 이 세션에서 못 했다** — 클라우드 상자에 `.env.local` 이 없어 로그인 뒤 화면을 못 띄운다. 로컬 세션의 9개 주소 실측(관리자가 코디 화면 5곳을 열어도 어드민 사이드바 유지 · `?inquiry=37` 모달)이 유일한 근거이고, **옮겨온 배지 3개는 화면으로 본 사람이 없다**(코드는 옛 어드민 화면 그대로). `DoD-6` 은 이 판에 cron/KPI 변경이 없어 해당 없음.
- **라운드**: 2 (1라운드 4건 → 수정 → 2라운드: 고친 파일 재검사·i18n 패리티·검사기 selftest 무발견).

## 2026-09-01 완성도 감사 (범위: diff — 화상통역 자막·번역 PR #1574, 합치기 관문)

- **스캔**: semantic 4유형(DoD-2 조용한 성공 · DoD-3 문서 드리프트 · DoD-6 잘못된 가정으로 0/왜곡 · DoD-7 시각 완결) / **발견 3건**
- **수정**:
  - `DoD-6` **이어붙이기 채점 스크립트(`scripts/score-stitch-live.ts`)가 중간 자막을 걸러내지 않았다.** 중간 자막은 정의상 같은 발화의 앞토막이라 섞이면 「붙일 수 있는 쌍」이 인위적으로 늘어 점수가 왜곡된다 → `is_partial=false` 필터 추가. **같은 부류 전수 확인: 이 테이블을 읽는 곳 4군데(회의록 요약·번역 기록 조회·덤프 스크립트·채점 스크립트) 전부 확인했고 KHIDI KPI 집계는 이 테이블을 아예 안 본다**(허위 실적 위험 없음 — `src/lib/khidi/*` 전수 grep).
  - `DoD-3` **이번 세션의 결정·발견이 장부에 안 닫혀 있었다** → `docs/PROJECT_CONTEXT.md` 최상단 중간 저장에 기록(진범·PO 결정 2건·되돌리는 env 이름까지).
  - `DoD-2` **「됐다」의 근거가 두 곳 비어 있었다**(사용량 기록·화자 역할 저장이 «코드를 넣었다»까지만) → 실제로 호출해 실측: `ai_usage_events` 에 토큰·비용이 적재되고(입력 429·출력 557·$0.0048, meta 에 상담 id·원문 길이·partial·finish_reason), `speakerRole=other/self` 가 그대로 저장되는 것을 DB 로 확인(시험 줄 2개는 지움).
- **보류**: 없음. 단 **실기기·양쪽 새 판이 필요한 2건은 「검증 못 함」으로 남긴다** — ①원문 동봉(`src`/`srcLang`)은 두 참가자가 모두 새 판이어야 효과가 난다 ②stt partial 의 응답 지연은 오디오 입력이 있어야 재므로 다음 실회의에서 확인.
- **라운드**: 2 (2라운드 무발견 — 수정이 만든 새 미완성 없음). 폰 크기(390×844) 기록 패널 실측: 가로 넘침 0(`scrollWidth=390`), 이름 배지·시각·원어 전부 읽힘.

## 2026-08-31 완성도 감사 (범위: diff — 비공개·환자 화면 다국어화 PR #1547, 합치기 관문)

- **스캔**: DoD-8(반쪽 배선) · DoD-1(6개어) · DoD-3(문서-현실 드리프트) · DoD-5(죽은 링크·막다른길) ·
  «시험이 진짜로 잠그나»(신규 관점). 3라운드 + 독립 감사 워크플로(5개 관점 병렬 fan-out → 발견 19건 →
  관점별 반증 검증).
- **발견 13건 → 전부 수정.** 감사가 없었으면 **판의 본체가 절반만 고쳐진 채 나갈 뻔했다.**

  **🔴 라운드1 — 「같은 부류」를 세다 만 것 (반쪽)**
  1. `/consultation/{id}` — `GUEST_LINK_PREFIXES` 는 셋인데 앞의 둘만 고쳤다. page 가 `"use client"` 라
     서버 `layout.jsx` 로 해결.
  2. `/inquiry/intake` — 코디가 «환자 언어»로 메일 발송하는 토큰 폼인데 제목이 영어였고,
     **noindex 가 없어 색인 가능**이었다(토큰 없이 열면 빈 화면 = 「내용 없는 페이지」 색인).
  3. `/education` — «진짜» 색인되는 화면이 영어였다. 번역 키가 같은 날 `app/patient/education`(죽은
     리다이렉트 껍데기)에 잘못 붙어 있었다. 키를 옮기고 죽은 metadata 삭제.

  **🔴 라운드2 — 「환자가 먼저 보는 화면」을 통째로 빠뜨림**
  4. **로그인 벽 7곳**(`/login`·`/signup`·`/find-id`·`/forgot-password`·`/reset-password`·
     `/auth/confirm`·`/account/password`) 이 영어 고정. `/patient/*` 는 전부 로그인 뒤라
     코디 링크를 누른 환자는 **반드시** 여기로 튕겨 온다(실측: `/patient/visa` → 307 → `/login`).
     즉 앞 라운드가 고친 화면보다 «먼저» 보는 화면이 안 고쳐져 있었다. + `/app`(앱 설치 안내).
  5. `/opinion/{token}` — `GUEST_LINK_PREFIXES` 에서 빠져 `<html lang>` 이 **항상 en**.
  6. 공유 카드 4곳(`/cost-calculator`·`/partners`·`/ru/for-russian-patients`·`/kk/for-kazakh-patients`)
     이 `openGraph` 만 정의하고 `twitter` 를 안 둬서 **루트의 영어 카드**를 물려받았다.
     앞 둘은 ru/kz 유료 광고 착지, 뒤 둘은 Yandex 색인 자산이다.
  7. `noindex` 인데 `canonical` 을 같이 내보내던 3곳(`/specialties/{dental,dermatology,plastic-surgery}`)
     — **이번 판이 세운 규칙을 정작 세 화면이 어기고 있었다.**
  8. `robots.txt` 의 죽은 선언 2줄(`Allow: /patient/education`·`/patient/visa`) — 실측하니 둘 다
     307 → `/login`(Disallow 대상). 크롤러는 그 화면을 본 적이 없다.

  **🔴 라운드3 — 시험이 «잠근다고 주장한 것»을 안 잠그고 있었다**
  9. `seoMeta.test.ts` 는 **「이미 localizedMeta 를 부르는 화면」만** 봤다 → 어떤 화면이 정적 제목으로
     되돌아가면 목록에서 그냥 빠지고 전부 초록. **판의 본체에 잠금이 0이었다.**
     `MUST_LOCALIZE` 29곳을 이름으로 박고, **돌연변이 시험으로 실제 실패를 확인**했다
     (`/login` 을 정적으로 되돌림 → 실패, 복구 → 통과).
  10. 같은 파일의 «인라인 base 금지» 검사가 파일 단위로 short-circuit → 두 번째 호출이 빠졌다. 호출 단위로 정정.
  11. `walk()` 가 `page.*` 만 걸어 새 `layout.*` 두 개가 검사에서 조용히 빠질 뻔했다. `layout` 도 걷게 확장.

  **🔴 내가 쓴 것을 내가 틀림 (같은 날 자기 정정 2건)**
  12. `KNOWN_ISSUES` 에 *「specialties 3곳이 크롤러에 열려 있어 검색 유입이 있을 수 있다」* 고 적었는데
      **틀렸다.** 실측하니 셋 다 `noindex` 였고 코드 주석에 **2026-06-17 PO 결정**이 그대로 있었다.
      `robots.txt` 의 `Allow` 는 크롤 허가일 뿐 색인이 아니다 — 페이지의 noindex 가 이긴다. 항목을 고쳐 썼다.
  13. `/opinion` 을 «환자용 화면인데 한국어로 샌다»로 읽고 **69개 문구를 번역할 뻔했다.**
      발송부(`app/api/coordinator/opinions/route.ts` `buildSummary`)를 읽으니 *"검사지·상세를 보시고
      소견 부탁드립니다"* — **받는 사람이 한국 전문의**였다. 본문 한국어가 «맞는 것»이다.
      번역을 취소하고, 잘못 쓴 커밋 메시지·주석·사전 문구(환자 시점 → 의사 시점)를 정정했다.
      **교훈: 화면을 고치기 전에 그 화면의 «독자»를 먼저 확인하라.**

- **검증 방법(정직)**: 프로덕션 빌드(`next build --webpack`) + `next start` 를 띄우고 쿠키
  `healo_lang=ru`/`kz`·`Accept-Language`·메신저 봇 UA 4종으로 실측. 로그인 없이 열리는 9곳은
  **Chromium 실브라우저 사진 + `document.title` 실값**으로 재서 PO 에게 냈다.
  ⚠️ 그 사진은 «저장소에 없다» — 세션 임시폴더에서 만들어 대화로 전달했을 뿐이라 세션이 끝나면 사라진다.
  다음에 같은 증거가 필요하면 다시 찍어야 한다(찍는 법: 프로덕션 빌드 기동 → Playwright 로
  쿠키 healo_lang 을 심고 각 주소 방문 → `page.title()` + 스크린샷).
  ⚠️ 서버를 새로 띄울 때마다 **옛 프로세스를 kill 했는지 확인**했다 — 이번 세션에 옛 서버가
  살아 있어 「안 고쳐졌다」는 거짓 판독이 한 번 났다(고침은 이미 들어가 있었다).
- **검증 못 함(정직)**: `/patient/*` 14곳 실화면 — 이 상자에 Supabase service_role 열쇠가 없어 로그인 불가.
  사전의 실제 값으로 대조만 했다(ru/kz 누락 0). 메신저 실카드도 못 봤다(실제 왓츠앱 발송 필요) —
  대신 봇 UA 4종으로 서버 응답을 쟀다.
- **안 고치고 기록만 한 것 3건** → `docs/KNOWN_ISSUES.md`:
  메신저 미리보기 카드가 봇에겐 영어인 것(고치는 recipe까지 적음) · 백오피스 탭 제목 60곳 · 피벗 전 미용 화면 3곳 삭제 여부.
- **라운드**: 3 (라운드3에서 나온 11이 마지막 신규 발견 — 이후 무발견)

## 2026-08-30 완성도 감사 (범위: diff — 전수 감사 세션 PR #1541, 합치기 관문)
- 스캔: DoD-3(문서-현실 드리프트) · DoD-7(시각·폰 폭 412px 실측) · DoD-2/6(조용한 실패) · DoD-1(6개어)
- **발견 2건 → 전부 수정** (둘 다 «직전 독립 리뷰 수리가 방금 만든» 드리프트 — 고치는 손이 문서를 낡게 한다):
  핸드오프의 시험 개수 «14개»→16개(회귀 시험 2개 추가 반영) · «1,481건»→1,483건.
- 실측한 것: 로컬 실서비스 빌드 기동 → 바뀐 공개 화면 4개(/ko/hospitals/immune·/ko/insurance·/ru·/kz)
  HTTP 200 + **새 통계값 실림·옛 값 0건**(93.5 있음/98% 없음, 73,7 있음/72,9 없음) +
  Playwright 폰 폭 412px 렌더에서 가로넘침 0px·통계 문구 잘림 0건.
  조용한 실패 부류는 독립 리뷰 게이트가 같은 날 3건(알림 curl -f 부재·청소 기아·경고 원문 노출)을 잡아 수리됨.
- 검증 못 함(정직): 초대 링크 만료 안내 실화면(만료 상태를 만들려면 DB 필요 — 문자열 6개어 구조는 검사로 확인) ·
  어드민 크롤 화면 실렌더(로그인 뒤편 — 라벨 매핑은 코드 대조).
- 라운드: 2 (1라운드 발견 2 → 수정, 2라운드 무발견 — 이후 무변경이라 종료)

## 2026-08-29 완성도 감사 (범위: diff — 앱에서 구글 로그인 잠금 + 안내 PR #1528)
- 스캔: DoD-5(죽은 링크·막다른길) · DoD-8(반쪽 배선) · DoD-7(시각·좁은 폭) · DoD-3(문서-현실) · DoD-1(6개어)
- **발견 5건 → 전부 수정.** 독립 리뷰 4 + 감사 1.
  🔴 **핵심은 「막다른 길」이었다.** 내가 앱에서 구글 버튼을 잠그면서 안내에
  *「비밀번호 찾기로 비밀번호를 만드세요」* 라고 적었는데, `forgot-password` 는 소셜 전용 계정이면
  재설정 메일을 **일부러 안 보내고** «로그인 화면의 Google 버튼을 누르세요» 안내를 보낸다.
  **즉 방금 잠근 버튼으로 되돌아온다.** → 실제로 되는 길(**폰 브라우저에서 열기**)로 바꿨고,
  그 길이 되는 이유도 실측했다(일반 브라우저는 `code-verifier` 쿠키를 SameSite=Lax 로 자기 통에 심는다).
- 독립 리뷰가 잡은 나머지: ①`/signup?provider=google` 자동 실행이 안 막혀 **앱에서 닫을 수 없는
  전체화면 오버레이**에 갇혔다(**잠그기 전보다 나빴다**) ②구글 입구가 3곳인데 2곳만 막았다
  (문의 퍼널의 「Google로 가입」이 그대로 열려 있었다) ③아이디 찾기 안내도 잠긴 버튼을 가리켰다.
- **감사가 추가로 잡은 1건 (전수 스캔)**: `forgot-password` 의 **소셜 안내 메일 본문**도 잠긴 버튼을
  가리키고 있었다. 메일은 앱/웹을 구분할 수 없으므로 «폰 브라우저에서 열어라» 경고를 한·영 양쪽에 넣었다.
- 전수 스캔 결과 구글 로그인 시작 지점은 **4곳(login·signup 버튼·signup 자동·퍼널)** 이 전부이고 모두 가드됨.
- 시각: 360×800(가장 좁은 흔한 안드로이드) ko·ru·kz × 로그인·가입 6조합 — **글자 잘림 0, 가로 넘침 0.**
- 깜빡임: 잠금·안내를 CSS(`html[data-healo-native="1"]`)로 내려 «첫 그림부터» 적용.
  수화를 기다리면 느린 웹뷰에서 멀쩡한 버튼이 잠깐 보이고 **그 틈에 눌리면 다시 갇힌다.**
- **가드 승격: 안 함.** 「잠긴 버튼을 가리키는 문구」는 자연어 대조라 기계 검출 조건이 안 선다
  (CLAUDE.md 규칙7 단서 ① 불통과). 대신 잠금 근거·지울 시점을 코드 주석 4곳에 못 박았다.
- 라운드: 2 (2라운드 무발견)

## 2026-08-28 완성도 감사 (범위: diff — 알림 딥링크 + 폰 목록↔상세 PR #1506)
- 스캔: DoD-5(죽은 링크·막다른길) · DoD-7(폰 반쪽·시각) · DoD-2(조용한 실패) · DoD-3(문서-현실) · DoD-1(6개어)
- **발견 14건 → 전부 수정.** 독립 리뷰 11 + 감사 3.
  독립 리뷰가 잡은 것 중 **3건은 그 세션에서 내가 방금 심은 것**이었다 — ①코디 메시지함 딥링크가
  첫 로딩 전 거름망을 바꿔 재조회가 건너뛰어져 «열리지 않았다» ②상세 칸을 폰에서 숨겼더니
  「검토 대기 큐」가 통째로 사라졌다 ③병원 의뢰 딥링크가 목록 밖이면 조용히 실패하고,
  나중에 갑자기 열리며 상태를 '열람'으로 바꿔 실적 집계를 오염시킬 수 있었다.
  **보안 1건**: `?inquiry=` 값이 안 걸러진 채 API 경로에 들어가 관리자 토큰으로 다른 API 를 부를 수 있었다.
- 감사가 추가로 잡은 3건: ①딥링크 재조회와 첫 조회의 **경합**(늦게 오는 쪽이 이김) →
  첫 로딩 뒤로 미뤄 한 번만 부르게 ②「왼쪽에서 대화를 선택하세요」가 **폰엔 왼쪽이 없어** 틀린 안내
  → 못 찾은 경우 전용 문구 6개어 신설 ③채점표(DoD-5)가 「파라미터 미해석은 정적분석 밖」이라
  적어둔 칸을 내가 메웠는데 장부를 안 닫았다 → rubric·DEFINITION_OF_DONE 같이 갱신.
- **가드 승격 1**: `npm run check:deeplinks` — `link:` 의 쿼리 딥링크를 모아 그 화면이 그 이름을
  실제로 읽는지 대조(삼항식 포함·자체시험 8가지·CI 차단). **이 부류는 세 번 터졌다**
  (2026-07-13 옛 대시보드 · 2026-08-28 코디 메시지함 · 같은 날 **검사가 스스로 잡은** 증상경보).
  본질은 「404 가 아니라 «목록»이 떠서 아무도 고장인 줄 모른다」는 것.
- **구조 정리**: 딥링크 받는 코드를 화면 5곳에 베껴 넣은 것이 위 ①③의 원인 → `useDeepLinkParam`
  공용 부품으로. `useSearchParams` 를 쓰므로 **이미 그 화면에 있는 사람이 종 알림을 눌러도** 열린다
  (예전엔 주소만 바뀌고 화면은 그대로였다).
- 라운드: 2 (2라운드 새 발견 0)
- ❌ **검증 못 한 것**: 실제 DB 의 알림을 눌러본 것은 아니다 — 이 클라우드 상자엔 Supabase 열쇠가
  없어 로그인 뒤 화면을 못 띄운다. 배치·폭은 실측(정적 복제 + Playwright), 실제 클릭은 배포 후 폰에서.

## 2026-08-19(오후) 완성도 감사 (범위: 환자 의뢰서 여정 전수 — 사람 클릭 → 기계 클릭 이관)
- 스캔: DoD-7(시각·클리핑) · DoD-2(조용한 실패: 첫 방문자 경로) · a11y(낭독기 상태) · 다국어(ru/kz)
- **발견 3건 → 전부 수정.** ①쿠키 동의 띠가 「보내기」를 덮어 **첫 방문자가 접수를 못 할 수 있었다**(자동 클릭이 잡음).
  ②동의 5칸이 낭독기에 「켜짐/꺼짐」을 안 알림(법적 기록) ③접힘 묶음이 「펼침/접힘」을 안 알림.
- 수정: 띠 회피를 ClientShell 층에서 일괄 + 어드민 알림 2곳 · aria 속성 2종 · 잘림 탐지기 공용화.
- **가드 승격 2**: `bottomBarCookieOffset.test.ts`(바닥 고정 요소는 `--cookie-banner-h` 필수 — 같은 사고 3번째) ·
  `e2e/referral-form.spec.ts`(여정 9가지 자동 클릭: 접수·전체 20칸·코디 화면·환자 언어·폰 잘림).
- 라운드: 2 (2라운드에서 새 발견 0 — 기존 공개페이지 잘림 스윕 4.5분 재실행 포함)

## 2026-08-19 완성도 감사 (범위: diff — 환자 의뢰서 개편 PR #1414)
- 스캔: DoD-2(조용한 실패 — 서버 저장·첨부·집계 끝단) · DoD-3(문서-현실: PROJECT_MAP·핸드오프·매뉴얼) · DoD-7(시각 — 6언어×2폭 스윕) + 독립 리뷰 5명(코드리뷰 스킬 xhigh)
- **발견 15건 → 13 수정 · 2 보류.** 유형2가 대부분: zod 가 모르는 키를 «조용히» 버려 cdFolder.path·stage 가 사라짐, 진단시기가 읽는 쪽과 다른 컬럼(평문)에 저장, 브리프가 의뢰서 칸을 못 읽음, 업로드 형식 별칭 불일치로 «올렸는데 사라짐», 첨부 경로 소유검사 누락(보안).
- 수정: 위 전부 + GA 깔때기·localizedMeta·patientLang enum·메일 결과 로그·임시저장 TTL. **가드 승격**: `src/lib/inquiry/referralSchemaDrift.test.ts` (화면 칸↔zod↔저장 대조 — 이 부류의 재발을 CI 가 막는다).
- 보류(PO 판단 아님, 후속 작업): classify-doc HMAC 티켓 · 순차 업로드/디바운스 · 포털 상세 select 합치기.
- 라운드: 2 (2라운드 리뷰어 B+C 8건이 마지막 발견, 3라운드 = 실측 #131 로 무발견)

## 2026-08-07 완성도 감사 (범위: diff — 비자 안내 문구 갱신, PR #1345)
- 스캔: DoD-1(한글 누출) · DoD-3(문서-현실 드리프트) · DoD-7(시각 완결성 — 360px, ko/ru/kz/en 4개 언어)
- **발견 2건 → 전부 수정 (DoD-3 문서-현실 드리프트).** 둘 다 같은 부류다: **내가 이번에 「숫자가 확정 안 됐다」고 적어놓고, 다른 문장에서는 그 숫자를 여전히 단정하고 있었다.**
  1. `docs/비자_대행_매뉴얼.md` 로드맵 — 같은 문서 §9 에서 「30건/50건/200명으로 갈린다」고 써놓고 15줄 아래에서 *"KHIDI 12건이 요건 30건의 40%"* 라고 비율을 단정. → 비율 사용 금지 문구로 교체.
  2. `docs/PROJECT_CONTEXT.md:1168` — *"지정 요건 = 초청 실적 30건"* 을 확정 사실로 적고 40% 계산까지. → 정정 + 확인처를 매뉴얼 §9 로 연결.
- 무발견(실측 근거):
  - DoD-1: `notes`/`note` 의 en·ru·zh·ja·kz 값에 한글 **0건**(정규식 전수). 6개 언어 칸 누락 **0건**.
  - DoD-7: **360px**(iPhone SE 보다 좁게) 에서 ko·ru·kz·en 4개 언어 — 문서 가로 넘침 **-15px(없음)**, 화면 밖으로 삐져나온 요소 **0개**. 문구가 길어진 자리(참고 박스·국가 안내)가 정상 줄바꿈됨.
  - DoD-3(코드↔문서): 옛 값(`약 5~7 영업일`·`USD 40`·`USD 60`·`7~14 영업일`)이 코드·문서 어디에도 **잔존 0건**.
- **보류/안 고친 것**: `docs/archive/PROJECT_CONTEXT_handoffs.md:3829` 에도 같은 「30건 = 40%」 단정이 있으나 **보관(archive)은 그 날짜에 무엇을 믿었는지의 기록**이라 사후 수정하지 않았다. 살아있는 문서 2곳만 고쳤다.
- 라운드: 3 (2·3라운드 연속 무발견으로 종료)
- ⚠️ 이 감사와 별개로 **독립 리뷰 게이트(`/code-review`)가 먼저 정합성 결함 4건**을 잡았고 전부 고친 뒤 이 감사를 돌렸다(국적 무관 데이터에 국적별 숫자 하드코딩 등). 두 관문이 서로 다른 것을 잡았다는 게 이번 기록의 요점.

## 2026-07-28 완성도 감사 ③ (범위: diff — 편집기 줄별 「언어 배지」 신설)
- 스캔: DoD-2(성공 실측) · DoD-3(문서-현실 드리프트) · DoD-7(시각 완결성 — 375px·1280px)
- **발견 1건 → 수정: DoD-3.** 오늘 실사고(코디 «카자흐어 저장한 게 사라졌다»)를 진단하고 고쳐놓고도 **반성문이 없었다**(규칙상 필수). → `#144` 작성 + `KNOWN_ISSUES` 에 후속 과제(편집 언어를 계정에 기억) 등재.
- 무발견(실측 근거):
  - DoD-7: 375px·1280px 둘 다 라벨 0폭 0건 · 배지 잘림 0 · 가로 넘침 없음(모바일엔 배지가 아랫줄로 자연스럽게 접힌다).
  - DoD-2: 되돌리기(빈 값 저장) 직후 배지가 꺼지고 원복하면 다시 켜지는 것까지 실측(값도 복구 확인). 분포 73줄 중 30발화.
- 라운드: 2 (2라운드 무발견 도달)
- **정직**: 화면낭독기 실기기 검증은 못 했다(aria 속성은 DOM 실측). 프로덕션 실화면은 다음 배포 창구 뒤 확인 대상.

## 2026-07-28 완성도 감사 ② (범위: diff — 콘텐츠 편집기 「저장 전 미리보기」 신설, 반성문 #140 방어)
- 스캔: DoD-2(성공 실측) · DoD-3(문서-현실 드리프트) · DoD-7(시각 완결성 — 확인창을 모바일 375px·데스크톱 1280px 두 폭에서)
- **발견 1건 → 수정: DoD-3 드리프트.** 사용설명서가 「**항목 이름**과 이전→새 문구를 훑어보라」고 하는데, 사전 키(`intakeLabels.cancer.*` 등)는 사람이 읽는 이름이 없어 화면엔 **키 경로만** 뜬다. → 「어떤 항목인지(이름이 있으면 이름, 없으면 항목 코드)」로 문구 교정 + 반성문 #140 의 기능 서술도 실제(라벨 병기·목록 얼림·줄바꿈 보존)에 맞게 보강.
- 무발견(실측 근거):
  - DoD-7: 8줄 + 300자 초장문·줄바꿈 포함 값으로 열었을 때 — 창이 화면 밖으로 안 나감(모바일 595/700px), 목록 자체 스크롤 동작, 가로 넘침 0, 칩 가로 넘침 0, 「이대로 저장」 버튼 뷰포트 안. 쿠키 배너에 덮이지 않음(`z-[10000]`).
  - DoD-2: ESC·「다시 볼게요」로 닫으면 저장 안 됨을 실측(«저장됨» 미출현). 확인창을 연 뒤 검색이 바뀌어도 목록 문자열 불변(얼림). 실패 시 에러 메시지 경로 유지.
- 라운드: 2 (2라운드 무발견 도달)
- **정직**: 스크린리더 실기기 검증은 못 했다(포커스 이동·Tab 순환·ESC 는 DOM 실측). 프로덕션 실화면은 머지 후 확인 대상.

## 2026-07-28 완성도 감사 (범위: diff — PR #1065 콘텐츠 편집기 변경이력 「이전 값」 수리 + 전후비교 색 + 문구중복 가드)
- 스캔: DoD-2(성공 실측) · DoD-3(문서-현실 드리프트) · DoD-7(시각 완결성 — 로그인 뒤편 백오피스 = 공개 스윕의 사각)
- **발견 1건 → 수정: DoD-3 드리프트 (내가 직접 만든 것)**. 독립 리뷰 반영으로 「문구 중복」 판정 범위를 *화면 전체 → 같은 묶음 형제끼리* 로 좁혔는데 **같은 커밋에서 문서를 안 고쳐**, 반성문 #136 과 코디 사용설명서가 옛 동작을 설명하고 있었다. 둘 다 수정. — 리뷰 지적대로 코드를 고칠 때 **그 코드를 설명하는 문서도 같은 diff 안에 있다**는 걸 놓친 자리.
- 무발견(실측 근거):
  - DoD-2: 이력 50줄 전부 이전값 표시(`(없음)` 0건) · 오탐 케이스 «Другое» 18칸·«Рак лёгких» 16칸 → 배지 0 · 진짜 중복을 만들자 배지 2 (전부 로컬 dev + 코디 실로그인 실측). 저장 유실 여부는 변경로그 145건 ↔ `content_overrides` 전수 대조로 0건 확인.
  - DoD-7: 이력 화면 1280px·900px 두 폭에서 색칸 잘림 0 · 문서 가로넘침 없음(최장 문구 446자 정상 줄바꿈) · 배지 추가로 인한 헤더줄 압착 없음.
- 라운드: 2 (2라운드 무발견 도달)
- **정직**: 이 화면은 한국어 전용 백오피스라 6개어 변형 잘림은 대상 아님. 프로덕션 실화면은 미검증(머지 후 확인 대상).

## 2026-07-27 완성도 감사 (범위: diff — PR #1046 렌더 중 브라우저값 읽기 5곳 + #1047 루트 레이아웃 언어 결정)
- 스캔: DoD-2(성공 보고 실측: 포털 SSR 언어를 6개어 전부 curl 로 실측) · DoD-3(문서-현실 드리프트: KNOWN_ISSUES·POSTMORTEMS 가 «/patient 서버 영어» 를 미해결로 적어둔 채 같은 날 코드가 고쳐지는 중) · 반쪽배선(SurveyForm 진입점·`pickLang` 소비처 전수) · 캐시 회귀(루트 레이아웃 동적화 여부)
- 발견 1건 → 수정: **DoD-3 드리프트** — `KNOWN_ISSUES.md` 최상단 「환자 포털 영문 깜빡임」 항목과 `POSTMORTEMS #133` 의 "여전히 서버 HTML 이 영어다" 서술이 #1047 로 사실이 아니게 됨. **같은 PR 안에서 둘 다 닫음**(#63 부류 예방). 이건 #133 의 요지("«후속 권장» 을 안 닫으면 20일 방치된다")를 그대로 재현할 뻔한 자리였다.
- 무발견: 6개어 SSR 전부 정확(ko/en/ru/kz→kk/zh/ja, 잘못된 쿠키값·쿠키없음 → en 폴백) · 공개 경로(`/ru`·`/kz`·`/en`)는 쿠키 무관하게 URL 언어 유지 · SurveyForm 진입점 1곳뿐(반쪽배선 없음) · 캐시 회귀 없음(루트 레이아웃은 변경 **이전에도** `cookies()` 호출 → 이미 전 라우트 동적)
- **라운드 3 (독립 리뷰가 이 감사의 사각을 드러냄 — 정직하게 기록)**: 위 "무발견"은 **스캔 범위가 결함을 안 지나갔기 때문**이었다. 리뷰가 잡은 진짜 결함 = **`LANG_OPTIONS`(21개) ↔ `HTML_LANG`(6개) 집합 불일치** — 옛 언어 쿠키(`vi`·`ar` 등)에서 「본문은 그 언어인데 `<html lang="en">`」. 내가 6개어만 훑어서 못 봤다. 교훈: **«N개 언어 실측» 은 «검증 로직이 허용하는 집합» 전체를 쳐야 한다** — 활성 목록만 치면 허용 집합의 구멍을 영영 못 본다.
  - 수정: 서버 렌더 언어 검증을 `LOCALES`(6) 로, 사전 주입은 `LANG_OPTIONS`(21) 로 **용도를 분리**(한 변수로 합친 게 원인). `check:content` 에 「`HTML_LANG` ⊇ `LOCALES`」 가드 신설 + 서버 검증이 `LANG_OPTIONS` 로 되돌아가는 것도 차단. 가드가 실제로 무는 것 확인(ja 매핑을 잠시 빼서 실패 재현 → 원복 후 통과).
  - 문서 정정 3건: 「전부 noindex」(사실 아님 — `/agency`·`/clinic`·`/notifications` 는 색인 가능 → 별도 항목으로 KNOWN_ISSUES 에 올림) · 「잘못된 쿠키값 → en」(수정 전엔 `vi` 가 «유효» 였으므로 거짓이었다) · 적용 범위가 «포털만» 이 아니라 «x-locale 없는 모든 경로» 라는 스코프 차이 명기.
- 검증 못 함(정직 표기): **로그인 뒤 `/patient` 실화면**(하단 탭 라벨이 러시아어·카자흐어로 길어졌을 때 잘리는지 = DoD-7 시각). 인증 게이트라 자동화로 못 봄 — `/login` 까지만 실렌더 확인했다. 라벨은 기존 i18n 키 그대로라 길이 변화는 «영어→해당언어» 한 번뿐이고, 그건 원래도 하이드레이션 후 발생하던 것(신규 위험 아님).
- 라운드: 3 (2라운드 무발견이었으나 독립 리뷰가 사각을 드러내 3라운드에서 진짜 결함 1건 수정)

---

## 2026-07-24 완성도 감사 (범위: diff — PR #951 메신저 대화 연속성+멘트 단락화)
- 스캔: i18n 6개어+kk(단락화한 4맵 — 파일약속/시간약속 금지·①② 존재는 기존 계약테스트가 재검증) · 문서 드리프트(admin 설명서 "사람 답장 후 AI 침묵"·"재상담=재동의" 서술 = 새 판정(종료시만 재동의)과 모순 없음 확인) · 반쪽배선(findOpenThread 호출부 텔레그램·왓츠앱 2곳 전수 — 웹 챗은 별도 판정(resolved||closed) 이미 동일 의미)
- 수정: 발견 0건 (수정 없음)
- 검증 못 함(정직 표기): 실기기 왕복(코디 답장 → 환자 재발신 시 재동의 안 묻는지) — 머지·배포 후 PO 텔레그램 메시지 1건으로 확인 가능
- 라운드: 2 (무발견 도달)

---

## 2026-07-24 완성도 감사 (범위: diff — PR #948 사후관리 D+ 케이던스)
- 스캔: 유형3(문서-현실 드리프트: KNOWN_ISSUES 설문·rescue 장부 vs 실코드/실DB) · 한글누출/언어누락(환자 배지 CADENCE_LABELS 6개어) · 죽은링크(종 알림 `/coordinator/inbox/[id]` 라우트 실재) · 반쪽배선(제안→환자 포털 GET/PATCH·벨 렌더 title/body/link 타입불문 — 코드 추적) · KHIDI §4 월별 로그(작업습관)
- 수정: ①KNOWN_ISSUES 🔴 설문 항목 제목·본문 7/24 실측 갱신(발송1·응답1 + #948) ②rescue 장부에서 설문·kpiHealthcheck 확장분 회수 완료 처리 ③KHIDI §4 7월 로그에 케이던스 1줄(K-02·K-03 연결)
- 발견(PO 대기): 응답 설문의 inquiry #39 가 `is_test=false`인데 7/23 기록은 "만족도 1건=PO 테스트" — 정체 확인 필요(테스트면 도장 → K-03 오염 방지)
- 검증 못 함(정직 표기): 환자 재예약 화면 케이던스 배지 실렌더(로그인 게이트 — 코드·6개어 사전만 확인) · 실이메일 수신(다음 cron 사이클)
- 라운드: 2 (무발견 도달)

## 2026-07-24 완성도 감사 (범위: diff — PR #946 핸드오프 사전질문+1회 수신확인)
- 스캔: i18n 6개어+kk(신규 ack 맵·수정 confirm 3맵, 파일약속/시간약속 금지 계약테스트로 고정) · 같은 부류 전수(핸드오프 안내 하드코딩 통로 — "has been notified" 류 전수 grep) · 설명서 동기(admin §텔레그램 봇 상담 응대)
- 수정: ①환자 포털 챗(`app/api/patient/chat`)이 연결 요청 시 **영어 하드코딩 한 줄**만 붙임(러 환자도 영어·사전질문 없음) → `pickHandoffConfirm(lang, true, true)`로 통일 ②admin 설명서 "AI는 침묵" 서술을 새 동작(즉시 문의 등록+사전질문+1회 수신확인)으로 갱신+updated 7-24
- 비발견: inquiry/ThreadChat(중앙사전 t()+en 폴백)·patient 클라이언트 토스트(6개어 로컬사전)는 이미 다국어 — 통과
- 검증 못 함(정직 표기): 실기기 텔레그램에서 사전질문→답변→1회 ack 실흐름(봇 대화 필요 — 계약테스트로만 고정)
- 라운드: 2 (무발견 도달)

---

## 2026-07-24 완성도 감사 (범위: diff — PR #942 코디 메신저 릴레이·채널 배지)
- 스캔: i18n 6개어 완전성(신규 3키, {channel} 자리표시자 포함 실검증) · 같은 부류 전수(스태프→chat_messages 삽입 통로 10개 라우트 전수 — 릴레이 필요 통로는 admin·portal 2개뿐, 둘 다 이번에 공용 모듈로 배선. resolve=playbook_responses 학습 드래프트라 비대상, 병원·에이전시=스태프 간 채널) · 설명서 동기(코디 §메시지 updated 7-24)
- 수정: 발견 0건 (수정 없음)
- 검증 못 함(정직 표기): 백오피스 로그인 뒤 실화면(채널 배지·미전달 표시·버튼 겹침 해소)은 스윕·프리뷰 자동화 밖 — PO 실클릭 항목
- 라운드: 2 (무발견 도달)

---

## 2026-07-24 PR #944 편집기 재검색·홈 전체 개방·블록 편집 (범위: diff)

- 스캔: 유형 1·2·3·5(문서드리프트)·7 / **발견 1건** (설명서 드리프트)
- **발견·수정**: 코디 설명서(`src/lib/manuals/index.js`)에 「콘텐츠 편집」 메뉴 항목이 **어제 편집기 출시(#918) 때부터 통째로 누락** — 이번 PR에서 ko·en·ru 3개 언어 추가 + updated 2026-07-24 (같은 PR 갱신 규칙 소급 준수).
- **유형3(유령 배선) 무발견**: 레지스트리 자동 등록이 노출하는 홈 섹션 11종(hero~bottomCta·misc·faq·emergency) 전부 `HomeClient.jsx`가 실제 렌더함을 grep 실측 — "편집돼도 아무 데도 안 보이는 키" 0. 신규 `matched` 필드는 클라 배지가 소비 ✓.
- **유형1**: 설명서 en·ru 추가분에 한글 누출 0, check:content 통과.
- **유형7(시각)**: textarea 전환·섹션 머리글은 **실클릭 검증 못 함**(코디 로그인 게이트 — 자동화 불가, 기억 `verify_authgated_portal`). 배포 후 검색 API 실호출 + PO 실사용으로 확인 예정.
- 라운드: 3 (2연속 무발견 도달) / 회귀 테스트 7건 추가(`registry.test.ts`), 반성문 #114.

## 2026-07-22 PR #877 푸터 사업자 정보 6개어 (범위: diff)

- 스캔: 유형 1·2·3·7 / **발견 1건** (유형3 1건 · 나머지 미달 0)
- **유형3**: 이번 PR이 새 가드(`check:content §27`)와 새 부류(#108)를 만들었는데 **판정 기준 SoR(`rubric.js` DoD-1)에는 반영이 없었다** — 루브릭이 코드 현실보다 뒤처지는 드리프트. → DoD-1 에 ⓐ`done` 항목 추가("ko 화면에 영어 라벨 하드코딩 0") ⓑ`guards` 에 §27 ⓒ`postmortems` 에 108 ⓓ`gap` 에 **방향 사각**(기존 가드가 전부 "비-ko 화면의 한글"만 보고 그 반대는 무검사였음) 명시. `DEFINITION_OF_DONE.md` 도 같이 갱신(`check:completeness` 가 쌍을 강제).
- **유형1**: `check:content` i18n 키 패리티 통과 + 6개 언어 실렌더 10줄 전부 확인(빈 값·`undefined`·키 이름 노출 0).
- **유형2**: PR 본문 주장 ↔ 최종 코드 대조 — 일치. 옛 키(`guaranteeInsurance`) 참조 잔재 0건 실측.
- **유형7**: 모바일 375px 러시아어(라벨 최장) 실측 — 11줄, **넘침 0**, 가로 스크롤 0. 최장 줄은 2줄로 자연 줄바꿈(32px).
- 라운드: 2 (2라운드 무발견)
- 검증: `tsc` 0 · `vitest` 578 · `next build --webpack` 0 · `check:content`(§27 양방향 돌연변이) · `check:completeness`

---

## 2026-07-22 PR #872 러/카 랜딩 언어전환 404 (범위: diff)

- 스캔: 유형 2·3·7 / **발견 3건** (유형3 2건 · 유형2 1건 · 유형7 0건)
- **유형3-①**: `docs/KNOWN_ISSUES.md:306` 이 이 랜딩들의 살아있는 SoR 인데 ⓐ이번 수정(언어전환 404)이 안 적혔고 ⓑ **이제 존재하지 않는 심볼 `proxy.ts` `LEGACY_SKIP`** 을 가리키는 죽은 참조였다(같은 PR에서 삭제됨). → 항목을 `LEGACY_LANDINGS`/`isLegacyLanding()` SoR 로 갱신 + 2026-07-22 해결 종결.
- **유형3-②(고치지 않음, 의도)**: `docs/POSTMORTEMS.md:765`(#77)·`docs/archive/PROJECT_CONTEXT_handoffs.md` 도 `LEGACY_SKIP` 을 참조하지만 **반성문·아카이브는 그 시점의 사실 기록**이라 현재형으로 고치면 역사가 왜곡된다 → 그대로 둠. 살아있는 문서(KNOWN_ISSUES)만 갱신하는 것이 원칙.
- **유형2**: PR #872 본문이 *"proxy.ts 의 LEGACY_SKIP 이 이 상수를 import"* 라고 주장했는데, 독립 리뷰 반영 2번째 커밋에서 **판정식까지 함수로 통합**해 `LEGACY_SKIP` 자체가 사라졌다 → **보고 ↔ 코드 불일치**. PR 본문 전면 갱신. (첫 커밋 기준으로 쓴 설명이 두 번째 커밋 뒤 그대로 남는 부류 — 리뷰 반영 후 PR 본문 재확인이 필요하다는 교훈.)
- **유형7**: 푸터 라벨 2개 실렌더 실측 — 모바일 375px 에서 잘림 0(ru 149px·kz 173px, 각 1줄 20px), 가로 스크롤 0, hydration 에러 0.
- 라운드: 2 (2라운드 무발견. 2라운드에서 수정이 0건이었으므로 3라운드는 동일 입력 재실행이라 생략 — 정직하게 밝힘)
- 검증: `tsc` 0 · `vitest` 578 · `next build --webpack` 0 · `check:content` · 실브라우저 언어전환 재클릭

---

## 2026-07-20 PR #831 치료 유령 컬럼 (범위: diff)

- 스캔: 유형 1·2·7 / **발견 3건 (전부 「내가 방금 만든」 반쪽배선 — 독립 리뷰 2라운드가 못 본 각도)**
- **유형1-①**: 새로 표시하게 만든 `duration`·`recovery_time`·`preparation`·`risks` 가 **번역 대상에 없고** 공개 상세에서 `localize()` 도 안 거쳤다 → 어드민이 한국어로 입력하면 **러시아·카자흐 환자 화면에 그 칸만 한국어**로 뜬다. 지금은 값이 전부 null 이라 안 터졌을 뿐, PO가 쓰는 순간 새는 구조였다.
  → `TranslatableFields`·`extractTranslatableFields`·프롬프트에 4개 추가 + 읽기 경로 `localize()` 경유. `risks`·`preparation` 은 **환자 안전 문구**라 "축약·생략 금지, 항목 누락은 사고" 규칙을 프롬프트에 명시.
  → 같은 부류 **`full_description`** 도 읽기는 `localize()` 인데 번역 대상엔 없었다(기존 구멍) → 같이 메움.
- **유형1-②(2라운드에서 발견)**: 번역 트리거 조건이 `name || description || tags` 손나열이라 **「주의사항」만 고치면 번역이 안 돌아** 그 칸만 영원히 한국어로 남는다. 호출부 6곳이 제각각(병원은 `+ specialties`, 프로필은 `+ location_kr`) → 단일 SoR `hasTranslatableField()` 로 통일. 번역 대상이 늘어도 조건을 빠뜨릴 수 없다.
- **유형7**: 어드민 폼(`TreatmentManager.jsx`) ↔ 저장 payload(`page.jsx`) **필드 집합 15개 완전 일치** 확인(입력칸 있는데 저장 안 되는 칸 0, 반대도 0).
- 보류(고치지 않음): `address_detail`(병원 주소 상세)도 `localize()` 없이 원문 노출 — **기존 것이고 주소는 한국어 표기가 오히려 유용**할 수 있어 PO 판단 몫으로 남김.
- 라운드: 3 (3라운드 무발견 도달)
- 검증: `tsc` 0 · `lint` 에러 0 · `vitest` 64파일 566개 · `next build --webpack` · `check:content` · `check:schema-refs`(자기시험 19개)

---

## 2026-07-15 축 C #2 (유형 6 — 컬럼레벨 schema-refs + stale 생성타입 발견·재생성 + 실버그 2건)

- 작업: `check:schema-refs`를 테이블 레벨 → **평문 select 컬럼 레벨**로 확장(생성타입 `src/types/database.types.ts` 대조, 비차단·경고).
- **발견①(도구가 도구를 고침)**: 확장 직후 22건 경고 → 전부 실재 컬럼이라 이상 → 생성타입이 **stale**(inquiries 35 vs 실DB 61 컬럼, Supabase MCP 실측)임을 발견. **생성타입 재생성**(148KB, 26컬럼 복원) = #63 문서-현실 드리프트를 코드 타입에 적용.
- **발견②(실버그 2건 수확→수리 완료, DB 실측 대조)**: 재생성 후에도 남은 = 진짜 없는 컬럼 참조 →
  1. ✅ `reminders/scheduleReminder.ts` profiles 없는컬럼 5개 → 등록사용자 리마인더 무증상 실패. **수리**: 실컬럼(full_name·role)만 + 이메일 auth.users 조회 + userId로 in_app 보장.
  2. ✅ `crawl/jobs/[id]/items/route.ts` crawl_raw_items.name(실=title) → **수리**: name:title alias + ilike("title").
  + tsc 회귀 1건(재생성 타입에서 inquiries.intake=Json spread 불가, step2/route) 수리.
- 정책: 컬럼레벨 우선 비차단(경고) — 재생성 직후 파서 엣지 대비. 안정 후 blocking 승격.
- rubric DoD-6 + DEFINITION_OF_DONE + KNOWN_ISSUES 동기 갱신.
- 의미: 유형6 가드 확장 한 번에 (a)stale 타입 발견·수리 (b)조용한 0 실버그 2건을 PO 화면 없이 수확 = 완성도 루프가 "그물 밖 통로"를 실제로 메움.

## 2026-07-15 축 C #1 (범위 무한정화 — 유형 1·5 가드 확장 + 실누출 수확)

- 작업: check:content 한글누출 가드 2룰(§4 줄단위·§7 파일단위)의 폴더 화이트리스트(app/patient+3폴더)를 `isPublicFacingFile()`(공개 화이트리스트 ∧ ¬백오피스 ∧ ¬api) 판정으로 확장 + 동적링크 404 검사 app/→src/ 확장.
- 측정→수확: 확장 직후 findings 소수(§7 2·§4 2)라 홍수 없음. 진짜 누출 2건 6개어화(**미완성→완성**):
  1. `app/inquiry/_components/UnifiedInquiryFunnel.jsx:946` — 업로드 힌트 "최대 10MB · 5개" 통짜 한글 → `tl("uploadHint")` 6개어 추가.
  2. (§7) 공개 컴포넌트 스캔이 이제 마케팅/환자 퍼널 전체를 봄.
- 오탐 정밀 제외(의도적 한국어 보호): ① `app/opinion`(국내 의사용 소견 화면) → 백오피스 제외목록으로 ② `src/components/GoogleMap.jsx`(한글=지오코딩 매칭 리터럴, 표시 아님) → allow ③ `대한민국 / Korea`(국가선택 자국명, 中国/日本과 동일 패턴) → allow.
- #81(costs 사각)·#73(알림링크 사각)은 **이미 해소돼 있었음**을 매핑으로 확인 — 재수리 아님, 진짜 남은 사각(공개 퍼널)만 닫음.
- rubric DoD-1·DoD-5 gap/guards + DEFINITION_OF_DONE.md 동기 갱신(check:completeness 정합 통과).
- 남음(축 C 잔여): 컬럼레벨 schema-refs(유형6) + 공통가정 소비자 전수 스캐너.

## 2026-07-15 완성도 감사 #1 (유형 3 문서-현실 드리프트 · 첫 실가동)

- 스캔: 유형 3(문서-현실 드리프트) — 살아있는 문서(KNOWN_ISSUES·PROJECT_CONTEXT 핸드오프·CLAUDE·DESIGN·manuals) 근거 대조.
- 발견: **드리프트 3 · 죽은참조 0 · 역드리프트 0** (+ 판단필요/DB확인 1).
- 수정(사실 교정, 확신 높음 3건 전부 종결):
  1. KNOWN_ISSUES #88 — consultation notes 이미 AES-256-GCM 암호화 완료인데 "미암호화 잔존"으로 남아 있던 것 → 종결.
  2. PROJECT_CONTEXT — "끝냈지만 미머지" 3건(#562·#567·#545) 실제론 전부 main 머지됨 → 머지완료로 교정 + 브랜치정리 안내 갱신.
  3. PROJECT_CONTEXT 리브랜드 TODO — PNG 앱아이콘·`healwith.co.kr` 도메인 둘 다 완료인데 TODO로 남아 문서 간 모순(KNOWN_ISSUES:358·관문12와 배치) → 종결.
- 보류(PO/DB 확인): playbook_pattern 0건 → "3-Tier RAG" 실제 1-Tier 여부(DB row 실측 필요, KHIDI 점수 연관). 정적 확인 불가라 남김.
- 라운드: 1 (유형 3 확신 높음은 소진).
- 유형 7(시각 회귀): rubric DoD-7에 `content-clip-sweep` 배선 완료 + 나이틀리 CI(프로덕션)에서 상시 가동. 이번 세션 ad-hoc 프리뷰 실행은 node_modules 없음(fresh clone)이라 불가 — `npm ci` 필요. 나이틀리가 커버하므로 별도 실행은 필요 시.
- 의미: **루프 첫 실가동에서 #63 부류 드리프트 3건을 PO 스크린샷 없이 기계가 수확·종결** = "미완성→완성" 첫 성과 + 후속 세션의 허위 재발견 헛수고 3건 예방.

## 2026-07-15 완성도 감사 (시스템 구축 — 기준선)

- 스캔: 구축일이라 전수 감사는 생략. 7유형 등록 + `check:completeness` SoR 무결성 게이트 통과 확인.
- 수정: 없음 (골격 착수 — 문서+루프 뼈대) / 보류: 없음
- 라운드: 0 (다음 세션부터 유형 3·7 대상 정식 감사)
- 메모: 골격 첫 타깃 = 유형 3(문서-현실 드리프트)·유형 7(시각 회귀) = 지금도 PO 눈이 유일한 탐지기인 두 유형. 축 B(cron·자동머지 배선)·축 C(범위 무한정)는 후속.
