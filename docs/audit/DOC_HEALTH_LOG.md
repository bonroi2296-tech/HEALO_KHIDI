# 문서 건강검진 로그 (주 1회 `/doc-health`)

> 문서 = 어시스턴트의 장기기억. 이 로그는 그 기억이 썩지 않는지 주 1회 검진한 기록이다.
> 최상단 엔트리 날짜를 세션 시작 훅이 읽어 **7일 경과 시 리마인드**를 띄운다. (2026-07-05 PO 승인)
> 형식은 `.claude/skills/doc-health/SKILL.md` 참고. 새 엔트리는 이 안내문 바로 아래에 추가.

---

## 2026-08-30 검진 (전수 감사의 문서 렌즈로 수행 — 직전 검진 후 27일, ⚠️ 주 1회 규칙 3주 미달)

- **스캔**: 살아있는 문서(`KNOWN_ISSUES`·`PROJECT_CONTEXT`·`PREVIEW`·`AUTOMERGE`·`SELF_QA`·`DEPLOY`·`GA4_SETUP` 등) + 코드·CI·원격 브랜치 41개 실측 대조. 단독 `/doc-health` 가 아니라 **2026-08-30 전수 감사**(다각도 finder → 발견마다 반증 검사, 42건 확정)의 일부로 수행 — 검진 방법은 08-03 때 박은 「상태값은 전수 grep 대조」 그대로.
- **발견**: 모순 **1**(AUTOMERGE 「프리뷰 링크 남겨라」 vs PO 2026-07-31 프리뷰 전면 차단 결정) · 드리프트 **7** · 죽은참조 **2** · 기한경과 **3**

**교정 (이번에 고침 — 전부 실측 근거 병기, PR #1541)**

1. **`KNOWN_ISSUES` 낡은 「미해결」 4건 닫음**: 환자 실명·임상요약 평문 2건(#1392 가 2026-08-14 에 이미 암호화 — `*_encrypted` 코드 실존 확인) · Gemini 비용 구멍(#1388 완료) · worker-src(`next.config.js:197` 실존) · 앱 뒤로가기 버전 표(스토어 10/1.0.9 실측 갱신).
2. **`PREVIEW.md` 거짓 2건**: E2E 역할 5개→6개(hospital 누락 — `e2e/auth.setup.ts` 실측) · 「E2E_* 키 없음」→`.env.example:157~168` 에 12키 실존.
3. **`AUTOMERGE.md:20` 모순 해소**: 「프리뷰 링크를 남겨라」를 폐기 표기 — «큰 UI 변경 막지 말라» 의도는 유지, 수단만 로컬 전/후 사진으로(2026-07-31 차단 결정 정렬).
4. **죽은참조 2건**: GA4 보관 문서가 가리키던 측정 기록 → `docs/PROBELLE_MEASUREMENT.md` 신설로 해소 · `check-content-consistency.mjs` §8 「src/emails 별도 전환 예정」 주석 → 실제 결말(2026-07-01 PO 지시, PR #539 로 삭제됨)로 정정.
5. 🔴 **역방향 드리프트(문서엔 «있다», 코드에 없음) 1건 — 문서를 고치지 않고 코드를 문서에 맞췄다**: 시험 문의 30일 자동 청소(purge-recordings 크론 배선)를 문서·과거 PR 메시지가 «있다»고 서술했는데 코드는 죽은 브랜치(docs/ict6-handoff)에만 있었다 → cherry-pick 으로 배선 복구(9e8e5861).
6. **기한경과 3건에 ⏰ 표식**: 8/07 타임라인 · 8/20 갤러리(`HospitalDetailLegacyClient.jsx:333` 코드상 미수리 병기) · 8/27 사전상담 43건(PO 영역 — 판단 없이 표식만).
7. **핸드오프 유실 10블록 소급 복원**(`docs/archive/PROJECT_CONTEXT_handoffs.md`, 2026-07-28~08-20 분) + 그 안의 유효 규칙 2건 이식(`SELF_QA` 「진짜야?」 실증 절 · `DEPLOY` 주소 고정 절).

**보류**: `KNOWN_ISSUES` 「🔎 2026-08-30 전수 감사 — 확인했지만 안 고친 것」 12건에 위임 — 전건 «왜 보류 + 트리거 + 재검토일» 부착(날짜 없는 보류 금지).

**반성문 재발률**: **186건 중 🔁 재발 58건 = 31%** (08-03: 175건 중 52 = 30% — 보합).

**못 잰 것**: ①실DB 대조(이 클라우드 상자엔 Supabase 열쇠 없음 — 08-03 검진의 실DB 항목들은 재측정 못 함) ②Vercel env 실존 여부 ③애플·구글 콘솔 현재 상태(PO 로그인 필요).

## 2026-08-03 검진 (전수 — 사실상 첫 정식 검진, 기준선 이후 29일 만)

- **스캔**: 문서 10개(`CLAUDE.md`·`DESIGN.md`·`PROJECT_CONTEXT`·`KNOWN_ISSUES`·`PO_PREFERENCES`·`LAUNCH_GATES_PO`·`POSTMORTEMS`·`TEST_ACCOUNTS`·`KHIDI_중간보고_베이스`·`src/lib/manuals/index.js`) + 파일참조 458개·npm 스크립트 12개·라우트 37개 실존 대조 + **실DB 직접 조회**
- **발견**: 모순 **0** · 드리프트 **3** · 죽은참조 **1** · 기한경과 **1** · 확인필요 **3**

**교정 (이번에 고침)**

1. 🔴 **죽은 참조 — `docs/KHIDI_중간평가_제출서류_체크리스트.md` 는 «한 번도 존재한 적이 없다»**(`git log --diff-filter=A` 로 확인). `KHIDI_중간보고_베이스.md` 가 *"서류 준비는 그 문서를 보고 진행한다"* 로 **유령을 가리키고 있었다.** D-24 에 서류 준비를 시작하려다 빈손이 될 자리 → 취소선 + 경고 + 실제 출발점(§0 제출물)으로 교정.
2. **KHIDI 베이스 머리말이 44일 낡음** — 「2026-06-20 · D-약 68일」 → **2026-08-03 · D-24**. 날짜뿐 아니라 **말투가 6월 시점("지금부터 쌓는다")** 이었다.
3. **KHIDI 성과지표 — 2026-08-03 실측 칸 신설**(유치 0/12 · 상담+사후 1/120 · 만족도 응답 1). 「자동 집계한다」가 *숫자가 쌓였다*는 뜻으로 읽히던 자리를 막았다.
4. **`KNOWN_ISSUES` KHIDI 0지표 항목에 재실측 블록 추가** — 07-29 → 08-03 **닷새 동안 세 지표 모두 변화 0**. 「어시가 할 수 있는 것 3개」 중 **①(문의 미연결 경고) ②(완료 유도)는 코드에 여전히 없다**(전수 검색 0건, 07-24 기재 이후 열흘째 미착수).
5. **기한 경과 — 만족도 「2026-07-31까지 첫 케이스가 사후관리로」가 지났고 결과 기록이 없었다.** 실DB: 사후관리 세션 **0건**. 단 07-23 PO 확인대로 **옮길 실 케이스가 애초에 없다** → 「기한을 다시 잡지 마라, 조건은 날짜가 아니라 첫 실환자 치료 완료다」로 교정하고, 다음 세션이 *"코디에게 밀어라"* 로 오독하지 않게 금지선을 같이 박음.

**보류 / 확인 필요 (안 고침 — 왜 안 고쳤는지 포함)**

- 🔴 **배포 창구 항목이 현실과 어긋난다.** `KNOWN_ISSUES` 는 *"오후 3시 창구가 2~3시간 늦게 열린다"* 인데 실측은 **예약(schedule) 실행이 2026-07-30 이후 0건**이고 `production` 브랜치는 **2026-08-03 하루에 두 번 이상** 갱신됐다. → **손대지 않았다**: 같은 날 다른 세션이 이 영역을 작업 중(`work/deploy-window-selfserve` · 「2026-08-03 [배포·끝단 실측]」 인수인계). **남의 차선.** 그 세션이 끝난 뒤 이 항목을 다시 재라.
- 🟡 **에이전시 백오피스 설명서가 25일 뒤처짐**(`updated: 2026-07-09` vs 화면 최근 변경 2026-08-03). 그 사이 첨부 상한이 **4.5MB→200MB**, 코디 대리 업로드가 생겼다. **사실 오류는 없다**(화면이 이미 「최대 200MB」를 6개 언어로 표시) → 설명서에 없는 건 «누락»이지 «거짓»이 아니라서, 사용자 문구를 3개 언어로 지어내지 않고 남겨둔다. `clinic`(2026-06-24)은 그 사이 실질 변경이 없어 **문제 아님**.
- 🟡 **Gemini 유료 결제 「보채기 복귀」 문턱 판정이 애매하다.** 관문 문서 기준은 *"비테스트 실문의 주 5건↑"* 인데 최근 7일 실측 **14건 중 연락처가 있는 건 2건**(둘 다 web·러시아어)이고 나머지 12건은 AI챗이 만든 문의(연락처·국적·언어 전부 빔). **세는 기준을 문서가 안 정했다** → 기준 확정은 PO 판단(돈이 걸림). ※ AI 호출은 14일 내내 매일 있었다(하루 2~20건).

**🔴 검진 직후 «검진이 놓친 것» 1건 — PO 가 잡았다 (같은 날 추가)**

- **모순 0건이라고 보고했는데 실제로는 1건 있었다.** `LAUNCH_GATES_PO.md` 는 관문7(Gemini 유료 결제)을 *"열림 · 코드로 검증 불가"* 로 두고 있었는데, **`KNOWN_ISSUES.md:1434` 에 2026-07-20 에 이미 닫혔다는 기록이 있었다**(PO 가 AI Studio 화면으로 `Tier 1 · 후불` 확인). → 어시가 **이미 끝난 일을 PO 에게 보챘다**(PO 지적: *"근데 제미나이도 유료 플랜으로 올려뒀잖아?"*). 관문 문서 3곳을 닫고, 남은 진짜 항목을 **spend cap(금전 백스톱)** 으로 바꿔 적었다.
- **왜 놓쳤나 (검사 방법의 결함)**: 모순 검사를 **표본 3쌍**(프리뷰 차단·배포 3시·테스트 계정 역할)만 돌렸다. **「이 관문 열렸나?」 같은 상태값은 문서마다 흩어져 있어 표본으로는 안 걸린다.**
- **다음 검진 때 바꿀 것**: 관문·이슈의 **상태값(열림/닫힘)은 전수로** 대조하라. 방법 = 관문 제목 키워드를 `KNOWN_ISSUES`·`PROJECT_CONTEXT` 에 그대로 `grep` 해서 **더 최신 날짜의 반대 서술이 있는지** 본다. 이번 건은 `grep "Gemini 유료"` 한 줄이면 걸렸다.
- **곁가지 발견**: 유료가 확정됐으므로 `GEMINI_PII_BILLING_CONFIRMED=true` 만 켜면 **AI 상담 회의록 요약이 살아난다**(`app/api/khidi/consultation/[id]/summarize/route.ts:79`). 이미 켜졌는지는 **어시가 Vercel 설정값을 못 읽어 모른다**.
- ✅ **곧바로 전수로 다시 훑어 «같은 부류» 1건을 더 찾았다** — `TEST_OFFICE_IPS`. `KNOWN_ISSUES.md:1187` 에 **PO 결정(2026-07-23) 「불필요, 닫음. 다시 요청하지 말 것」**이 적혀 있는데도 관문 문서는 여전히 「미확인」으로 요청하고 있었다. **명시적 금지를 어기고 있던 셈.** 닫았다.
- **같은 방법으로 대조했으나 «진짜로 열려 있는» 것들(오탐 아님)**: DPA 서명(다른 문서에 기록 0건) · Resend 도메인 검증(`KNOWN_ISSUES:1188` 도 여전히 열림으로 일치) · 등록증 2곳(코드 실측 2곳 그대로).
- **검진 방법 자체를 고쳤다**: 관문 문서 「이미 닫힌 관문」 표에 **다음 세션용 대조 절차**를 박았다 — *"PO 에게 옮기기 전에 제목 키워드를 `KNOWN_ISSUES`·`PROJECT_CONTEXT` 에 검색해 더 최신 날짜의 반대 서술이 있는지 먼저 봐라."*

**반성문 재발률**: 전체 **175건 중 🔁 재발 52건 = 30%** (제목줄 🔁 기준). 최근 재발은 #129(2026-07-27, #61 부류).

**검사**: `check:content`·`check:rules`·`check:handoff`·`check:hook-data` 결과는 이 엔트리를 올린 신청서(PR)에 기재.

**못 잰 것**: ①Vercel 설정값(env) 실존 여부 — 읽는 도구가 없다 ②애플·구글 콘솔의 현재 상태 — PO 계정 로그인 필요 ③`PO_PREFERENCES` 「보관」 20만자 본문 — 대기실만 봤다(보관은 설계상 안 읽는 영역).

## 2026-07-05 검진 (시스템 구축 — 기준선)

- 스캔: 구축일이라 전수 검진은 생략, 다음 주부터 정식 검진.
- 교정: 없음 / 보류: 없음
- 반성문 재발률: 0/76건 (🔁 표시 제도 오늘 시작 — 과거 건 소급 표시 안 함, 앞으로 쌓이는 것만 집계)
