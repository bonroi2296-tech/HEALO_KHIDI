# -*- coding: utf-8 -*-
"""
_facts.py: 정부지원과제 산출물이 공유하는 「현행 사실」 단일 소스(SoR)
=============================================================================

⚠️ 왜 이 파일이 있는가 (2026-07-27 신설)
-----------------------------------------------------------------------------
산출물 01·02·04·06·07 이 2026-04 기준으로 작성된 뒤 3개월간 갱신되지 않아,
**지금은 존재하지 않는 화면(/intake·/partner/*·/doctor/*)을 설명하고 있었다.**
06_사용자매뉴얼은 사용자에게 `/partner` 로 접속하라고 안내하고 있었고, 그 경로는
저장소에 폴더조차 없다.

근본 원인은 두 가지였다.
  1) 같은 사실(라우트·역할·지표)이 문서마다 **각자 하드코딩**돼 있어, 한 곳을 고쳐도
     나머지가 남았다.
  2) 자동 검사(`npm run check:content`)가 **.docx 내부를 못 읽어** 아무도 못 잡았다.

→ (1) 의 해법이 이 파일이다. **사실은 여기 한 곳에만 적고, 생성기는 import 해서 쓴다.**
   (2) 의 해법은 `scripts/check-content-consistency.mjs` §32(산출물 문서 검사).

사용법:
    import _facts as F
    F.TIERS, F.ROUTES_RETIRED, F.KPI_ACTUAL ...

갱신 규칙:
  · 라우트·계층·기능이 바뀌면 **코드를 고친 그 PR에서 이 파일도 고치고 생성기를 재실행**한다.
  · 실적 수치(KPI_ACTUAL)는 **추정 금지: 운영DB 정확 COUNT 로 재조회**하고
    AS_OF 날짜를 함께 올린다.
"""

# ── 문서 공통 메타 ───────────────────────────────────────────────────────────
PROJECT = {
    "사업명": "2026년 KHIDI 「ICT 기반 외국인환자 사전상담·사후관리 지원 사업」",
    "수행기관": "본로이 (Bonroi)",
    # ⚠️ 서비스명은 healwith 하나다. 옛 이름 HEALO 는 상표권 출원 이슈로 버렸고 «병기»도 하지 않는다
    #    (2026-08-20 PO 결정). 사업계획서(2026-04 제출본)에 HEALO 가 210회 나오지만 그 뒤에 바뀐 것이므로
    #    옛 제출본을 기준 삼지 마라. 저장소 이름 HEALO_KHIDI 는 옛 흔적일 뿐이다.
    "플랫폼": "healwith",
    "플랫폼_도메인": "healwith.co.kr",
    "파트너 의료기관": "면력한방병원 (4개 지점 · 외국어 표기는 Immune Hospital 로 통일, 2026-09-06 PO 결정)",
    # 중간평가 날짜의 정본은 docs/KNOWN_ISSUES.md 「KHIDI 중간평가」. 이 파일엔 «월»만 둔다.
    "중간평가": "2026. 9.",
    "사업기간": "2026. 4. 6. ~ 2026. 11. 20.",   # 협약서 2026-186-001
}

# 문서 판번호 — 생성기 표지에 함께 찍는다. 갱신할 때 여기만 올린다.
DOC_VERSION = "v1.3"
DOC_DATE_KO = "2026년 9월 6일"
DOC_DATE_DOT = "2026.09.06"

# 사실 확인 기준일 — 문서 표지·각주에 그대로 인쇄한다(언제 기준인지 독자가 알게).
AS_OF = "2026. 9. 6."

# 확인 근거(문서 말미 「근거자료」 표에 사용). 출처 없는 서술을 만들지 않기 위함.
PROVENANCE = [
    ("코드", "src/lib/auth/accountTiers.ts", "계정 계층 7종의 단일 표준"),
    ("코드", "src/lib/khidi/targets.ts", "성과지표 공식 목표(12/120/90)"),
    ("코드", "src/lib/khidi/kpi.ts", "성과지표 집계 정의(테스트 데이터 제외 규칙 포함)"),
    ("빌드", "Next.js 라우트 목록(CI 빌드 산출)", "실제 존재하는 화면 경로"),
    ("데이터", f"운영데이터베이스 정확 집계({AS_OF})", "실적 수치의 원천"),
]

# ── 계정 계층 (src/lib/auth/accountTiers.ts 와 1:1) ──────────────────────────
# ⚠️ doctor(의사)는 계정 계층이 아니다. 화상상담 초대링크 게스트 또는 병원 계정으로 참여.
TIERS = [
    # (계층, 라벨, 권한 저장 위치, 전용 포털, 설명)
    ("guest", "비회원(게스트)", "초대링크 토큰", "—",
     "계정 없이 화상상담 초대링크로만 상담방 입장. 의료진·통역사가 이 방식으로 참여."),
    ("patient", "사용자(환자)", "로그인 계정(기본값)", "/patient",
     "문의·상담·사후관리. 별도 역할 없음이 기본."),
    ("coordinator", "코디네이터", "app_metadata.role", "/coordinator",
     "내부 스태프. 환자 여정·상담 일정·증상 알림 관리."),
    ("admin", "관리자", "app_metadata.role", "/admin",
     "운영자. 전체 권한. 통합 콘솔에서 코디 화면까지 포함."),
    ("domestic_hospital", "국내 의료기관", "hospital_users 테이블", "/hospital",
     "제휴 한국 병원 담당자(owner/manager/viewer). 진료 의뢰(리드) 확인·상태 갱신·견적 회신. 병원 정보·시술 카탈로그 편집은 공개 연동 준비 중이라 아직 비활성이다."),
    ("overseas_agency", "해외 에이전시", "agency_users + agencies.partner_type", "/agency",
     "해외 환자 유치 파트너. 환자 의뢰 · 단계별 진행 확인 · 코디네이터와 케이스 메신저."),
    ("overseas_medical", "해외 의료기관", "agency_users + agencies.partner_type", "/clinic",
     "환자를 한국으로 의뢰하는 현지 병원. 에이전시와 달리 검사결과·영상·소견을 케이스에 올릴 수 있다(사후관리 ICT)."),
]

# ── 폐지·이전된 경로 (문서에서 절대 쓰면 안 되는 것) ─────────────────────────
# ⚠️ 목록의 단일 소스 = retired-terms.json.
#    CI 검사기(scripts/check-content-consistency.mjs §32)가 **같은 파일**을 읽는다.
#    여기에 따로 적어두면 또 갈라진다 — 그게 이번 드리프트의 원인이었다.
import json as _json
import os as _os

_HERE = _os.path.dirname(_os.path.abspath(__file__))
with open(_os.path.join(_HERE, "retired-terms.json"), encoding="utf-8") as _f:
    _RETIRED = _json.load(_f)

# (옛 경로, 현행, 사유) — 검사·비교용(내부)
ROUTES_RETIRED = [(r["old"], r["now"], r["why"]) for r in _RETIRED["routes"]]

# 문서에 인쇄할 때 쓰는 표. 옛 경로를 그대로 찍지 않는다 —
# 평가위원이 살아 있는 화면으로 오해하고, 산출물 검사기(§32)도 자기 문서를 위반으로 잡는다.
# (실제로 초안 부록이 이 표 때문에 30건 위반으로 잡혔다.)
ROUTES_RETIRED_PRINTABLE = [(r["now"], r["oldLabel"], r["why"]) for r in _RETIRED["routes"]]
# (옛 낱말, 사유)
TERMS_RETIRED = [(t["old"], t["why"]) for t in _RETIRED["terms"]]

# ── 현행 주요 화면 ──────────────────────────────────────────────────────────
ROUTES_PUBLIC = [
    ("/", "홈"),
    ("/inquiry", "통합 문의 퍼널: AI 상담 / 상담원 연결 / 문의서 작성 3택"),
    ("/care-journey", "치료 여정 안내(6개 언어)"),
    ("/telemedicine", "원격협진 안내"),
    ("/hospitals", "제휴 의료기관 목록 / `/hospitals/[slug]` 상세"),
    ("/treatments", "암종별 치료정보 / `/treatments/[slug]` 상세(한방 프로그램 포함)"),
    ("/consultation/[id]", "화상 상담방(초대링크로 계정 없이 입장)"),
    ("/survey/[token]", "만족도 설문 응답"),
    ("/opinion/[token]", "전문의 소견 작성(세컨드 오피니언)"),
]

# ── 다국어 ──────────────────────────────────────────────────────────────────
LANGUAGES = [("ko", "한국어"), ("en", "영어"), ("ru", "러시아어"),
             ("kz", "카자흐어"), ("zh", "중국어"), ("ja", "일본어")]

# ── 상담 채널 (⚠️ 연동 수준을 구분해서 적는다 — 과장 금지) ───────────────────
# 위챗·라인은 '바로가기 링크'다. 봇 연동(AI 응답·상담기록 축적)은 텔레그램·왓츠앱뿐.
CHANNELS = [
    ("웹 (통합 문의 퍼널)", "연동", "AI 상담·문의서·상담원 연결. 대화 전체가 기록으로 축적."),
    ("텔레그램", "연동", "봇 웹훅. AI 자동응답 + 스태프 그룹 릴레이 + 상담기록 축적."),
    ("왓츠앱", "연동", "봇 웹훅. AI 자동응답 + 상담기록 축적."),
    ("위챗", "안내 링크", "메신저 바로가기 제공. 봇 연동은 미적용."),
    ("라인", "안내 링크", "메신저 바로가기 제공. 봇 연동은 미적용."),
]

# ── 화상 상담(원격협진) 기능 ────────────────────────────────────────────────
TELEMEDICINE = [
    ("영상·음성", "LiveKit 기반 WebRTC 다자 통화. 계정 없이 초대링크로 입장."),
    ("실시간 자막", "발화를 인식해 자막 표시. 화자를 역할이 아니라 사람 기준으로 구분(색·이름·좌우)."),
    ("실시간 통역", "한국어 ↔ 러시아어 등 상호 번역 자막. 통역 음성 봇 참여 시 음성 통역."),
    ("기록", "상담 대화·번역 기록을 암호화 저장하여 사후 확인 및 품질 점검에 활용."),
    ("안정성", "하울링 2단 감지, 무응답 자동 종료, 자막 중단 시 복구 안내."),
]

# ── 성과지표 (목표: src/lib/khidi/targets.ts / 실적: 운영DB) ─────────────────
KPI_TARGET = {
    "attraction": 12,      # K-01 외국인환자 유치(건)
    "consultAndCare": 120, # K-02+K-04 사전상담+사후관리(건)
    "satisfaction": 90,    # K-03 환자 만족도(점)
    "languages": 6,        # K-05 다국어
}

# ⚠️ 추정 금지. 아래는 AS_OF 기준 운영DB 정확 COUNT.
#    테스트 데이터는 inquiries.is_test 표식으로 제외한 실건이다.
KPI_ACTUAL = {
    "attraction": 0,          # K-01. 한국 도착·진료 개시 기준 → 아직 0
    # K-02. 2026-08-06 PO 지시로 두 매체 합산으로 정의 확대 — 상세는 KPI_측정방법_명세.md
    "preConsultation": 7,     # 영상 사전상담 1(문의 #60) + 환자에게 전달된 소견 6
    "preConsultationBreakdown": (1, 6),   # (영상, 전달된 소견)
    "followUp": 0,            # K-04. session_type='follow_up' & completed → 0
    "satisfactionSamples": 0, # K-03. 실환자에 연결된 설문 응답 0건
    "inquiriesReal": 8,       # 전체 54건 중 실건 8 (web 6 + 소급등록 2). 8/25 기계 생성 시험 152건 정리 뒤
    "sessionsReal": 1,        # 전체 159건 중 실환자 문의에 연결된 것 1
    "languages": 6,
}

# ── 품질검증 실적 (AS_OF 기준 실행 결과 — 추정 아님) ─────────────────────────
# 단위테스트: `npx vitest run` / 통합·E2E: `npx playwright test --list`
QUALITY = {
    "unit_files": 193,
    "unit_tests": 1843,
    "unit_result": "전건 통과 (2026-09-06 실행, 32.7초)",
    "e2e_files": 51,
    "e2e_tests": 222,
    "smoke_files": 25,        # @smoke 표식 — 매 변경(신청서)마다 도는 것
    "smoke_tests": 128,
    "ci_checks": 28,          # ci.yml 의 자동 검사 단계 수(check:guard-coverage 가 «진짜 잡는지» 확인한 개수)
    "npm_audit": "Critical 0 · High 0 · Moderate 3 (2026-09-06 실행)",
    "ci_gates": [
        ("타입·빌드", "TypeScript 검사(tsc --noEmit)·린트·프로덕션 빌드(next build --webpack)", "매 변경 자동 실행"),
        ("단위 테스트", "vitest 193개 파일 1,843건 전건 실행 — 로직 퇴행 차단", "매 변경 자동 실행"),
        ("스키마·마이그레이션", "없는 표·칸 조회 차단, 마이그레이션 재실행 안전(멱등) 검사, CHECK 목록에 없는 값 차단", "매 변경 자동 실행"),
        ("콘텐츠·번역", "폐지된 경로·용어, ru·kz 키 누락(차단), 번역 품질(사실 유실·언어 섞임), 암종 콘텐츠 6개 언어, 언어 접두어 링크, 법률 문서 6개 언어·조문 인용", "매 변경 자동 실행"),
        ("보안", "원시 오류 문구 노출 차단, 요청 제한 저장소 범위, 이메일 소유 판정, 시크릿 스캔, 의존성 취약점(high/critical 차단)", "매 변경 자동 실행"),
        ("성과지표 정직성", "공식 산출물이 시험 데이터를 실적으로 세는 것 차단", "매 변경 자동 실행"),
        ("검사기 자체 시험", "새 검사가 «진짜 잡는지» 결함 견본으로 확인(28종)", "매 변경 자동 실행"),
        ("스모크·변경 반경 E2E", "@smoke 25개 파일 128건 + 변경이 닿는 화면만 골라 클릭 검증", "신청서마다"),
        ("전체 E2E", "51개 파일 222건 — 6개 역할 세션(환자·코디·관리자·에이전시·해외 의료기관·병원)", "본판 합침 뒤 + 매일 밤 실서비스"),
        ("접근성·성능", "axe-core WCAG 2.1 AA(공개 + 로그인 뒤 화면) · Lighthouse", "매주 월요일"),
        ("AI 자가시험", "실서비스 채팅 경로로 50문항 — 통과율·첫 글자 시간·RAG 조각 수 저장", "주 2회(월·목)"),
        ("실서비스 감시", "/api/health — UptimeRobot 5분 + 깃허브 예약", "상시"),
    ],
}

# ── 화면·API 규모 (2026-09-06 `find app` 실측) ──────────────────────────────
ROUTE_STATS = {
    "pages": 141,        # app/**/page.* (언어 접두어 제외한 실제 화면 수)
    "api_routes": 231,   # app/api/**/route.ts|js
    "migrations": 174,   # migrations/ 파일 수(.sql 170)
    "i18n_keys": 2052,   # 화면 문구 사전 키 수(언어당). ru·kz 는 CI 가 누락 시 차단
    "languages": 6,
}

# ── 정기 자동 실행 (정본 = vercel.json crons, 시각은 UTC) ─────────────────────
CRONS = [
    ("/api/cron/dispatch-reminders", "5분마다", "상담 리마인더·교육 콘텐츠 등 예약 발송(reminders_scheduled)"),
    ("/api/cron/consultation-reminders", "10분마다", "초대 토큰 기반 상담 임박 메일"),
    ("/api/cron/detect-cold-leads", "매일 00:30", "상담 단계에서 7일 무동작 문의를 코디에게 알림"),
    ("/api/cron/daily-deploy", "매일 06:00 (KST 15:00)", "배포 창구 — 본판 최신 커밋을 실서비스로"),
    ("/api/cron/indexnow", "매일 07:00", "검색엔진 색인 자동 제출(빙·얀덱스·네이버)"),
    ("/api/cron/dispatch-surveys", "매일 09:00", "만족도 설문 발송 + 사후관리 단계별 교육 콘텐츠 발송"),
    ("/api/cron/detect-silent-patients", "매일 15:00", "사후관리 환자 3일 무입력 경보"),
    ("/api/cron/kpi-snapshot", "매일 15:05", "성과지표 일별 스냅샷·집계 오류 감시"),
    ("/api/cron/automation", "매일 16:00", "AI 응답 플레이북 자동 개선·A/B 확정"),
    ("/api/cron/purge-recordings", "매일 17:30", "상담 녹화 90일 만료분 파기(녹화는 기본 꺼짐)"),
    ("/api/cron/run-regression-tests", "월·목 18:00", "AI 자가시험 50건(실서비스 채팅 경로와 동일)"),
]

# ── 자동 검사 워크플로 (정본 = .github/workflows) ─────────────────────────────
WORKFLOWS = [
    ("CI (ci.yml)", "신청서·본판 변경마다", "타입 검사·단위 테스트·린트·빌드 + 자동 검사 28종(콘텐츠 일관성·번역 품질·마이그레이션 재실행 안전·스키마 참조·오류 노출·요청 제한·법률 문구·성과지표 정직성 등)"),
    ("E2E Tests (e2e.yml)", "신청서마다 스모크·변경 반경 / 본판 합침 뒤 전체 / 매일 19:00 UTC 실서비스 야간", "실제 클릭 검증 51개 파일 222건(6개 역할 세션). 실패 시 이슈 자동 생성·회복 시 자동 닫기"),
    ("Audit (live) (audit-live.yml)", "매주 월 16:00 UTC", "접근성 axe-core WCAG 2.1 AA(공개 화면 + 로그인 뒤 화면) · Lighthouse 성능·SEO"),
    ("훑기 대장 (sweep.yml)", "매일 23:00 UTC", "npm run sweep — 상시 상태를 같은 자리에서 훑고 볼 것이 있을 때만 메일"),
    ("Chat Smoke (chat-smoke.yml)", "매일 18:30 UTC", "AI 챗 안전 가드(완치 단정·지어낸 병원·연락처 없는 「접수됨」) 스모크"),
    ("Uptime (uptime.yml)", "10분(깃허브 예약 특성상 실측 하루 7회)", "/api/health 2회 연속 실패만 장애 판정. 실서비스 감시의 정본은 UptimeRobot(5분, 외부)"),
    ("Daily Deploy (daily-deploy.yml)", "매일(Vercel 예약이 깨움)", "배포 전후 실서비스 커밋 대조"),
    ("Mirror to GitLab (gitlab-mirror.yml)", "push 마다", "오프사이트 백업 미러"),
    ("평문 개인정보 잠그기 (encrypt-legacy-pii.yml)", "수동", "평문 칸 세기·잠그기"),
]

# ── 운영 데이터 규모 (AS_OF 운영DB COUNT — 시험 표식 제외 값은 따로 적음) ────
DATA_VOLUME = [
    ("문의(inquiries)", "54건 (실환자 8 · 나머지는 사람이 넣은 점검 자료. 기계 생성 시험 152건은 2026-08-25 정리)"),
    ("AI 상담 대화(chat_threads / chat_messages)", "396 스레드 / 1,077 메시지 (시험 문의 제외 · 채널: 웹 394 · 텔레그램 4 · 에이전시 1)"),
    ("화상 상담 세션(consultation_sessions)", "159건 (실환자 문의 연결 1 · 나머지는 에이전시 미팅·시험 방)"),
    ("통역 자막(consultation_translations)", "3,735건 (시험 방 제외 3,396)"),
    ("전문의 소견(case_opinions)", "6건 전부 환자 전달 완료 · 소견 요청 3건"),
    ("서류 번역(attachment_translations)", "23건"),
    ("근거 문서(rag_documents / rag_chunks)", "63건 / 105조각"),
    ("AI 응답 품질 판정(ai_response_evaluations)", "493건 (8/24 판사 수정 이후 61건: 평균 0.974 · 0.7 이상 98.4%)"),
    ("AI 자가시험(ai_regression_runs)", "2,512건 (50문항 × 회차. 최근 9/03 통과율 94%)"),
    ("AI 사용 기록(ai_usage_events)", "3,269건 (최근 30일 추정 비용 US$23 — 그중 자가시험이 약 1/3)"),
    ("알림(notifications)", "1,175건 (최근 30일 300)"),
    ("푸시 기기 등록(device_tokens)", "23건"),
    ("증상 보고(symptom_reports) / 경보(symptom_alerts)", "22건 / 6건 (시연·점검 포함)"),
    ("교육 콘텐츠(education_contents)", "18건 (암 5종 · 6개 언어)"),
    ("제휴 의료기관(hospitals)", "발행 8곳 (협진 대학병원 4 + 면력한방병원 4지점) · 시험 등록 1"),
    ("성과지표 스냅샷(kpi_snapshots)", "111일치"),
]

# ── AI 품질 실측 (자가시험 = 실서비스 채팅 경로) ─────────────────────────────
AI_QUALITY = {
    "regression_latest": "2026-09-03 · 50건 · 통과 94% · 평균 0.951 · 완료 P50 4.35초",
    "regression_first_token": "2026-08-21 · 첫 글자 P50 3.90초 · P95 5.33초 · RAG 50/50 가동(평균 6조각) — NFR-02(5초) 초과 5건(10%)",
    "regression_note": "8/24~9/03 실행은 옛 경로(첫 글자 미계측). 실서비스 경로 복원(#1617)은 9/05 본판 반영 → 다음 실행(9/07)부터 첫 글자가 다시 잡힌다",
    "live_first_token": "2026-08-21 실서비스 스트리밍 10건: 첫 글자 중앙값 2.30초 · 상위 5% 3.84초 (표본이 작다)",
    "judge_since_0824": "8/24~9/06 판사 61건: 평균 0.974 · 0.7 이상 98.4% (8/24 이전 값은 판사 결함으로 오염)",
    "grounding": "구글 검색 그라운딩은 한 번도 동작한 적이 없다(설치된 SDK 에 없는 옵션이라 조용히 무시됨). 실제 외부 보강은 HIRA 공공 API + 네이버 지역검색",
}

# ── 남은 일 · 기술 부채 (향후 개발자용 — «완료» 뒤에 숨은 것) ─────────────────
# (영역, 무엇이 남았나, 어디를 보면 되나)
GAPS = [
    ("AI 상담 3계층", "구글 검색 그라운딩 미작동(옵션 이름이 SDK 와 다름). 정식 googleSearch 도구로 바꾸거나 «3계층» 표현을 내릴 것", "src/lib/chat/generateReply.ts 의 useSearchGrounding 주석"),
    ("AI 응답 속도", "실서비스 경로 자가시험에서 첫 글자 P95 5.33초로 목표 5초를 넘긴 회차 있음(10%). 병목은 제미나이 «생각 수준» 호환 사다리의 첫 왕복", "src/lib/chat/regressionRunner.ts · ai_regression_runs.first_token_ms"),
    ("인앱 알림", "벨은 30초 폴링이다(Supabase Realtime 아님). 실시간이 필요하면 상담방 메시지처럼 channel 구독으로", "src/components/NotificationBell.jsx"),
    ("이메일 발송 로그", "일반 발송은 로그 표에 남지 않는다(관리자 테스트 발송만 기록). 발송 실패를 되짚으려면 Resend 콘솔", "src/lib/email/sendEmail.ts · admin_notification_logs"),
    ("병원 포털", "「병원 정보」·「시술 카탈로그」는 기능 플래그로 꺼져 있고(공개 연동 준비 중), 진료 결과 입력·가용 일정 화면은 없다", "app/hospital/_components/featureFlags.js"),
    ("예약", "병원 가용 일정을 코디가 «입력할 화면»이 없다. 구글 캘린더 등록은 서비스 계정 캘린더로 단방향", "src/lib/calendar/googleCalendar.ts"),
    ("견적", "AI 적정가 자동 산출은 없다. 코디가 견적을 쓰고 이력을 관리한다", "app/coordinator/cost-estimates"),
    ("증상 위험도", "규칙 기반(응급 키워드 + 점수식)이지 AI 판정이 아니다. 파일 머리말의 «2차: AI 분석»은 미구현", "src/lib/followup/symptomAnalyzer.ts"),
    ("개인정보 칸 정리", "값은 전부 암호문이지만 «평문 이름의 칸»에 저장 중. 평문 컬럼 삭제 마이그레이션은 실행 금지(돌리면 26~29건 소실)", "migrations/20260420_drop_*_plaintext.sql 머리말"),
    ("만족도(K-03)", "실환자 응답 0건. 발송 조건이 코디의 [상담 완료] 클릭에 걸려 있다", "app/api/cron/dispatch-surveys/route.ts"),
    ("결제", "결제 연동 없음(환자가 병원에 직접 결제). 정책 미결", "—"),
    ("접근성(로그인 뒤 화면)", "8/17~8/31 3주 연속 측정 실패(networkidle 함정) → #1648 로 고침, 9/07 실행이 첫 재측정", "scripts/audit/a11y-scan.mjs"),
    ("검색 유입", "빙 색인 0쪽·백링크 0. IndexNow 자동 제출은 붙였고(9/06 첫 제출 146건) 효과는 2주 뒤 재측정", "src/lib/seo/indexNow.ts"),
    ("앱 스토어", "애플 4번째 심사 대기(9/03 재제출) · 구글 프로덕션 검토 대기(9/02). 앱 안 애플 로그인은 아직 끝까지 못 간다", "docs/APP_STORE_LISTING.md"),
    ("사후관리 실데이터", "유치 확정 환자가 없어 사후관리 일정·경과기록은 견본으로만 동작 확인", "followup_schedules · progress_records"),
]

# 실적을 그대로 쓰되 왜 아직 낮은가를 사실로 덧붙인다.
KPI_NOTE = (
    "사후관리 계열(④⑤⑥)은 실환자가 치료 완료 단계에 도달해야 실적이 발생하는 구조이므로, "
    "기능 구현 완료와 실데이터 축적은 별개 지표로 관리함."
)
