# HEALO 정부지원과제 산출물 인덱스

**사업명:** 2026년 KHIDI 「ICT 기반 외국인환자 사전상담·사후관리 지원 사업」
**수행기관:** 본로이 (Bonroi)
**플랫폼:** HEALO
**파트너 의료기관:** 면력한방병원 (4개 지점)
**상태:** 플랫폼 구축·배포 완료(6월), 실서비스 운영 중 — 기능 구현 96%(22/23)
**최종 갱신:** 2026-08-20


> ### ⚠️ 2026-08-19 정정 — 다른 문서로 옮기기 전에 읽을 것
> **교육 콘텐츠(사후관리 ⑤)를 「미구현·미연결」로 적지 마라.** 실서비스 `/education` 에서
> **러시아어 포함 6개 언어로 암 5종(위·유방·간·폐·갑상선) 가이드가 실제 서비스 중**이다(당일 실서비스 확인).
> 화면 문구는 i18n 사전(`patientEdu.*`)에서 나온다.
> 빈 곳은 두 가지뿐이다 — ①DB `education_contents` 18건 + `/api/khidi/education` 을 화면이 안 부름(콘텐츠가 두 벌로 갈라짐)
> ②`src/lib/followup/educationEngine.ts`(귀국 후 단계별 자동 발송)가 어디서도 안 불림.
> 미진사유는 「교육 콘텐츠 미구현」이 아니라 **「단계별 자동 발송 미연결」**로 좁혀 적을 것.
>
> **병원 수는 8곳**이다(협진 대학병원 4 + 면력한방병원 4지점). DB `hospitals` 는 9행이지만
> 1건은 `TEST 병원 (국내 의료기관)` 이라는 시험 등록이며 `is_published = false` 다
> (2026-08-20 재조회 — 「이름·주소가 빈 행」이라던 이전 설명은 틀렸다).

---

## ⚠️ 산출물 갱신 규칙 (2026-07-27 신설 — 반드시 읽을 것)

**사실은 문서마다 적지 않는다. `_facts.py` 한 곳에만 적고 생성기가 읽어간다.**

| 파일 | 역할 |
|---|---|
| `_facts.py` | 계정 계층·화면 경로·채널·화상상담 기능·성과지표·품질검증 수치의 **단일 소스**. 문서 갱신은 여기서 시작한다. |
| `retired-terms.json` | 폐지·개명된 화면 경로 목록. **파이썬 생성기와 CI 검사기가 같은 파일을 읽는다.** |
| `update_reports.py` | 생성기가 없는 04·06·07·08·09 를 제자리 수정 + 부록 갱신(재실행 안전). |

**화면·계층·기능을 바꾼 PR에서 해야 할 일**
1. 경로를 폐지·개명했으면 `retired-terms.json` 에 한 줄 추가.
2. `_facts.py` 의 해당 항목 수정. **실적 수치는 추정 금지 — 운영DB 정확 COUNT 재조회 후 `AS_OF` 갱신.**
3. 생성기 재실행 — **순서가 중요하다.** `update_reports.py` 는 생성기가 뽑아낸 문서를 다시 손보므로
   반드시 마지막이다(먼저 돌리면 생성기가 덮어써 없던 일이 된다):
   ```
   python make_req.py && python make_func.py && python make_eval.py      && python make_ict_req.py && python make_backoffice_req.py      && python make_screen_design.py && python update_reports.py
   ```
4. `npm run check:content` 통과 확인 — **산출물 .docx 안에 폐지된 경로가 남아 있으면 CI 가 실패한다(§32).**

> **왜 이런 규칙이 생겼나:** 01·02·04·06·07 이 4월 기준으로 3개월간 방치돼 **지금은 없는 화면**
> (인테이크 별도 경로·파트너 포털 경로·의사 전용 화면)을 설명하고 있었다. 06_사용자매뉴얼은
> 사용자에게 없는 주소로 접속하라고 안내했다. 검사기가 .docx 를 못 읽어 아무도 못 잡았다.
> 자세한 경위는 `docs/POSTMORTEMS.md` #125.

---

## 산출물 분류

### A. 기획·요구 (Phase A)
KHIDI 제출 산출물의 기반 자료. 사업 시작 시 작성, 변경 시 버전 관리.

| 파일 | 분량 | 용도 |
|---|---|---|
| `01_요구사항정의서.docx` | 22p | 페르소나 4종, FR-28개, NFR-23개 |
| `01-1_화면설계서.docx` | 23p | ⭐ **화면 10종 lo-fi 와이어프레임**(P-01~P-10) + 화면별 목적·구성요소·데이터·다음 화면 연결. 사업계획서 1단계 산출물 「요구사항 정의서·화면 설계서」의 후자. 생성기 `make_screen_design.py` (그림은 `scripts/docs/make_wireframes.py` → `wireframes/`) |
| `02_기능명세서.docx` | 30p | 14 기능그룹, 코드 매핑 |
| `EVAL_MATRIX.docx` | 15p | 공고문 평가지표 대응표 |
| `11_과제요구사항_6대ICT_분석및구현대비표.docx` | 14p | ⭐ **공고 6대 ICT 서비스 요구사항 → 분석(도출기능 F-01~22) → 구현 계획 → 구현 결과 → 착수 후 고도화** 대비. 공문서 장·조 체계. 중간평가 ②추진실적·③향후계획 직접 증빙 |
| `11_과제요구사항_6대ICT_분석및구현대비표.html` | — | 위 산출물의 시각화판(서비스별 5단계 트랙) |
| `10_백오피스_재설계_요구사항대비표.docx` | 9p | 백오피스 5계층 재설계 요구사항 22건 분석표 + 반영 대비표 + RTM (11의 §24 「백오피스 재설계」 세부, 01의 FR/NFR 추적 하위) |
| `10_백오피스_재설계_요구사항대비표.html` | — | 위 산출물의 시각화판(요구↔반영 좌우 대비) |

### B. 매뉴얼·검증 산출물 (Phase B)
개발 결과물에 딸린 문서. **KHIDI 정기 보고서(중간·최종)는 지정 양식으로 따로 만들며 여기서 관리하지 않는다**(PO 결정 2026-08-20).

| 파일 | 분량 | 용도 | 작성 시점 |
|---|---|---|---|
| `06_사용자매뉴얼.docx` | 20p | 환자/코디네이터/의료진용 |
| `07_관리자매뉴얼.docx` | 18p | 시스템 관리자용 |
| `08_테스트결과서.docx` | 12p | 단위·통합·E2E 테스트 |
| `09_산출물목록.docx` | 8p | 전체 인벤토리 |

### C. 운영 관리 대장 (Opus 작성, 진행 중 업데이트)
사업 진행 중 매월·주간·실시간 갱신. 감리·점검 시 사실 증빙.

| 파일 | 갱신 주기 | 용도 |
|---|---|---|
| `PROJECT_STATUS.md` | 월 1회 | 진척도·이슈·30일 계획 |
| `MONTHLY_REPORT_GUIDE.md` | 1회 (변경 시) | KHIDI 월간보고 양식 셀 매핑 + SQL |
| `KPI_측정방법_명세.md` | 1회 (KPI 변경 시) | K-01~K-05 측정 방법·SQL |
| `리스크_관리_대장.md` | 월 1회 + 발생 시 | R-01~R-10 리스크 추적 |
| `변경관리_대장.md` | 변경 발생 즉시 | CR-NN 변경 이력 |
| `외부_의존성_관리대장.md` | 분기 1회 + 변경 인지 시 | 외부 서비스·키·비용 |
| `사전상담_접점_대장.md` | 접점 발생 즉시 | ⭐ 성과지표 K-02(사전상담)·K-04(사후관리) 건별 근거 |
| `개선방향_2026_05.md` | — | 착수 시점(2026-04) 개선 방향 분석 — 이후 실행 결과는 산출물 08·11 참조 |

### D. 자동화 스크립트
`make_req.py`, `make_func.py`, `make_eval.py` — Phase A .docx 생성 스크립트 (재실행 가능)
`make_backoffice_req.py` — 산출물 10(백오피스 재설계 요구사항 대비표) .docx 생성 스크립트 (재실행 가능, `python3 make_backoffice_req.py`)
`audit_docs.py` — **산출물 전수 검사기** (`npm run check:docs`). 약점 자진신고·표 계산·판정 개수 대조·문서 간 숫자 대조·미래 날짜·내부 용어·빈 칸·문구 중복·워드↔웹판 대조 10종을 기계가 본다. 「다 됐다」고 말하기 전에 돌린다.
`make_ict_req.py` — 산출물 11(과제 요구사항 6대 ICT 분석·구현 대비표) .docx 생성 스크립트 (재실행 가능, `python3 make_ict_req.py`)

> ⚠️ **산출물 11 갱신 규칙**: 6대 ICT 관련 기능을 추가·고도화하면 `make_ict_req.py`의 해당 서비스 「구현 결과」·「착수 후 고도화」와 제23조 월별 이력, 제28조 실데이터 수치를 갱신하고 재생성한다. 실데이터 수치는 **추정 금지 — 운영DB 정확 COUNT로 재조회**.

### E. 보관 (예정)
- `monthly-reports/` — 매월 KHIDI 제출본 사본
- `incident-reports/` — 사고 발생 시 보고서

---

## 갱신 일정

| 시점 | 작업 |
|---|---|
| 매월 1일 | `PROJECT_STATUS.md` 갱신, `MONTHLY_REPORT_GUIDE.md` 따라 xlsx 작성 |
| 매주 월 | 리스크 대장 점검, 외부 의존성 변경 점검 |
| 변경 발생 즉시 | `변경관리_대장.md` 신규 행 추가 |
| 사업 50% 시점 | 중간보고서 (KHIDI 지정 양식, 이 폴더 밖에서 관리) |
| 사업 종료 | 최종보고서 (KHIDI 지정 양식) + 이 폴더의 산출물 일체 제출 |

---

## 코드 구현 현황 요약

| 구분 | 비율 | 비고 |
|---|---|---|
| ✅ 완료 | **74%** (17/23 기능) | 인증·LiveKit·암호화·6개 암종 페이지·다국어 등 |
| 🟡 부분 | **26%** (6/23) | 화상 자막·예약 리마인더·사후관리 AI·러시아어 UI 전체 등 |
| ❌ 미구현 | **0%** | 전 기능 최소 베타 도달 |

상세는 `02_기능명세서.docx` 참조.

---

## 참조 문서 (코드베이스)

| 문서 | 위치 | 용도 |
|---|---|---|
| 보안 체크리스트 | `docs/SECURITY_CHECKLIST.md` | 개발 시 필수 게이트 |
| AI 아키텍처 리포트 | `docs/AI_ARCHITECTURE_REPORT_2026_04.md` | 모델 라우팅 전략 |
| SEO 감사 리포트 | `docs/SEO_AUDIT_2026_04.md` | 검색 최적화 |
| Yandex SEO 가이드 | `docs/YANDEX_SEO_SETUP.md` | Webmaster 등록 |
| 배포 체크리스트 | `docs/DEPLOY_CHECKLIST.md` | 운영 절차 |
| 크론 상태 | `docs/CRON_STATUS.md` | 자동화 일시 비활성 |

---

## 사람이 채워야 하는 빈칸 (TBD)

`Phase B` 보고서들에 [TBD] 표기 자리:

- 04 중간보고서 — 8월 시점 예산 수치, KPI 실적
- 05 최종보고서 — 사업 종료 후 실적·만족도·사례
- 06 사용자매뉴얼 — 화면 스크린샷 (`[화면: /intake]` 표기 자리)
- 07 관리자매뉴얼 — 운영 노하우 추가
- 08 테스트결과서 — 실측 Lighthouse·npm audit 결과

---

## 참조 PDF (KHIDI 발행)

| 문서 | 분량 | 위치 |
|---|---|---|
| 공고문 | 10p | `(공고문) ...참가기관 모집 공고.pdf` |
| 별지서식 | 32p | `(별지서식) ...별지서식.pdf` |
| 사업계획서 | 81p | `(서식_2) ...사업계획서_본로이.pdf` |
| 월간 업무 보고 양식 | xlsx | `(양식) 월간 업무 보고.xlsx` |
