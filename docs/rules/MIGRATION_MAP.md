# CLAUDE.md 재구성 전수 대조표 (2026-07-28)

> **약속: 규칙 삭제 0건. 위치만 옮겼다.** 이 표가 그 증거다.

**결과: 14,887자 → 8,844자 (−41%). 없어진 규칙 0건.**

**유실 검증(1회성, 이관 시점):** 옛 `CLAUDE.md`의 의미 있는 줄 153개를 새 `CLAUDE.md` +
이관 문서 전체와 공백 무시로 대조 → **어디에도 없는 줄 0개**(아래 4번의 의도적 문구 수정 5줄 제외).
※ 이 대조는 이관 시점의 1회 검증이다. 규칙은 앞으로 정상적으로 바뀌므로 옛 원문을 CI 에 박아두지 않는다
(그 자체가 문서-현실 드리프트가 된다). 앞으로 CI 가 매번 보는 것은 **구조**다 — `npm run check:rules`:
`CLAUDE.md` 상한 / 대기실 상한 / 분류 태그 필수 / 죽은 문서 참조 / 고아 문서.

---

## 1. `CLAUDE.md` 에 그대로 남은 것 (항상 적용)

| 옛 섹션 | 사유 |
|---|---|
| 새 세션 시작 시 필수 (PROJECT_CONTEXT·/handoff·중간 저장) | 모든 세션의 시작·끝 |
| 신규 UI 작업 전 `DESIGN.md` 확인 | 위반 시 머지 거부 = 품질 헌법 |
| 사용자 프로필 / 소통 스타일 / 버튼 관문 | 매 응답 적용 |
| 자율 안전선 2가지(돈·되돌리기) | 매 작업 적용 |
| 반복 확정된 작업 규칙 8개 | 매 작업 적용 (+2개 추가, 아래 3번) |
| ⭐ 실측·재검증이 기본값 | 매 작업 적용 (사례·원문은 `SELF_QA.md`) |
| KHIDI 평가 = 상시 기준 (핵심 3줄) | PO가 "모든 작업의 상시 기준"으로 정함 |
| 프로젝트 개요 — 기술 스택 | 코드 만질 때마다 필요 |
| 빌드 명령 + 「머지 자유 / 배포 하루 한 번」 | 매 작업 적용 |
| 보안 핵심 규칙 6줄 | 짧고 치명적 |
| 코드 컨벤션 3줄 | 짧고 상시 |

## 2. 옮긴 것 (트리거로 불러옴)

| 옛 `CLAUDE.md` 섹션 | 옮긴 곳 | 원문 보존 |
|---|---|---|
| ⚠️ 자동 운영 규칙 (1~4번 전부) | `docs/rules/AUTOMERGE.md` | 그대로 |
| 빌드 & 배포 상세 (창구·프리뷰·OS) | `docs/rules/DEPLOY.md` | 그대로 |
| ⚠️ 출시 전 self-QA (DB/RLS·데이터흐름·인증·소프트삭제) | `docs/rules/SELF_QA.md` | 그대로 |
| ⚠️ 상시 루틴 — 오류는 기계가 잡는다 (4단계·i18n·문서검진) | `docs/rules/BUG_ROUTINE.md` | 그대로 |
| ⚠️ AI 품질은 평균이 아니라 "분포"로 | `docs/rules/AI_QUALITY.md` | 그대로 |
| ⚠️ 계층별 백오피스 사용설명서 | `docs/rules/MANUALS.md` | 그대로 |
| 프로젝트 개요 — 주요 라우트·주요 시스템 | `docs/rules/PROJECT_MAP.md` | 그대로 |
| 프리뷰 팁 | `docs/rules/PREVIEW.md` | 그대로 + 낡은 서술 정정(아래 4번) |
| ⚠️ KHIDI 중간평가 상세 (평가항목·일정·습관) | `docs/rules/KHIDI.md` | 그대로 |
| ⚠️ 병렬 세션 규칙 4개 | `docs/PARALLEL_SESSIONS.md` 하단 | 그대로 |

## 3. 취향 원장에서 제자리로 내려보낸 11건 (2026-07-28 되찬 분량)

| 새 취향 | 판정 | 간 곳 |
|---|---|---|
| 「된겨?」엔 끝단 실측으로 답하라 | 이미 있는 규칙의 배포판 사례 | `docs/rules/DEPLOY.md` + `CLAUDE.md` 실측 2번에 흡수 |
| 제안 문장 속 원어도 풀어라 | 말투 규칙의 구체화 | `CLAUDE.md` 소통 스타일 + 말투 훅 |
| 고장 발견 시 담당 세션 먼저 확인 | 상황별(수리 착수 전) | `docs/rules/AUTOMERGE.md` |
| 사용자 제보는 증상이지 진단이 아니다 | 상황별(버그 캘 때) | `docs/rules/BUG_ROUTINE.md` |
| 문제 보고는 재현 화면 + 숫자 | 상황별 + 말투 | `docs/rules/BUG_ROUTINE.md` + 말투 훅 |
| 숫자는 「권위 있는 화면」에서 확인 | 상황별(돈·성과 숫자) | `docs/rules/SELF_QA.md` |
| 절약 장치가 PO 흐름 막지 않게 | 상황별(정책 설계) | `docs/rules/DEPLOY.md` |
| 할 일 3갈래 이상이면 세션 배분 제안 | 상황별(병렬) | `docs/PARALLEL_SESSIONS.md` |
| 선택지 내밀 땐 양쪽 숫자 먼저 | 상황별(버튼 만들 때) | `docs/rules/AUTOMERGE.md` + `CLAUDE.md` 소통 1줄 |
| 한 줄 결론 먼저, 부연은 뒤에 | 말투 | 말투 훅 |
| 두 번 설명해서 안 통하면 그림으로 | 말투 | 말투 훅 |
| **「재개하자」엔 표 먼저** | **매번 적용** | **`CLAUDE.md` 반복 확정 규칙 9번** |

원문은 `docs/PO_PREFERENCES.md` 「보관」에 그대로 남는다 (삭제 0).

## 4. 의도적으로 문구를 바꾼 5줄 (규칙 변경 아님)

| 옛 문구 | 새 문구 | 왜 |
|---|---|---|
| 훅이 「활성 취향」을 자동으로 띄움 | 훅이 「분류 대기실」을 자동으로 띄움 | 이름 변경(대기실 = 최종 목적지 아님) |
| `/handoff` … 취향을 누적 | … 취향을 **분류 태그와 함께** 누적 | 분류 강제 도입 |
| 상세는 「빌드 & 배포」 | 상세는 `docs/rules/DEPLOY.md` | 이동에 따른 경로 갱신 |
| 「반복 확정된 작업 규칙 (— 2026-07-28)」 | 날짜 접미 제거 | 계속 추가되는 목록이라 |
| 병렬 세션 「상세·PO용 워크플로: PARALLEL_SESSIONS.md」 | (그 문서 안으로 들어가 자기 참조 제거) | 이동 |

**추가 정정 1건:** 옛 프리뷰 팁의 *"로그인 뒤 화면은 로컬에서 못 연다"* 는 도구 현실보다 낡았다.
`e2e/auth.setup.ts` 가 역할별 세션(`patient`·`admin`·`coordinator`·`agency`·`clinic`)을 저장한다.
`hospital` 역할만 목록에 없어 확인 불가 — 옛 서술 원문과 함께 `docs/rules/PREVIEW.md` 에 기록.
