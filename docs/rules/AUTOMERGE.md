# 자동 운영 규칙 — PO 번거로움 최소화

> **트리거:** 합치기 신청서(PR)·머지·자동머지·리뷰 게이트·완성도 감사·보고 주기·중복 작업 확인
> **원래 위치:** `CLAUDE.md` 「⚠️ 자동 운영 규칙」 (2026-07-28 이관 — 내용 변경 없음)
> **요약본:** `CLAUDE.md` 「자율 안전선」 2줄 — 상세는 이 문서가 단일 SoR

---

## ⚠️ 자동 운영 규칙 — PO 번거로움 최소화 (2026-06-22 PO 요청·어기지 마라)

> **🔄 2026-06-22 PO가 자율 안전선을 넓힘 (이전 "UI는 무조건 물어봐"를 명시적으로 뒤집음 — 다음 세션은 옛 규칙으로 되돌리지 마라).** PO 의도: "정식 서비스 운영"을 한 방에 맡길 수 있게 자율 범위를 키우되, 진짜 위험(돈·삭제·보안)만 붙잡아라.

**PO가 직접 멈춰 세워야 할 건 2가지뿐: ①돈 나가는 것(소액=알려만, 큰돈=확인) ②되돌리기 어려운 것(데이터 삭제·파괴, 인증·암호화·PII·보안).** UI/화면 변경은 더 이상 "무조건 확인"이 아니다(아래 1번). 그 외는 어시스턴트가 알아서 + 자동검사로 닫는다.

1. **저위험 PR은 CI 통과 시 자동 머지(프로덕션까지)** — 타입·테스트·가드·문서·비시각 변경 + **저위험 UI(오타·번역·작은 문구·색 미세조정·명백한 버그 수정)**까지 CI 초록이면 PO 머지버튼 안 기다리고 어시스턴트가 머지·배포.
   **⚠️ 2026-07-28 (2차 정정) — 머지는 평소대로 즉시 하면 된다.** 한때 「`merge-queued` 라벨만
   달고 3시까지 대기」로 바꿨었는데 **되돌렸다**(PO 정정: 묶어야 할 건 머지가 아니라 배포다.
   PR 을 24시간 붙잡으면 작업이 서랍에 갇힌다). 이제 머지해도 실서비스 빌드가 안 돈다 —
   배포만 오후 3시 창구가 한 번에 접는다. 상세는 `docs/rules/DEPLOY.md`.
   **큰 UI/플로우/카피 톤 변경**은 막지 말고 만들되, 검토 자료는 ~~프리뷰 링크~~ → **로컬 화면 사진(전/후)으로 남겨라 → PO가 나중에 비동기로 검토**(머지 전 자연스러운 검토 기회). **프리뷰 링크는 폐기된 수단이다 — 2026-07-31 PO 지시로 프리뷰는 완전 차단, PO 가 요청할 때만 연다**(절차 = `docs/rules/DEPLOY.md`. [preview] 를 달아도 안 지어지므로 링크를 남기면 죽은 링크가 된다 — 실제 PO 혼란 전례 있음). 멈춰 세우는 건 ①돈 ②되돌리기 어려움뿐.
   - **DB 마이그레이션**: 가역적(컬럼·테이블·인덱스 *추가*)은 자동 적용 OK. 🛑 **삭제·컬럼 drop·데이터 파괴·되돌리기 어려운 변경은 여전히 PO 확인.**
   - ⚠️ **자율이어도 안 변하는 것**: `DESIGN.md`(Legacy 톤·"AI 만든 느낌" 금지)·보안 핵심 규칙·`check:content`. 자율 범위가 넓어진 거지 품질 헌법이 풀린 게 아니다.
   - 🔍 **독립 리뷰 게이트 (2026-07-05 PO 승인)**: 자동머지 전에 **작성 맥락을 공유하지 않는 별도 리뷰 에이전트**(`/code-review` 또는 동급의 새 subagent)로 diff를 검토시켜라. CONFIRMED급 정합성(correctness) 결함이 나오면 자동머지 중단 → 고치거나 PO에게. 근거: 작성자=판정자=머지자 동일인 구조는 "처음 보는 유형" 버그를 CI가 못 잡음(CI 가드는 전부 사후약방문). 문서·주석만 바꾼 PR은 생략 가능.
   - 🚫 **「초안(draft)으로 올려두고 PO 손을 기다린다」 금지 (2026-07-31 PO 지적)**: 초안 신청서는 **애초에 합칠 수가 없다.** 그런데 초록불을 다 확인해놓고 초안으로 둔 채 *"PO 가 초안을 풀고 합칠 때까지 대기"* 라고 스스로 적고 **10시간 동안 1시간마다 상태만 8번 확인**한 사고가 났다(실질 작업 0). PO: *"너 혼자 뭐하고 있었냐"*. **초안을 푸는 것도, 합치는 것도 어시 몫이다** — 저위험이면 초록불 확인 즉시 초안 풀고 합쳐라. 초안으로 남길 이유는 「아직 만들다 만 것」뿐이고, 그 경우엔 **왜 초안인지·무엇을 기다리는지**를 신청서 본문 맨 위에 적어라.
     - ⏰ **자동 체크인(`send_later`)은 「기다릴 것이 있을 때」만.** 내가 할 수 있는 일이 남아 있으면 체크인이 아니라 그 일을 해라. 같은 결과가 **2번 연속 무변화**면 그건 기다림이 아니라 방치다 — 왜 안 끝났는지 다시 보고, 내 손으로 끝낼 수 있으면 끝내라.
   - 🏷️ **자동머지 걸기 전 «스쿼시 뒤 제목»을 한 번 봐라 (2026-09-07 실사고 #1671 → #1672)**: 깃허브는 **커밋이 1개뿐인 신청서**를 스쿼시할 때 신청서 제목이 아니라 «그 커밋 제목»을 본판에 쓴다(저장소 기본값으로 보임 — 설정 화면은 이 상자에서 못 읽었고, 결과가 그 동작과 일치했다). 자동 저장 훅 커밋(`chore: 작업 자동 저장 (시각)`) 위에 설명 커밋 없이 신청서를 올려 자동머지를 걸었더니 본판 이력이 `2cadaf84 chore: 작업 자동 저장 (2026-09-07 19:12) (#1671)` 이 됐다. **해로운 건 이력 가독성만이 아니었다(독립 리뷰가 잡음)** — 배포 스킵 판정(`scripts/vercel-ignore-build.sh`)의 «자동저장 커밋은 안 짓는다» 규칙이 프로덕션 판정보다 앞에 있어서, 그 제목이 본판 머리인 채 창구가 열리면 **그날 실서비스 배포가 조용히 취소**될 판이었다(재현: 그 제목 + production + DEPLOY_WINDOW=1 → exit 0). #1672 가 그 규칙(옛 «규칙 0»)을 지웠다 — 프로덕션은 창구 규칙이, 프리뷰는 [preview] 규칙이 이미 판정해 «아무것도 지키지 않던 코드»였고, 창구 배포는 제목 불문 빌드(자체검사 4건이 부활까지 막는다). **규칙**: ①신청서 커밋이 자동 저장 것 «하나뿐»이면 중간 저장 한 줄이라도 담은 설명 커밋을 올린 뒤 자동머지. ②커밋 2개 이상이면 깃허브가 «신청서 제목»을 쓰니 신청서 제목이 자동 저장 문구가 아닌지 본다(`gh pr create --fill-first` 가 첫 커밋 제목을 신청서 제목으로 올린다 — `--fill` 은 2개 이상이면 가지 이름을 쓴다, gh 소스 기준). ③`gh` 가 있는 로컬 세션은 `gh pr merge <번호> --auto --squash --subject "<신청서 제목>"` 로 제목을 못 박는 게 제일 확실하다. **2026-09-08 실측으로 확정**(#1677): `--auto` 와 같이 먹는다 — 검사가 끝나고 저절로 합쳐진 본판 커밋 `deb3f36f` 의 제목이 신청서 제목이 아니라 `--subject` 문구 그대로였다. ⚠️ **다만 `--subject` 를 주면 깃허브가 제목 끝에 «(#번호)» 를 안 붙인다** — 본판 이력에서 신청서 번호를 잃기 싫으면 `--subject` 문구 끝에 «(#번호)» 를 손으로 적어라. 클라우드 세션: 자동머지 도구(`enable_pr_auto_merge`)엔 제목 칸이 없으니 ①②로 가고, «직접» 합칠 땐 `merge_pull_request` 의 `commit_title` 에 신청서 제목을 넣어라. **기계 검사**: CI `check:squash-title` 이 «스쿼시 뒤 제목»을 저장소 설정대로 계산해(기본값: 1개 → 커밋 제목, 2개 이상 → 신청서 제목 / 설정이 «신청서 제목»이면 언제나 신청서 제목; 설정은 gh api 로 읽고 못 읽으면 «두 후보를 다 본다»(막는 쪽으로 틀림)) 자동 저장 접두어면 빨간불 — 자체 시험이 판정·설정 판독·훅 문구 드리프트를 재고 통과/전체를 찍는다. 접두어 단일 출처 = `scripts/lib/autosave-title.sh`. ⚠️ 신청서 제목 때문에 걸리면 제목을 고친 «뒤 커밋을 하나 더» — 제목 편집만으론 검사가 다시 안 돈다. **PO 몫(선택, 클릭 1번)**: 저장소 Settings → General → Pull Requests → Squash merging «Default to pull request title and description» — 커밋 1개짜리 함정 자체가 없어진다. 검사는 설정을 읽어 따라가므로 손댈 것 없다(그 뒤에도 신청서 제목이 자동 저장 문구인 경우는 계속 잡는다). **2026-09-08 실측**: 로컬 세션이 `gh api -X PATCH` 로 `PR_TITLE`·`PR_BODY` 로 바꿨다(PO 클릭 불필요). 단 **CI 는 늘 두 후보를 다 본다** — #1672 검사 로그가 `저장소 스쿼시 제목 설정: UNKNOWN (출처: 못 읽어 두 후보 모두 검사)` 였다(GITHUB_TOKEN 응답엔 그 칸이 안 온다). 그러므로 설정을 바꿨어도 규칙 ①(자동 저장 커밋 하나뿐이면 설명 커밋 추가)은 CI 를 통과하려면 그대로 지켜야 한다.
   - 🧩 **완성도 감사 게이트 (2026-07-15 PO 승인)**: 자동머지 전에 **UI/플로/카피가 바뀐 PR**이면 `/completeness-audit`를 diff 범위로 1회 돌려라(반복 미완성 7유형 = 한글누출·죽은링크·반쪽배선·시각잘림·문서드리프트 등을 *의미로* 점검 — CI 정적 가드의 "그물 밖 통로"를 메움). 미완성이 나오면 고치고 다시. 판단 기준 SoR = `docs/DEFINITION_OF_DONE.md`(기계판독 `src/lib/completeness/rubric.js`). ⚠️ 감사는 어시가 도는 리뷰이지 **배포가 아님** → Vercel 배포 한도와 무관. 문서·주석만 바꾼 PR은 생략 가능.
2. **보고는 하루 1번 요약으로 묶기** — 틈틈이 알리지 말 것. 일상 빌드·CI·webhook 등 시스템 알림은 PO에게 옮기지 마라(결과 무관한 내부 동작). 그날 "된 것 + PO가 정할 것 1~2개(버튼)"만 묶어 보고. 진짜 결정이 필요할 때만 즉시 부름.
   - 📋 **자동머지 일지 (2026-07-05 PO 승인)**: 하루 요약에 **그날 자동머지한 PR 목록 + 각각 "저위험 판정 근거 한 줄"**을 반드시 포함. "저위험" 딱지도 어시스턴트가 붙이는 구조라, PO가 사후에 판정 오류를 훑어 잡을 수 있는 유일한 장치다.
3. **새 작업 시작 전 중복 확인** — 세션시작 훅의 "열린 작업 목록"을 먼저 보고, 이미 같은 영역을 하는 브랜치가 있으면 새로 만들지 말고 PO에게 알리거나 그 작업을 이어가라(2026-06-22 배포·안전가드·AI챗 3중복 재발 방지).
4. **버그는 원인+재발방지까지 한 세트** / **시키기 전에 먼저 제안** / **검증 못 한 건 솔직히(가능하면 자동 스모크로 대신)** — 누적 취향(`docs/PO_PREFERENCES.md`)과 일관.

---

## 자동 검사가 «실패한 것»과 «시작조차 못 한 것»은 다른 판정이다 (2026-08-06 추가)

> 취향 원장에서 이관. 계기: GitHub 인프라 장애가 이어져 신청서 2개의 검사가 못 돌았다.

**증상**: `Failed to resolve action download info. Error: Service Unavailable` 로 죽거나, 러너를 15분간 못 잡아 `cancelled` 된다.
이건 **코드가 틀렸다는 신호가 아니라 검사가 «안 붙은 것»**이다.

`ci.yml` 주석의 「검사가 안 붙은 것과 통과한 것은 다르다 → 합치지 마라」를 기계적으로만 적용하면
**인프라 장애가 이어지는 동안 아무것도 못 합친다.** 순서를 이렇게 잡아라:

1. **검증된 대처를 먼저** — 신청서를 **닫았다 다시 열기**(이 저장소에서 두 번 먹혔다. 어시 권한으로 된다).
2. 그래도 안 붙으면 **무엇이 안 돌았는지 + 대신 무엇으로 갈음했는지**를 **합침 기록(커밋 메시지)에 명시**하고 판단한다.
3. **문서만 바뀐 판은 여정 검사가 «판정할 대상 자체»가 없다** — 로컬 검사(`check:handoff`·`check:rules` 등)로 갈음하고 그 사실을 적는다.
   ⚠️ **코드가 바뀐 판이면 갈음하지 마라.** 그건 「안 붙었으니 합치지 마라」가 그대로 적용되는 자리다.

**왜 이 갈래가 필요한가**: 「검사가 빨간불」과 「검사가 아예 안 돌았다」를 같은 칸에 넣으면,
전자는 «내 코드를 고쳐야 할 신호»인데 후자에까지 그 반응을 하게 된다 — 고칠 게 없는데 고치려 든다.

---

## 고장을 발견했을 때 — 발견(내 몫)과 수리(담당 세션 몫)를 분리

> 2026-07-28 취향 원장에서 이관. 원문 경위는 `docs/PO_PREFERENCES.md` 「보관」.

**고장을 발견했으면 「내가 고칠까?」 버튼을 내기 전에 그 영역을 하는 세션이 있는지부터 확인하라.** 3시 배포 창구가 죽어 있는 걸 실측으로 잡아내고 수리 선택지 3개를 버튼으로 내밀자 PO 답이 **"그거 다른 세션이 지금 작업중이니깐 넌 다른거 해"** 였다 — 즉 **버튼 자체가 낭비**였고, PO가 세션 간 영역을 실시간으로 배분하고 있다. **발견(내 몫)과 수리(담당 세션 몫)를 분리**해라: 진단 결과·원인·선택지는 문서(핸드오프)에 남겨 담당 세션이 집어가게 하고, 착수 전에 열린 PR 목록 + 최근 원격 브랜치로 같은 영역을 먼저 대조하라. 열린-PR-대조 규칙이 *보고할 때*였다면 이건 ***수리에 손대기 전*** 판이다.

### ↔ 다만 「내가 만든 것」은 내가 닫는다 — 위 규칙과 헷갈리지 마라

> 2026-08-03 경계 정리. 취향 원장의 「발견자가 처리자다」(2026-07-28)와 위 「발견과 수리 분리」가 **제목만 보면 반대로 읽혀서** 한 줄 긋는다.

| 상황 | 어떻게 |
|---|---|
| **내가 새로 만든** 가드·검사·경보가 처음 잡아낸 것 | **그 세션에서 닫아라.** 도구만 만들고 결과를 미루면 도구를 안 만든 것과 같다 |
| 남이 쓰는 차선(다른 세션의 작업본·작업폴더)에서 발견한 고장 | **손대지 말고** 진단·원인·선택지를 문서에 남겨라 — 위 절 그대로 |

**정리·감사류 지시는 부분이 아니라 「전수」를 기대한다** (2026-07-28 PO: *"다른 열린 브랜치들도 다 대조해서 정리해줘"* → *"그럼 더 정리할거 없어?"*).
원격만 훑지 말고 로컬·태그·임시보관까지 훑고 **「남은 건 이것뿐, 이유는 이것」** 으로 닫아라.
**「안 했다」가 아니라 「왜 안 했는지」까지가 완료 보고.**

### ↔ 그리고 「방치됐으니 내가 합친다」 전에 «주인에게 물어라» (2026-09-10 추가)

> 위 두 절이 「손대라 / 손대지 마라」를 갈랐다면, 이 절은 **손대기로 정한 뒤의 순서**다.

PO 지시로 초안 신청서 #1741 을 「방치됐나」 판정했다. 근거는 ①마지막 커밋 뒤 **1시간 32분 조용** ②로컬 세션 목록에 그 신청서를 물고 있는 세션이 없음 ③본문 「확인 못 한 것」이 브라우저 화면 검증뿐 ④GitHub 리뷰 0건. **네 가지가 다 맞았는데 판정은 틀렸다.**

07:42 에 초안을 풀고 합쳤더니 **07:57 에 그 세션이 후속 신청서(#1744)를 냈다.** 리뷰 지적 5건이었고 그중 둘은 완성도 감사가 「머지 전 반드시 닫을 것」으로 지목한 것이었다. 한 신청서로 끝날 일이 둘로 갈렸다(배포 창구 전이라 실서비스 영향은 0).

| 내가 본 신호 | 왜 판정 근거가 못 되나 |
|---|---|
| 커밋이 조용하다 | **리뷰·완성도 감사·독립 검증은 커밋 없이 수십 분 간다** |
| 로컬 세션 목록에 없다 | `list_sessions` 는 **로컬만** 보여준다. `ListAgents` 의 `cloud`·`Remote Control` 줄을 같이 봐야 한다 |
| 본문 「확인 못 한 것」이 하나뿐이다 | 그것은 **그 시점에 그 세션이 아는 것**이다. 그 뒤 검사가 더 찾아냈을 수 있다 |
| GitHub 리뷰 0건 | 리뷰 결과가 아직 안 올라온 상태일 수 있다 |

**→ 순서:** ①**`SendMessage` 로 주인에게 먼저 물어라**(30초) ②답이 없으면 `ListAgents` 로 클라우드·원격까지 훑어라 ③그래도 담당이 없으면 이어받되, **검증 결과는 코멘트로 남기고 「초안 해제」와 「머지」는 주인 몫으로 두어라.** 그 상자로만 할 수 있는 일(브라우저·실DB 열쇠)을 채워 주는 것과 합쳐 버리는 것은 다르다.

## 자율 위임을 받았을 때 — 「끝났다」를 섣불리 선언하지 마라

> 2026-06-22 취향 원장에서 이관 (밤새 3~4회 반복 지적). 원문 경위는 `docs/PO_PREFERENCES.md` 「보관」.

PO 가 *"밤새 토큰 활용해 뭐든 해라"* 한 뒤 어시가 합치기 신청서 2개 만들고 「지켜보기만 하는 상태」로 멈추자
**"벌써 끝이야? 퍼펙트해? 출시해? 나 감독해야해?"** 가 연달아 왔다. 멈출 때마다 **실제로 더 할 안전·고가치 일이 남아 있었다.**

1. **「끝/완벽/이제 지켜보기만」을 섣불리 선언하지 마라** — 「안전·고가치 일이 바닥났다」의 바를 너무 낮게 잡고 있다. 한 번 더 깊이 파면 대개 더 나온다.
2. **코드 개선이 아니어도 일이다** — 평가 준비 문서·실제 DB 검증·점검·전략 분석 전부 가치 있다 (*"코드 개선이 꼭 아니라도 뭐든"*).
3. **PO 가 「더 해」라고 재촉하게 만들지 마라**(= 감독 강요). 자율을 위임받았으면 PO 입력 없이 다음 안전 작업으로 스스로 이어가라.
   단 위험·실서비스·PO 검증이 필요한 건은 **멈추는 게 아니라 «피해서 안전한 걸로 계속»** 이다.

## 울트라코드(전수 워크플로)는 「매번」이 아니라 가치 높은 데만

> 2026-06-30 취향 원장에서 이관. 원문 경위는 `docs/PO_PREFERENCES.md` 「보관」.

PO: *"모든 세션 울트라코드는 과부하 같다, 효율적으로 쓰자. 근데 일반모드는 신뢰가 안 간다."*

**핵심 진단**: 일반 모드가 못해서가 아니라 어시가 **「검증 완료」라고 뻥쳤기 때문**이다.
어드민 메뉴를 바꾸고 「임시주소 HTTP 200 = 검증」이라 보고했는데, 그 200 은 로그인 문지기 화면이라 **바뀐 메뉴를 단 한 번도 안 거쳤다.**

| 쓸 때 | 안 쓸 때 |
|---|---|
| 감사·전수조사·출시 직전 최종점검·*"이거 진짜 맞아 다 훑어"* | 만들기·고치기·화면·문구·문서 (자동검사로 닫히는 것)·잡담 |
| 돈·보안·개인정보·DB 삭제·KHIDI 평가 직결 → 「검토 붙이기」 | — |

**파리 잡는 데 망치 금지**(비용 의식). 그리고 신뢰 회복은 모드가 아니라 **검증 정직**으로 한다 —
자동 검증이 안 되는 영역(로그인 뒤 화면)은 모드로 때우지 말고 **자동 테스트로 닫아라**(모드와 무관하게 매번 도는 신뢰).

## 선택지를 내밀 때 — 버튼 전에 양쪽 숫자를 붙여라

> 2026-07-28 취향 원장에서 이관.

**선택지를 내밀면 PO는 「그 선택의 이득」부터 묻는다.** 저장소 비공개 전환을 버튼으로 물었더니 **"공개 저장소 혜택이 뭐 있는건데? 그거부터 설명해줘"** 가 돌아왔다. 손실(위험)만 설명하고 이득을 안 적었던 게 원인. → 트레이드오프 결정을 물을 땐 **양쪽의 이득·비용을 실측 숫자로 먼저**(그때는 CI 사용량 월 5,000분 → 비공개 시 월 $25) 제시하고 그다음 버튼.

## 「PO 가 할 일」 목록·결정 버튼을 내기 전에 열린 합치기 신청서(PR)부터 대조하라

문서(핸드오프·KNOWN_ISSUES)만 훑고 미결항목을 버튼으로 내밀면 **이미 다른 세션이 하는 일을 또 시킨다.**
2026-07-28 실제 사례: 「E2E 가 실서비스 DB 를 문다 — 비용 결정 필요」를 버튼으로 물었더니 PO 답이
*"e2e는 뭐 다른 세션이 작업하고 있지 않냐?"* — [#1081](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1081) 이
이미 월 $0 방식으로 진행 중이었다(통역 버튼·쿠키 배너도 마찬가지).

- 미결항목을 보고할 때는 **`gh pr list` + 최근 원격 작업본(브랜치)을 먼저 대조**한다.
- 진행 중인 것은 「PO 할 일」이 아니라 **「돌아가는 중」** 으로 분류한다.
- **문서는 특정 시점 스냅샷이라 항상 낡아 있다** — 문서를 근거로 PO 를 움직이지 마라.
