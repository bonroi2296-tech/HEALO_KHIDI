# HEALO KHIDI — 프로젝트 맥락 / 세션 인수인계

> **새 세션은 이 문서를 먼저 읽어라.** 코드만 봐선 안 보이는 "왜 이렇게 결정했는지"와 현재 상태·다음 할 일을 담음. PO(강주영)는 비개발자 → 짧고 직설적 한국어, 용어는 쉽게 풀되 원어 병기(CLAUDE.md), 결과물 우선.
>
> 📌 **핸드오프 단일 SoR(single source of truth) = 이 파일.** 인수인계는 여기 최상단에만 쌓는다(`/handoff` 스킬). 과거 독립 핸드오프 문서들은 `docs/archive/`로 이동(흩어지면 다음 세션 혼란 — 실제 사고 있었음).
> 🗂️ **군살 방지: 최상단엔 최신 4개 세션만 유지.** 그 이전 세션 핸드오프는 [`docs/archive/PROJECT_CONTEXT_handoffs.md`](archive/PROJECT_CONTEXT_handoffs.md)로 옮긴다(기록 보존). 새 핸드오프가 5개째 쌓이면 가장 오래된 걸 거기로.
> ↳ **왜 2가 아니라 4인가(2026-07-21 상향)**: 병렬 세션이 하루 3개씩 핸드오프를 쓰면서 **당일 작업이 반나절 만에 창고로 밀려나** 다음 세션 시작 훅에 안 뜨는 일이 실제로 났다(머지 6건짜리 세션이 통째로 증발할 뻔함). 병렬 세션 수보다 넉넉해야 "오늘 것"이 남는다.

---

## 🔖 세션 핸드오프 (2026-07-21 심야 — 보관 3건 전부 적용 + 병렬 PR 5개 순서대로 정리 → **브랜치 25→5·PR 0개** / ⚠️ **어시 오판 3건이 같은 세션에서 나옴**)

> 앞 블록(「보관 항목 전수 색출」)의 **연속 세션**이다. PO가 *"이제 또 새 작업 우다다다 시켜도 되게 정리 싹 했어?"* 라고 물어 실측했더니 **아니었다** — 병렬 세션 3개가 각자 브랜치·워크트리·PR을 만들어 아침에 정리한 게 하루 만에 되돌아와 있었다. 그걸 순서대로 풀었다.

**1. 이번 세션 한 일**

*보관 3건 전부 적용 (PO 지시 "일단 적용, 나중에 다듬을게")*
- **[#849 머지]** `/partners` 공개 제휴 랜딩 — 최신 main 위 재기준 후 머지.
- **[#856 머지]** 만족도 설문 **케이스 기반 발송** + **소견 AI 자동번역**. rescue 브랜치를 통째로 얹지 않고 **덩어리만 떼서 최신 본판 위에 재작성**.
- 독립 리뷰 **2라운드**에서 CONFIRMED 결함 **11건** 적발 → 전부 수정. CI(tsc·빌드·테스트·스키마가드)는 **전 과정 초록이었다** — 이 부류는 정적 검사로 안 잡힌다.

*병렬 PR 5개를 순서대로 (지뢰 제거)*
- 🚨 **#860 이 내 #856 코드를 품고 있었다 — 그것도 2라운드 수정 *이전* 버전.** 자동저장 훅이 내 작업본을 복사한 브랜치에서 갈라져 나온 탓. 그대로 머지했으면 livekit 무동작 수정·코디 편집 날리는 버그·§21 구멍이 **전부 되살아났다**(#839 부류 재발).
  → `git rebase --onto main` 으로 **멱등가드 커밋 1개만** 재적용. #860 은 base 가 내 브랜치라 자동 닫힘·재오픈 불가 → **[#863]** 으로 새로 올려 머지.
- **[#861 머지]** 환자 응급 전화 동선 복구(다른 세션) — 반성문 번호 충돌(#105 중복)을 **#106 으로 재번호**, 검사기 §21/§24 충돌을 **둘 다 살리는 방향**으로 해소.
- **[#862 머지]** 지킬 수 없는 24/7 약속 3곳 제거(PO 확인: *"못 받을 것 같아, 좀 오버"*).
- **[#858 머지]** flaky 3번째 기록. **[#864]** 보관 해제·설문 항목 정정.
- **브랜치 25→5 · 워크트리 5→3 · 열린 PR 0개.** 복원 SHA 는 계속 축적.

**2. 왜 그렇게 했는지**

- **머지 순서가 곧 안전장치였다.** 겹치는 파일(`check-content-consistency.mjs`·`POSTMORTEMS.md`)을 PR 3개가 동시에 건드리고 있어, 순서를 안 정하면 **나중 것이 앞의 수정을 덮는다.** 겹침 없는 것(#862·#858) → 지뢰 품은 것(#856) → 그 위 재기준(#863·#861) 순으로 풀었다.
- **PR 이 충돌하는지는 제목이 아니라 파일로 판단해야 한다** — 아래 4번 오판 참고.
- **자기시험이 제값을 했다.** #856 에 넣은 §21 fixture 가, 재기준 중 **#860 쪽 구현의 같은 부류 구멍**(정규식이 세미콜론을 넘어 다음 문장까지 뻗어 앞 문장의 error 로 뒤 위반이 묻힘)을 **즉시 잡았다.** 가드는 만든 순간이 아니라 *깨지지 않음을 매번 확인할 때* 가드다.

**3. 안 끝났거나 보류**

- **🔴 만족도 설문은 코드를 고쳐도 여전히 0건.** 실환자 11건 중 `follow_up` **0건**, 상담 35건 중 `completed` **0건**. **진짜 병목은 코드가 아니라 운영** — 아무도 단계를 안 옮긴다. K-02·K-03 이 같은 원인으로 0. ⏰ **7/31 까지 첫 케이스가 사후관리로 넘어가야** 8/27 평가 표본이 쌓인다.
- **`/api/portal/emergency` 삭제 미완료** — PO 결정은 났으나 담당 세션이 아직 안 끝냈다. §21 유예 목록에 한시 등재 중(파일 지워지면 그 줄도 삭제할 것).
- **`/partners` PO 숙제 2건** — 유입 링크 위치 없음(주소 직접 입력해야 보임) · 대학병원 표기(사이트맵 포함 → 구글 색인됨).
- **`#861` 이 남긴 공백** — 교육 페이지 "즉시 연락하세요"의 목적지. 대표번호를 24/7 로 쓰지 않기로 했으므로 다른 안내가 필요하다.
- 남은 브랜치 5개 중 `work/agency-claim-inbox` 는 **고유 파일 0건** — 지워도 된다.

**4. 주의·함정** ⚠️

- 🚫 **PR·브랜치가 "충돌한다"고 말하기 전에 `gh pr view <n> --json files` 로 실제 파일을 대조하라.** 제목의 반의어는 근거가 아니다(아래 오판 ③).
- ⚠️ **자동저장 훅이 다른 세션 브랜치에 내 작업본을 복사한다.** 이번에 그 복사본에서 갈라진 PR 이 낡은 코드를 되돌릴 뻔했다. **머지 전 `git diff --stat origin/main <브랜치>` 로 삭제(-) 줄 수를 반드시 볼 것.**
- ⚠️ **PR 의 base 를 `main` 이 아닌 다른 작업 브랜치로 잡지 마라.** 그 브랜치가 머지·삭제되면 PR 이 **자동으로 닫히고 재오픈이 안 된다**(#860 이 그렇게 죽었다).
- ⚠️ **리뷰 서브에이전트에 도구를 전부 열어주지 마라.** 리뷰만 시켰는데 PO 화면에 작업 칩을 띄웠고, PO 가 어시 제안으로 오해해 실행했다. **읽기 전용으로 띄우고, 후속 작업 칩은 어시가 직접 판단해 하나씩.**

**5. 다음 세션이 먼저 할 일**

1. ⚠️ **직전 미검증분** — #856·#861 의 실동작을 아무도 화면에서 확인하지 않았다. 소견 자동번역은 실케이스가 2건뿐이고 둘 다 번역 전이며, 설문은 대상 케이스가 0건이라 **로컬에서 도달 자체가 불가**했다.
2. **PO에게 사후관리 단계 이동을 요청** — 이게 8/27 지표의 유일한 병목이다(위 3번).
3. `#864` 머지 후 `work/agency-claim-inbox` 정리.
4. `/api/portal/emergency` 삭제 완료 확인 → §21 유예 목록에서 그 줄 제거.

**6. 검증 상태**

- ✅ **머지 6건 실측** — #849·#856·#858·#861·#862·#863 전부 CI 초록 확인 후 머지, 매번 `check:live-meetings` 로 상담 없음 확인. 머지 후 `git cat-file` 로 본판 실재 확인(`translateOpinion.ts`·`EmergencyNumbers.jsx`·유일인덱스 마이그레이션).
- ✅ **가드 2개를 돌연변이 시험으로 실증** — §21 은 fixture 3종(잡아야 할 2·잡으면 안 될 1) 자기시험 내장. §24 는 한국 119→118 주입 시 **6개 언어 전부에서 검출** 확인 후 복원.
- ✅ **DB 유일 인덱스** `uniq_surveys_inquiry` 프로덕션 적용(적용 전 중복 0건 실측, 가역적).
- ✅ 전 과정 `tsc` 0 · `next build --webpack` 0 · `vitest` 574건 · `check:content`·`check:schema-refs`.
- ❌ **화면 실동작 미검증** — 위 5-1.
- ❌ **`/partners` 폼 실제 제출 안 해봄**(DB 층만 확인).

**7. 다음 세션 첫 프롬프트**

> `docs/PROJECT_CONTEXT.md` 최상단 읽어. 2026-07-21 에 보관 3건을 전부 적용하고 병렬 PR 5개를 순서대로 정리해 **열린 PR 0개·브랜치 5개**로 끝냈다. 먼저 할 일 = ①**만족도 설문이 코드는 고쳐졌는데 여전히 0건** — 실환자 케이스가 `follow_up` 으로 하나도 안 넘어와서다. **7/31 까지** PO 가 케이스를 사후관리로 옮겨야 8/27 평가 표본이 쌓인다고 알릴 것 ②#864 머지 ③`/api/portal/emergency` 삭제 완료 확인. 🚫 **함정: PR 이 충돌하는지 제목으로 판단하지 마라 — `gh pr view <n> --json files` 로 파일을 대조하라**(어시가 이걸로 오판해 PO 에게 멀쩡한 작업을 멈추라고 했다). PR base 는 반드시 `main`(다른 작업 브랜치로 잡으면 그게 머지될 때 PR 이 죽는다). ⚠️ 어시의 2026-07-21 판정 정확도가 낮았다(오판 3건) — **"고쳤다/괜찮다"는 말을 그대로 믿지 말고 실측으로 재확인할 것.**

---

## 🔖 세션 핸드오프 (2026-07-21 밤 — "겹친 세션들 정리가 안 됐다, 보관해둔 걸 아무도 못 찾더라" → **보관 항목 전수 색출 + 브랜치 190→52·워크트리 23→2 + 위험 PR 1건 차단**)

> PO 원문: *"여러 세션으로 겹쳐서 개발을 진행하다보니 정리가 안된거 같아. 정리 세션도 여러번 갈아탔거든? 근데 공통적으로 **만들다가 적용하지 않고 일단 보관만 해두자고 남겨둔 걸 제대로 파악하지 못하더라고.**"*
> **이 세션의 한 줄**: 보관 항목이 안 잡혔던 건 게을러서가 아니라 **판정 기준이 틀려서**였다 — 아래 2번 참고. 기준을 바꾸니 190개 중 13개로 좁혀졌다.

**1. 이번 세션 한 일** (코드 변경 0 — 문서·브랜치 정리만)

*전수 조사*
- 로컬 브랜치 **190개** · 원격 8개 · 워크트리 **23곳** · PR **637건**(머지 597·닫힘 37·열림 3) 전수 대조.
- 판정을 **3단계**로: ①`origin/main..브랜치` 커밋 유무 → ②브랜치가 건드린 파일이 지금 main 과 다른가 → ③**그 브랜치로 PR 을 올린 적이 있는가**. ③이 결정타였다.

*정리 실행*
- **로컬 브랜치 190 → 52** (126개 삭제). 전부 **PR 머지 완료 + 워크트리 미점유**만 골랐고, **복원용 SHA 목록을 [`docs/audit/BRANCH_CLEANUP_20260721.txt`](audit/BRANCH_CLEANUP_20260721.txt) 에 커밋**해뒀다(되살릴 땐 `git branch <이름> <SHA>`).
- **워크트리 23 → 2** (`HEALO_KHIDI` 본체 + `HEALO_worktrees/hospital-info`=main). 제거 전 **23곳 전부 `git status` 로 미커밋 0 확인**(작업 유실 없음). 워크트리를 지워도 브랜치·커밋은 남는다.
- 남은 52개 = 보관 4 + 닫힌 PR 6 + PR 없는 나머지 + 최근 것들.

*위험 차단*
- 🔴 **PR #839 는 머지하면 안 된다 — 닫았다.** 낡은 `main` 기준이라 머지하면 **삭제된 것들이 부활**한다: `/search`·`/stories`(PO *"둘다 날려버려"*) · `self-check.sh` 훅 · 마이그레이션 4개 · `pickAllowed.ts`. diff 기준 **삭제 1,303줄**. 반성문 **#96「낡은 파일 통째로 덮어 PO 지시 삭제코드 부활」과 같은 부류의 재발**이다. 핸드오프 기록만 이 문서로 흡수.
- **PR #852**(충돌 상태로 방치)의 세션 기록을 이 문서로 흡수 후 닫음 — 아래 「2026-07-21 저녁」 블록이 그것.

*장부 교정*
- `KNOWN_ISSUES.md` 「📦 보관」 표의 **`kpiHealthcheck.ts` = 신규**가 틀렸다 → main 에 **이미 50줄짜리가 있고** rescue 브랜치 것은 104줄 **확장판**이다(#63 문서-현실 드리프트 부류). 표 수정.
- **보관 항목 3번(소견 AI 자동번역)을 독립 항목으로 승격.** 지금까지 rescue 브랜치 표의 한 칸에만 적혀 있어 **"rescue 를 안 살리면 이것도 없다"는 사실이 안 보였다.**
- `partners-preview` 브랜치가 `work/partners-landing` 과 **`/partners` 내용이 동일한 중복**임을 명시(보관본은 `work/partners-landing` 하나).

**2. 왜 그렇게 했는지 — 「보관한 걸 못 찾는」 진짜 원인**

- **원인은 판정 기준이다.** 흔히 쓰는 `git rev-list origin/main..브랜치`(= 커밋이 본판에 있나)로 재면 **190개 중 141개가 "미머지"로 뜬다.** 우리는 GitHub **스쿼시 머지**를 쓰기 때문에, 머지가 끝난 브랜치도 커밋 해시가 달라 영원히 "안 합쳐진 것"으로 보인다. **진짜 보관 항목 3건이 가짜 137건 속에 묻히는 구조**였다 — 정리 세션이 몇 번을 갈아타도 같은 목록을 보고 같은 곳에서 포기했다.
- **바꾼 기준: "이 브랜치로 PR 을 올린 적이 있는가?"** 만들고 올리지도 않은 것 = 적용할 생각이 없었거나 보류된 것이다. 이걸로 재니 **190 → 13개**로 줄었고, 그중 실제 보관은 **3건**이었다(나머지 10은 자동저장 노이즈·백업본·이미 흡수됨).
- **워크트리를 지워도 되는 이유**: 워크트리는 "브랜치를 펼쳐놓은 작업 폴더"일 뿐이라 지워도 브랜치와 커밋은 저장소에 남는다. 위험은 **미커밋 파일**뿐이라 23곳 전부 먼저 확인했다.
- **브랜치를 물어보지 않고 지운 이유**: 되돌리기 어려운 작업이 아니다(복원 SHA 목록을 저장소에 커밋). CLAUDE.md 🚦묻기 전 관문 3조건 어디에도 안 걸린다.

**3. 안 끝났거나 보류 — 📦 보관 목록 (이 세션의 핵심 산출물)**

> **전부 "만들었는데 적용 안 한 것"이다. 정리 대상으로 오해해 지우지 마라.** 상세는 [`docs/KNOWN_ISSUES.md`](KNOWN_ISSUES.md) 최상단.

| # | 무엇 | 어디에 | 왜 보관 | 되살리는 비용 |
|---|---|---|---|---|
| 1 | **`/partners` 공개 제휴 랜딩** (B2B 유입구, 6개 언어 1,015줄 + 접수 API) | 브랜치 `work/partners-landing` · PR **#849 초안**<br>(`partners-preview` 는 같은 내용 중복본) | PO *"이건 일단 보관만 해둬"* (2026-07-21) | **0 — 검증 끝남.** 머지만 하면 됨. 단 PO 결정 2개 선행(유입 링크 위치 · 대학병원 표기) |
| 2 | **만족도 설문 발송 기준 변경** (`상담 completed` → `케이스 follow_up 진입`) | `rescue/local-uncommitted-20260716` 의 `dispatch-surveys/route.ts` · `generateSurveyToken.ts` | PO *"나중에 쓸 일 있음 쓰게 보관만"* (2026-07-21) | **작음(파일 2개).** DB 는 준비 끝(`surveys.inquiry_id`·`inquiries.followup_started_at` 실재) |
| 3 | **소견 AI 자동번역** (`translateOpinion.ts` + `opinions/translate` API) | 같은 rescue 브랜치 · **`work/agency-claim-inbox` 에도 동일본 있음** | 2의 덤으로 같이 묶여 보류됨 | **작음(신규 파일 2개, 172줄).** 마이그레이션은 이미 본판에 있음(#844) |

- ⚠️ **3번은 지금 「반쪽 배선」 상태다** — DB 칸(`case_opinions.auto_translated_text`)과 마이그레이션은 **본판에 이미 들어가 있는데 채우는 코드만 없다.** 그래서 소견 2건 중 번역 0건. 1·2번과 달리 **PO 가 명시적으로 보류한 게 아니라 rescue 브랜치에 딸려 들어가 같이 묻힌 것**이다 — 되살릴지 별도 판단 필요.
- 🔴 **살아있는 미해결 2건**(보관 아님, `KNOWN_ISSUES.md` 최상단): 만족도 설문 구조적 0건(**8/27 중간평가 공식 지표**) · 환자포털↔운영데이터 단절(4주째 그대로).
- **남은 로컬 브랜치 52개 중 「PR 없이 남은 것」 6개**는 자동저장 노이즈·백업본으로 판정했다(`po-wip-backup-20260622`·`backup/consult-work-20260720`·`pr724check`·`fix/776-conflict-resolve`·`typing-fix`·`save/self-check-hook`). **전부 내용이 본판에 있거나 대체됨을 파일 단위로 확인**했지만, 지우진 않았다(급하지 않음).

**4. 주의·함정**

- ⚠️ **브랜치 정리할 때 `git rev-list origin/main..브랜치` 만 보고 판단하지 마라.** 스쿼시 머지 때문에 머지된 것도 "미머지"로 뜬다. **PR 유무(`gh pr list --state all`)로 갈라라** — 이게 이 세션이 찾은 방법이고, 이전 정리 세션 3번이 여기서 막혔다.
- ⚠️ **`work/partners-landing` · `rescue/local-uncommitted-20260716` · `work/agency-claim-inbox` 를 지우지 마라.** PR 이 없거나 초안이라 정리 대상처럼 보이지만 위 표의 보관 항목이다.
- ⚠️ **rescue 브랜치를 통째로 얹지 마라** — 2026-07-16 이후 본판이 크게 움직였고 `caseStatus.ts` 는 KHIDI 지표 계산 직결이라 잘못 얹으면 숫자가 틀어진다. **덩어리 단위로 떼서 최신 본판 위에 재작성**이 안전.
- ⚠️ **낡은 브랜치의 핸드오프 PR 을 그냥 머지하지 마라(#839 실사고).** 문서만 바뀐 것처럼 보여도 낡은 기준점이면 **그 사이 삭제된 코드를 되살린다.** 머지 전 `git diff --stat origin/main <브랜치>` 로 **삭제(-) 줄 수**를 반드시 볼 것.
- ⚠️ **이 세션도 공용 폴더(`C:\Users\user\Desktop\HEALO_KHIDI`)에서 돌았다.** 다만 이번엔 워크트리를 전부 정리하는 게 목적이라 불가피했고, 다른 세션이 안 도는 걸 확인했다(미커밋 0). **다음 세션은 `bash scripts/new-session.sh <영역>` 으로 분리할 것.**

**5. 다음 세션이 먼저 할 일**

1. ⚠️ **직전 미검증분 먼저 확인**: 이 세션은 **빌드·CI 를 안 돌렸다**(문서·브랜치만 건드림). 이 핸드오프 PR 의 CI 가 초록인지 보고 머지.
2. **보관 3건을 PO 와 한 번에 결정.** 특히 **2번(만족도 설문)은 2026-07-31 이 사실상 데드라인** — 설문은 보낸 뒤 응답까지 와야 8/27 평가 점수가 된다.
3. **소견 AI 자동번역(보관 3번)은 별도 판단** — PO 가 보류한 게 아니라 딸려 묻힌 것이고, 지금 DB 칸만 있고 코드가 없는 반쪽 상태다.
4. 직전 세션 숙제 그대로: `/admin/treatments` 에서 **사람이 클릭해서** 치료 1건 등록 왕복 확인(API·DB 만 검증됨).

**6. 검증 상태**

- ✅ **브랜치·워크트리 실측** — 190→52, 23→2. 삭제 126건 전부 성공(실패 0), 복원 SHA 목록 저장소에 커밋.
- ✅ **워크트리 미커밋 0 확인** — 제거 전 23곳 `git status --porcelain` 전수. **작업 유실 없음.**
- ✅ **보관 항목 파일 단위 실측** — `git cat-file -e origin/main:<경로>` 로 `translateOpinion.ts` **본판에 없음** 확인, `kpiHealthcheck.ts` **본판에 있음**(50줄 vs rescue 104줄) 확인, `/partners` 4개 파일 본판에 없음 확인.
- ✅ **#839 위험 판정 근거** — `git diff --stat origin/main docs/handoff-20260721-cleanup` 실행 결과 삭제 1,303줄에 `/search`·`/stories`·`self-check.sh`·마이그레이션 4개 포함.
- ✅ **닫힌 PR 6건 전수 확인** — #767·#548·#705·#515·#518·#817 전부 **후속 PR 로 대체됨**을 본판 코드로 교차확인(예: #767 의 `DUPLICATE_IDENTITY` 가 `app/consultation/[id]/page.jsx` 에 실재).
- ❌ **빌드·CI 안 돌림** — 이 브랜치는 문서 3개만 바뀌어 앱 빌드에 영향 없다고 판단했으나 **직접 확인은 안 했다** → 5번 1항.
- ❌ **보관 3건의 되살리기 자체는 안 해봤다** — "되살리는 비용"은 파일 수·검증 기록 기준 추정이지 실제 재적용은 안 했다.

**7. 다음 세션 첫 프롬프트**

> `docs/PROJECT_CONTEXT.md` 최상단 읽어. 직전 세션이 **겹친 세션들 정리 + 「보관만 해둔 것」 전수 색출**을 했고, 보관 3건이 표로 정리돼 있다(`/partners` 랜딩 · 만족도 설문 발송기준 · 소견 AI 자동번역). 먼저 할 일 = ①이 핸드오프 PR CI 초록이면 머지 ②**보관 3건 PO 결정** — 특히 만족도 설문은 8/27 평가 지표라 **7/31 이 사실상 데드라인** ③`/admin/treatments` 사람 클릭 왕복 확인. 🚫 함정: 브랜치 정리할 때 `git rev-list` 로 판단하지 마라(스쿼시 머지 때문에 머지된 것도 미머지로 뜬다) — **PR 유무로 갈라라.** 낡은 브랜치의 핸드오프 PR 은 삭제줄 수 먼저 확인(#839 가 그래서 닫혔다).

---

## 🔖 세션 핸드오프 (2026-07-21 저녁 — "쌓인 내용 정리해봐라" → **치료 등록 5개월 0건의 진범 색출 + 브랜치 241→4개 정리(회수 6건)**)

> PO "쌓인 내용 정리해봐라"로 시작. 정리하다 **치료(시술) 등록이 5개월간 0건**인 걸 발견해 원인을 파고들었고, 브랜치를 훑는 과정에서 **지웠으면 사라졌을 것 6건**을 건졌다. 이 세션의 교훈 한 줄: **목록만 보고 일괄 삭제하면 안 된다 — 4개 브랜치에 건질 게 있었다.**

**1. 이번 세션 한 일**

*치료 유령 컬럼 (머지 완료)*
- **[#831 머지]** 치료·병원 등록이 한글 이름이면 통째로 실패하던 것. 원인이 **두 겹**이었다: ①옛 미용시술 카탈로그 스키마 잔재 컬럼 17개 ②**진범 = 공용 헬퍼 `extractKrFields()`** — `name_kr`·`description_kr`·`tags_kr`·`specialties_kr` 을 payload 에 얹는데 **이 넷이 어느 테이블에도 없어**(실측: `%_kr` 은 `hospitals.location_kr` 하나뿐) 이름에 한글이 들어가는 순간 저장이 실패. **치료뿐 아니라 병원 등록·수정·프로필도 같이 막혀 있었다.** 헬퍼 삭제 + 실컬럼(`duration`·`recovery_time`·`preparation`·`risks`)으로 대체, 어드민·병원포털 폼과 환자 상세페이지까지 배선(입력→저장→표시가 처음으로 연결됨).
- 곁다리로 닫힘: 오퍼 자동추출 마지막 단계(`offers/apply`) · `rag_documents` 의 `verified_at`/`verified_by` · 번역 트리거 누락(주의사항만 고치면 번역이 안 돌던 것).
- **가드 보강**: `check-schema-refs.mjs` 축 D 에 **변수 경유 쓰기**(`const payload={…}` … `.insert([payload])`) 검사 추가 + 스프레드 통째 건너뛰기 폐지 + CRLF 정규화(윈도우에서 검사기가 통째로 죽던 것). 자기시험 **7 → 19개**. 반성문 `POSTMORTEMS.md` **#103**.

*데이터 정리 (PO 지시)*
- 치료 더미 **9건 전부 삭제** + 딸린 **RAG 문서 6건**도 삭제(안 지웠으면 AI 챗이 환자에게 산후조리·난임 프로그램을 계속 안내). 백업 = `_backup_treatments_20260720`(9행) · `_backup_rag_documents_treatments_20260720`(6행). **`treatments` 테이블은 현재 0행** — 이제 새로 넣는 게 곧 실데이터다.

*브랜치 정리 — 241개 → 4개*
- **원격 브랜치 237개 삭제**(머지됨 207 · 닫힘 19 · 대체됨 11). 판정 근거는 전부 실측(PR 상태 + 본판과 파일 동일성 대조).
- **지우기 전에 건진 것 6건**: 마이그레이션 3개(**[#844 머지]** — DB엔 적용됐는데 저장소에 없던 것, 하나는 KHIDI 만족도 지표 배선) · 유실 세션기록 3건(**[#842 머지]** — 그중 하나는 비번 원문 노출 **보안 수정 기록**) · 🔴 살아있는 미해결 이슈 2건(환자포털 단절, 만족도 설문 0건).
- 대체 확인 후 삭제한 예: `feat/portal-unified`(폐기된 Premium 디자인 위에 지어짐) · `feat/tier-restructure`(`/partner` → `/hospital` 개명 전 옛 경로) · `claude/trademark-korean-logo`(시간대 수정이 본판에 이미 동일하게 존재).

*타입 자물쇠 (머지 완료)*
- **[#836 머지]** `pickAllowed<T>` — 허용 컬럼 목록을 생성타입으로 대조해 **컴파일 단계에서** 유령 컬럼을 막는다. 독립 리뷰가 **"가드 자신이 조용히 실패한다"**(타입 인자 빠뜨리면 그냥 통과)를 잡아내 **`NoInfer` 로 실패-닫힘** 보강. 시험 구멍(구현을 바꿔도 통과하던 것)도 돌연변이 시험으로 확인해 메움.

*보관 처리 (PO 지시)*
- **[#849 초안]** `/partners` 공개 제휴 랜딩(B2B 유입구, 6개 언어 1,015줄) — 완성돼 있으나 PO "일단 보관". 초안 상태로 박아 실수 머지 차단.
- `rescue/local-uncommitted-20260716`(코드 974줄 — 소견 AI 자동번역·설문 발송 기준 변경·`kpiHealthcheck`) — PO "나중에 쓸 일 있음 쓰게 보관만".
- 둘 다 `KNOWN_ISSUES.md` 「📦 보관」 항목에 **"방치 브랜치로 오해해 지우지 마라"** 명시.

**2. 왜 그렇게 했는지**

- **더미 데이터를 되묻지 않고 지운 이유**: 처음엔 "6개 언어 번역·실가격이 있으니 진짜 콘텐츠 아니냐"고 PO 에게 **두 번 되물었다가 지적받았다**("몇 번 얘기하니"). 데이터가 **잘 만들어져 보이는 것**은 더미가 아니라는 근거가 못 된다 — 씨드 스크립트가 그럴듯하게 채운 것뿐. **피벗 전 도메인인지(산후조리·난임·소아성장 = 여성/웰니스)로 판단**해야지 완성도로 판단하면 매번 틀린다. 안전장치는 **질문이 아니라 백업**.
- **브랜치를 하나씩 열어본 이유**: PO 지시. 결과적으로 옳았다 — 6개 브랜치 중 **4개에 건질 게 있었다**. 특히 문서만 바뀐 브랜치는 "핸드오프니까 중복이겠지"로 넘기기 쉬운데, 실제로는 **본판에 한 번도 안 들어간 유실 기록**이었다.
- **가드를 새로 얹지 않고 뚫린 걸 고친 이유**: 이번은 #97 **부류 재발**이다. 재발이면 새 가드를 추가할 게 아니라 **왜 기존 가드가 못 막았는지**를 고쳐야 한다(CLAUDE.md 재발 추적 규칙).

**3. 안 끝났거나 보류**

- **🔴 만족도 설문이 구조적으로 0건** — 운영 발송 경로는 `dispatch-surveys` cron 하나뿐인데 `consultation_sessions.status='completed'` 를 찾는다. **35건 전부 `scheduled`, completed 0건** → `surveys` 0건. 만족도는 **8/27 중간평가 공식 지표 4개 중 하나(목표 90점)**. 해법(발송 기준을 `inquiries.case_status='follow_up'` 으로 이전)은 `rescue/local-uncommitted-20260716` 에 있고 **DB 는 이미 준비 끝**(`surveys.inquiry_id`·`inquiries.followup_started_at` 실재). **PO 가 "보관만" 지시해 미적용.**
- **🔴 환자 포털 ↔ 운영데이터 구조적 단절** — 2026-06-23 진단이 **4주 지난 지금도 그대로**(`inquiries.patient_user_id` 컬럼 없음 · `followup_schedules` 0행 · `consultation_sessions.patient_id` 35건 전부 null). 데이터만 2배로 쌓임. `KNOWN_ISSUES.md` 최상단에 복원.
- **로컬 브랜치 175개 중 160개가 껍데기**(원격이 사라진 것). **안 건드렸다** — 이 저장소를 **워크트리 10곳이 공유**하고 다른 세션 2곳이 지금도 작업 중이라, 잘못 지우면 그쪽이 꼬인다. 세션 시작 훅엔 안 뜨므로 실질적 방해는 없음.
- 치료 9건 삭제로 **`treatments` 0행** — 한방 특화 페이지(`/specialties/korean-medicine`)의 프로그램 목록이 비어 있다(원래도 전부 미공개라 비어 있었음, 변화 없음).

**4. 주의·함정**

- ⚠️ **`extractKrFields()` 를 되살리지 마라.** 삭제했다. `name_kr` 류는 어느 테이블에도 없는 컬럼이고 되읽는 코드도 0건이었다. 한국어 원문 경로는 `name_ko`+`i18n`(`triggerMultiLangTranslation`).
- ⚠️ **축 D 가드의 원리적 한계**: *함수 반환값 스프레드*(`...someHelper(…)`)와 *허용목록 루프* 안쪽은 **정적 검사가 불가능**하다. 실제로 이번 진범이 그 사각으로 빠져나갔다. **진짜 종결은 supabase-js 2.110 업그레이드**(#836 이 그 1차).
- ⚠️ **`work/partners-landing` · `rescue/local-uncommitted-20260716` 를 「방치 브랜치」로 오해해 지우지 마라.** PR 이 없거나 초안이라 정리 대상처럼 보이지만 **PO 가 보관을 지시한 것**이다.
- ⚠️ **rescue 브랜치를 통째로 얹지 마라** — 2026-07-16 이후 본판이 크게 움직였고 `caseStatus.ts` 는 KHIDI 지표 계산 직결이라 잘못 얹으면 숫자가 틀어진다. **덩어리 단위로 떼서 최신 본판 위에 재작성**이 안전.
- ⚠️ **장부 파일(`KNOWN_ISSUES.md`)에 PR 을 여러 개 걸지 마라** — 이번에 #837 과 #842 가 같은 파일을 고쳐 충돌(DIRTY)이 났고, #837 을 #842 로 합치고 닫아서 해소했다.

**5. 다음 세션이 먼저 할 일**

1. ⚠️ **직전 미검증분 먼저 확인 — 치료 등록을 사람이 실제로 클릭해서 해보기.** API·DB 층은 실측했지만(한글 이름 치료를 실DB 에 insert → 성공 → 삭제) **어드민 화면에서 사람이 클릭하는 왕복은 못 했다**(실서비스 로그인 필요). `/admin/treatments` 에서 치료 1건 등록 → 저장되는지 → 목록에 뜨는지 확인.
2. **만족도 설문 0건을 어떻게 할지 PO 에게 확인.** 2026-08-27 중간평가 지표라 데이터가 쌓일 시간이 필요하다 — 해법은 준비돼 있고(위 3번) 파일 2개만 살리면 되지만 PO 가 "보관만" 지시한 상태다. **설문은 보낸 뒤 응답까지 와야 점수가 되므로 늦어도 2026-07-31 까지는 결정이 필요**하다는 점을 알릴 것.
3. `/partners` 랜딩(#849) 검토 — 프리뷰 https://healo-khidi-git-work-partners-landing-bonrois-projects.vercel.app/ko/partners . 정할 것 2개: **유입 링크 위치**(헤더·푸터 어디에도 없음) · **대학병원 표기 정직성**(협진 네트워크로 4곳을 적었는데 본로이 직접 MOU 가 아님).
4. (여유 있으면) 로컬 브랜치 160개 정리 — 다른 워크트리 10곳이 공유하니 **다른 세션이 안 도는 시간에** 할 것.

**6. 검증 상태**

- ✅ **CI 통과 실측** — 머지한 4건(#831·#836·#842·#844) 전부 GitHub CI(ci·Smoke Tests·Vercel) 초록 확인 후 머지. 열린 PR = **#849(초안·보관)** · **#839(다른 세션)**.
- ✅ **본판 반영 실측** — `git show origin/main:` 으로 파일 실재 확인: 마이그레이션 3개 · `pickAllowed.ts`(NoInfer 3곳) · `KNOWN_ISSUES` 보관항목 3건 · 창고 회수기록.
- ✅ **실DB 왕복 검증** — 고친 코드가 만드는 payload 모양 그대로 **한글 이름 치료를 실DB 에 insert → 조회 성공 → 삭제**. `/partners` 접수 폼도 동일 방식으로 `partner_outreach` 저장 확인(테스트 행 삭제 완료).
- ✅ **가드가 원래 버그를 잡는지 실증** — 고치기 전 코드로 되돌려 실행 → 유령 컬럼 **10건 적발**. 독립 리뷰가 제시한 **오탐 재현 코드 3종은 전부 exit=0**(오탐 0).
- ✅ 3중 게이트 — 독립 리뷰 2라운드(#831) + 완성도 감사 3라운드(#831) + 독립 리뷰 1라운드(#836). **각 게이트가 서로 다른 결함을 잡았다**(리뷰=진범·오탐, 감사=한국어 누출 반쪽배선).
- ❌ **검증 못 함 — 어드민 화면 사람 클릭 왕복.** 실서비스 로그인이 필요해 못 했다 → 5번 1항으로 승격.
- ❌ **검증 못 함 — `/partners` 폼을 실제로 제출해보지 않음.** DB 층만 확인했고 화면에서 버튼을 누르진 않았다.
- 📝 **정정: 이번 세션에서 내가 "빌드 통과"라고 여러 번 보고한 것은 근거가 부실했다.** `grep -E "Compiled|Failed|error"` 로 확인했는데 실제 오류 문구는 `Error occurred prerendering`(대문자 E)이라 **패턴에 안 걸렸고, 종료 코드를 한 번도 안 봤다.** 실제로는 이 워크트리에서 `next build` 가 exit 1 이었다. **다만 origin/main 도 동일하게 실패**(이 폴더에 `.env` 없음 + Next.js `workStore` 내부 오류)하고 **CI 는 통과**하므로 코드 결함은 아니다. 앞으로 빌드 확인은 **종료 코드로** 할 것.

**7. 다음 세션 첫 프롬프트**

> 먼저 `docs/PROJECT_CONTEXT.md` 최상단 핸드오프 읽어. 그다음 ①어드민 `/admin/treatments` 에서 치료 1건 실제로 등록해보고 저장·목록표시까지 되는지 확인(직전 세션이 API·DB 만 검증하고 화면 클릭은 못 했음) ②만족도 설문이 구조적으로 0건인 건(8/27 평가 지표) 어떻게 할지 나한테 물어봐 — 해법은 `rescue/local-uncommitted-20260716` 에 준비돼 있고 파일 2개만 살리면 된다더라.

---

---

## 🔖 세션 핸드오프 (2026-07-21 오후 — 서치콘솔 유령 URL 1건 추적 → **폐기 라우트 7개가 "임시 이사"라 색인에 눌러앉아 있던 것 수정 + 죽은 코드 삭제**)

> PO가 서치콘솔 스크린샷과 함께 `https://healwith.co.kr/en/treatments/fertility-support-program` 링크를 주며 "여기 페이지 뭔지, 왜 404 나오는지. 리뉴얼 전 잔재 같은데 정확하게 파악해봐"로 시작. 같은 날 다른 블록(성능 진단 / 백업 암호화)과는 **별개 세션**이다.

**1. 이번 세션 한 일**

- **[PR #847](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/847) 머지·프로덕션 배포 완료** (`9f0e8440`).
  - 폐기한 공개 라우트 **7개를 임시(307) → 영구(308) 리다이렉트**로 전환: `/search`·`/stories`·`/intake`·`/consult/start`·`/success`(각 `page.jsx`, `permanentRedirect()`) + `/partner`·`/doctor`(`proxy.ts` 미들웨어 분기, `NextResponse.redirect(url, 308)`).
  - **죽은 코드 삭제**(PO "둘다 날려버려"): `app/search/SearchResultsClient.jsx`(16KB) · `app/stories/StoriesClient.jsx`(9.6KB) · `src/lib/stories/storiesData.js`. 리다이렉트 껍데기 `page.jsx` 2개는 옛 링크·북마크용으로 남김.
  - **자동검사 룰 `check:content` §23 신규**: 공개 영역에서 "기본 export 본문이 리다이렉트 한 줄뿐인 page"(=라우트 폐기용 껍데기)가 `redirect()`를 쓰면 CI 실패.
  - **반성문 #104** 기록 (🔁 #20 부류 4연타).
- **PO가 물어본 URL 자체는 조치 불필요**로 판정 — 이미 정상 404. `fertility-support-program` = 2026-02-23 `scripts/seed-myeonryeok.cjs`로 심은 면력한방병원 한방 웰니스 더미 6종 중 하나(「난임지원 프로그램」). 7/14 비공개 토글 → 7/20 DB 삭제(백업 `_backup_treatments_20260720`에 9행 보존).

**2. 왜 그렇게 했는지**

- **서치콘솔 화면의 "통"이 결정적 단서**였다. 그 URL이 「크롤링됨 – 현재 색인이 생성되지 않음」에 있었지 「찾을 수 없음(404)」이 아니었다 = 구글이 **200을 받았던** 기록. 최종 크롤링 2026-07-10 = 소프트404(화면만 404, 상태코드 200) 수리한 **7/14 이전**. 즉 반성문 #87 시절의 잔상이지 현재 결함이 아님 → 손댈 것 없음으로 결론.
- **진짜 문제는 "더 있니?" 전수 스캔에서 나왔다.** Next.js `redirect()`의 기본값이 **307(임시)**이고 저장소 전체에 `permanentRedirect` 사용처가 **0건**이었다. 임시는 구글에 "원본이 곧 돌아온다"는 신호라 옛 URL을 색인에서 안 뺀다. `/intake`·`/consult/start`·`/success` 3개는 **주석에 "영구 리디렉트"라고 써놓고 구현만 임시**였다(의도-구현 불일치).
- **가드를 "부정 제외"가 아니라 "긍정 식별"로 만든 이유**: 초안 2개가 연달아 깨졌다. ①`text.includes("<")` → 주석의 `<StoriesClient />` 부등호 하나로 검사가 통째로 꺼짐(fail-open). ②주석 제거 후 `return <` 검사 → `app/treatments/[slug]/page.jsx`가 JSX를 변수에 담아 `return content;`로 반환해 **오탐**. 최종은 껍데기 형태 자체를 정규식으로 잡는 방식.

**3. 안 끝났거나 보류**

- **없음** — 이번 세션 작업은 머지·배포·실측까지 종결. 다음 세션이 이어받을 잔여 작업 없음.
- (선택) 서치콘솔에서 해당 항목 「수정 결과 확인」 버튼 — PO 몫이며 안 눌러도 다음 크롤에 자동 반영.

**4. 주의·함정**

- ⚠️ **`pkill`은 Windows에서 조용히 실패한다** (이번에 실측으로 발견). Git Bash에서 `pkill -f "next start"`를 돌려도 에러 없이 프로세스가 **살아남는다** — 이번 세션이 측정용으로 띄운 `next start` 4개 + dev 1개가 전부 유령으로 남았고, 워크트리 삭제가 `Invalid argument`로 막혀서야 발견됐다(`node_modules`의 `next-swc…node` 파일 잠김).
  - 끌 때: PowerShell `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like "*<워크트리이름>*" }` → 그 PID만 `Stop-Process -Force`. **커맨드라인으로 내 것인지 확인 후 죽여라** — node 프로세스 대부분은 Claude Code 자체 MCP 서버(kordoc·server-pdf·mcp-remote)라 무턱대고 죽이면 세션이 깨진다.
  - **워크트리 삭제가 "Invalid argument"로 실패하면 십중팔구 이 유령이 원인.**
- ⚠️ **`/partner`는 robots.txt Disallow 대상이 아니다.** `app/robots.js`에는 `/hospital`(새 이름)·`/doctor`만 있고 `/partner`(옛 이름)는 리네임하면서 누락됐다. 그리고 `proxy.ts`의 `/partner` 분기(L253)가 `/hospital` 인증 체크(L299)보다 **먼저** 실행돼 미로그인·구글봇도 그냥 리다이렉트를 받는다 = **로그인 벽 밖**. 코드 주석에 있던 "로그인 벽 안이라 SEO 무관"은 거짓이었다(실측으로 정정).
- ⚠️ **robots.txt Disallow는 색인 제거 수단이 아니다 — 오히려 반대.** 크롤을 막으면 구글이 리다이렉트·noindex를 **못 보므로** URL-only 항목이 더 오래 남는다. `/doctor`가 그 상태(308을 걸었지만 Disallow라 구글이 못 봄). 실제로 색인에 뜨면 robots에서 풀어 308을 보게 하는 게 제거 경로 — 현재는 노출 근거가 없어 보류.
- **이 사이트의 308은 브라우저 캐시가 안 된다** — 응답 헤더가 `Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate`. 즉 "영구 리다이렉트라 되살리기 어렵다"는 일반론이 여기선 **적용 안 된다**(파일만 원복하면 됨). 세션 도중 어시스턴트가 확인 없이 "캐시돼서 성가시다"고 말했다가 실측으로 정정한 대목.
- **가드 §23의 알려진 한계(미탐)**: 폐기 껍데기를 화살표 함수 등 다른 형태로 쓰면 검사가 안 돈다. 오탐으로 CI를 막는 것보다 낫다고 판단해 이 쪽을 택함(주석에 명시).
- ⚠️ **(핸드오프 머지 후 추가) 공용 폴더(`C:\Users\user\Desktop\HEALO_KHIDI`)에서 새 세션을 열면 세션시작 훅이 *낡은* 핸드오프를 띄운다.** 실제로 이번에 그랬다 — 그 폴더가 다른 세션 브랜치(`fix/encrypt-backup-tables`)에 앉아 `origin/main`보다 6커밋 뒤쳐져 있었고, 훅은 **워킹트리 파일**을 읽으므로 2026-07-21 머지한 핸드오프가 안 보였다. `main`은 `hospital-info` 워크트리가 점유 중이라 그 폴더에서 checkout 으로 못 고친다. → **새 세션은 반드시 `bash scripts/new-session.sh <영역>`으로.** 공용 폴더에서 세션을 열지 않는 것이 유일하고 확실한 회피책(병렬세션 규칙 1번의 몰랐던 부작용).

**5. 다음 세션이 먼저 할 일**

1. ⚠️ **직전 미검증분 먼저 확인**: **구글이 실제로 옛 URL을 색인에서 빼는지는 검증 못 했다**(구글 쪽 동작이라 몇 주 걸림). 서치콘솔 「페이지」에서 `/en/treatments/fertility-support-program` 항목이 「크롤링됨–색인 안 됨」 → 「찾을 수 없음(404)」으로 옮겨갔는지, `/search`·`/stories`·`/intake` 등이 「페이지에 리디렉션 있음」으로 잡히는지 확인.
2. `docs/KNOWN_ISSUES.md`에 이번 건 관련 항목이 있으면 종결 표시(있는지 확인 안 했음).
3. 그 외 이 세션발 잔여 작업 없음 — 새 주제로 진행 가능.

**6. 검증 상태**

- ✅ **[PR #847](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/847) CI 전부 초록** — `ci` 3m33s pass / `Smoke Tests (PR)` 5m7s pass / `Vercel` 배포 완료. 스쿼시 머지 후 `origin/main` = `9f0e8440`.
- ✅ `npx next build --webpack` 통과 / `npm run check:content` 통과.
- ✅ **프로덕션 실측(배포 후)**: 7개 경로 전부 **308** 확인. 최종 도착지 200 확인(`/search`→`/en/hospitals`, `/stories`→`/en`, `/intake`·`/success`→`/en/inquiry`).
- ✅ **원본 URL 404 확인** — 일반 UA + 구글봇 UA 둘 다. 폐기 치료 slug 9개 전부 하드 404.
- ✅ **사이트맵 정상** — 188 URL, 폐기 경로 혼입 0건.
- ✅ **삭제 안전성** — 삭제 전 참조 전수 확인(import 0건), 삭제 후 `main`에서 파일 부재 확인, 고아 에셋 0건(storiesData의 이미지 3개는 전부 외부 Unsplash URL).
- ✅ **가드 유효성 3중 검증** — ①현 상태 오탐 0 ②수정 되돌리면 적발 ③fail-open 케이스(주석에 JSX 표기) 재현해도 적발.
- ✅ **독립 리뷰 게이트 통과** — 별도 리뷰 에이전트가 **결함 3건 적발**(가드 fail-open / 반성문의 거짓 근거 / `patient/education` 신호 불일치). 전부 실측 재확인 후 2차 커밋으로 반영. **리뷰 없었으면 거짓 근거가 반성문에 그대로 남았을 건**이라 이 게이트의 가치가 다시 입증됨.
- ❌ **구글 색인 실제 제거는 검증 못 함** — 구글 쪽 동작이라 확인 수단이 없다(위 5-1로 승격).
- ❌ **`docs/KNOWN_ISSUES.md` 관련 항목 존재 여부 미확인** (위 5-2).

**8. (머지 후 추가) 이 세션의 자기평가 — 다음 세션이 반복하지 말 것**

PO가 *"뭐 물어볼 때마다 뭐가 계속 파도파도 나오냐? 설정 꼬였냐?"*라고 물어 원인을 따져본 결과(설정은 정상 — 훅 6개 작동·CI 통과 확인):

- **이번 세션에서 어시가 스스로 못 잡고 남이 잡아준 결함 5건.** 독립 리뷰가 2건, **PO 되물음이 3건.**
- 그중 3건은 **정보가 이미 손에 있었는데 안 본 것**: ①리뷰 보고서에 "이건 확인 못 했다"고 적힌 캐시 헤더를 안 보고 완료 보고 ②`robots.js` grep 한 번이면 될 걸 코드 주석 복사 ③세션시작 훅 출력이 화면에 떠 있었는데 낡은 핸드오프인 걸 못 봄.
- **공통 패턴: PO가 물어본 딱 그 범위만 확인하고 멈춘다.** 그래서 PO가 한 번 더 물으면 한 겹 더 나온다 — **PO의 되물음이 검증 단계를 대신하고 있는 상태.**
- **⛔ "그럼 훅을 만들자"는 반사적 제안을 하지 마라.** 이 세션에서 실제로 그랬다가 PO에게 *"고쳐질지 자신 없다면서 훅은 왜 만드냐"*고 지적받고 철회했다. 검출 조건이 자연어 문자열 맞추기라 오탐투성이 + 3건 중 1건만 커버 → **고친 것처럼 보이는데 안 고쳐진 상태**가 되므로 안 만드는 게 맞다.
- **✅ 실제로 값을 한 건 「독립 리뷰 게이트」 하나뿐이다. 단, 이번엔 PR을 올린 *뒤에* 돌려서 거짓 근거가 PR 본문·반성문에 이미 들어간 뒤였다.** → **다음부터는 PR 올리기 전에 돌려라.** 새 장치를 만들 게 아니라 있는 장치를 더 일찍 쓰는 것이 이 부류의 유일한 실효 대책.

**7. 다음 세션 첫 프롬프트**

> 먼저 `docs/PROJECT_CONTEXT.md` 최상단 핸드오프 읽어. 그 다음 서치콘솔에서 폐기 URL들이 색인에서 빠지고 있는지 확인해줘 — `/en/treatments/fertility-support-program`이 「크롤링됨–색인 안 됨」에서 「찾을 수 없음(404)」으로 옮겨갔는지, `/search`·`/stories`·`/intake`가 「리디렉션 있음」으로 잡히는지. 그리고 `docs/KNOWN_ISSUES.md`에 이번 건 관련 항목 있으면 종결 처리해.

---

## 🧭 오전 다중세션 통합 정리 (2026-07-01) — 무엇이 배포됐고 / 무엇이 미머지로 남았나

> **왜 이 블록:** 오늘 오전 여러 창(병렬 세션)에서 각자 작업 후 각자 핸드오프 → 배포된 건 main·SoR에 잘 쌓였지만, **끝냈는데 아직 본판에 안 합친(미머지) 작업 3건**이 각 세션 브랜치에만 있고 이 SoR엔 기록이 없었음(=다음 세션이 놓칠 위험). 그 3건을 여기 한 곳에 모아 다음 세션 큐로 승격. (세부 이야기는 아래 오후·오전(2) 핸드오프 + `archive/`에 이미 있음 — 여기선 안 겹치게 '무엇이 남았나'만.)

**✅ 오늘 오전 실서비스(main·프로덕션) 반영 완료** — 다 머지·CI초록:
- **어드민 대청소** #555 (가짜숫자·가짜성공률 제거 / 병원 6곳 활성화 / 매칭 실작동 / 상담취소 실API / AccuracyPanel 실측)
- **병원 페이지** #554 의사사진 자체호스팅(핫링크 제거) + 토글 애니 · #559 신촌 의료진 현행화(27→28명)·실사진 · #565 토글 "밀림" pin 수정
- **콘텐츠 자체호스팅** #551 암종 카드/합병증 이미지(immunehospital 핫링크 제거)
- **비자** #549 체크리스트 계정동기화·러카 누적일수 · #552 PDF 톤 Premium→기본톤 교정
- **환자앱** #544 견적 상세페이지 6개어화(ko/en/ru/kz/zh/ja)
- **KHIDI 지표** #557 만족도 표본부족 env 스위치 · 유치 전환 대시보드 **채널별(유입경로) 분해** (migration 반영)
- **SEO** #547 BreadcrumbList·WebSite SearchAction 구조화데이터
- **디자인/정리** #560·#561 활성 디자인 명칭 'legacy'→'기본 톤' 개명 · #539 죽은 premium 이메일 시스템 삭제

**✅ (완성도 감사 2026-07-15 종결) 아래 3건은 전부 main 머지 완료** — #562=`45e58f7c`·#567=`e83f9b50`·#545=`9a8dcb9c`. 문서만 "미머지"로 남아 있던 드리프트(#63 부류). 아래는 이력 보존용:
1. **파트너 발굴 아웃리치 추적기** [PR #567 · 브랜치 `work/partner-outreach`] — 코디·어드민 백오피스 신규 기능(카자흐 직원 Assel이 파일 대신 백오피스에서 파트너 영업 추적). **완성 + 프로덕션 DB에 표 `partner_outreach`+시드 6곳 이미 적용.** 남은 것: ①프리뷰에서 화면 클릭 검증(후보추가·상태변경·탭필터·CRUD, 코디+어드민 둘 다) → 이상 없으면 **머지** ②Assel 계정에 코디네이터 권한 부여(`/admin/staff`). (큰 UI라 PO 눈으로 보고 머지하기로 했던 건)
2. **초청장 발급주체 = 등록 유치의료기관(병원) 명의** [PR #562 · 브랜치 `claude/kazakhstan-keta-config-ko4g7b`] — `VisaInvitationLetter.jsx`+`inviterHospitals.ts` 완성, 미머지. (같은 세션의 비자 정정 #535·541·549·552는 이미 머지됨 — #562만 남음.)
3. **이메일 수신률 문서** [PR #545 · 브랜치 `work/email-deliverability`] — `docs/EMAIL_DELIVERABILITY.md`(DMARC·콜드 아웃리치 플레이북). DMARC 감시 켜기·Google Postmaster 등록은 이미 실행(외부 완료). 문서라 CI 초록시 자동머지 대상.
- (추가 열린 검증) #565 토글 "밀림"은 코드·배포 반영됐으나 **실브라우저 스크롤 동작만 미검증**(검증환경 헤드리스라 눈으로 못 봄) — 오전(2) 핸드오프 6번 참조.

**🧹 정리해도 되는 브랜치(작업 이미 main에 반영 = squash 머지됨, 지워도 안전):** `claude/handoff-2026-07-01-am`·`handoff/admin-cleanup-0701`·`work/admin-backoffice`·`work/hospitals-roster-refresh`·`work/hospitals-toggle-ui`·`work/hospital-toggle-scroll-fix`·`claude/rescue-548-doctor-selfhost`·`claude/seo-audit-improvements`·`claude/inspiring-williamson-56fbfc`·`claude/patient-detail-i18n`·`claude/satisfaction-min-n-env`·`claude/fix-all-errors-sweep`·`claude/khidi-conversion-source-breakdown`·`claude/handoff-cancer-img-selfhost`. ~~**남겨둘 것(미머지 작업 있음):** `work/partner-outreach`·`claude/kazakhstan-keta-config-ko4g7b`·`work/email-deliverability`.~~ → 세 브랜치 모두 머지 완료(위 참조), 이제 정리해도 안전 (완성도 감사 2026-07-15).

---

## 🏷️ 서비스명 변경 — HEALO → **healwith** (2026-06-16 확정·적용)

**상표 문제로 서비스명을 `HEALO` → `healwith`(항상 소문자 표기)로 최종 변경. 앞으로 모든 신규 작업은 `healwith`로 한다.**

- **표기 규칙**: 화면·문서 어디서나 **소문자 `healwith`** (문장 첫머리도 소문자). 로고는 투톤(heal=teal-600 / with=slate).
- **이번에 바꾼 것 (화면에 보이는 것)**: app/src/components 의 브랜드 텍스트·i18n 6개 언어 문자열·메타데이터·이메일 발신자명·PDF/견적/초청장 문서번호 접두사·헤더 워드마크·favicon(`h`)·manifest. (`HEALO`→`healwith` 약 1,144곳)
- **일부러 안 바꾼 것 (그대로 둠 — 건들면 깨지거나 기록보존)**:
  - `HEALO-KHIDI` (코드 내부 프로젝트 코드명, 20곳), `HEALO_EMAIL_FROM` (환경변수명)
  - `healo-khidi` (Vercel 프로젝트명·배포 URL·repo = 인프라), `components/healo/` (폴더 경로), 소문자 `healo`(예시 비번 `healo1234`·placeholder 이메일·기존 `healo.kr` URL)
  - **docs 내부 개발 히스토리 문서**: 과거 기록이라 본문 유지 (이 핸드오프 노트로 변경 사실만 명시).
- **아직 남음 (TODO)**:
  - ~~PNG 앱아이콘 재생성~~ ✅ **완료**(2026-06-23 `943481c`, KNOWN_ISSUES:358 종결과 일치 — 완성도 감사 2026-07-15가 문서 간 모순 교정).
  - ~~도메인 `healwith.co.kr` 등록~~ ✅ **완료**(2026-06-29 라이브 HTTP 200, LAUNCH_GATES 관문12 일치 — 완성도 감사 2026-07-15 교정).
  - **상표 출원**(Madrid) 별도 트랙.
  - Vercel 프로젝트명/배포 URL 변경은 인프라 마이그레이션이라 보류(현 `healo-khidi.vercel.app` 유지).
- 계획·범위 상세: `docs/REBRAND_HEALWITH_PLAN.md`.

---

## 1. 이 서비스가 뭔가 (피벗 후)
- **KHIDI HEALO** = 카자흐스탄·러시아·CIS **암환자**를 한국 **종양 병원**으로 연결하는 의료관광 컨시어지.
- **중요한 피벗**: 예전엔 "한국 전체 병원 디렉토리(크롤링)"였으나 → **암환자 컨시어지**로 전환. 디렉토리 시절 잔재(대량 import·크롤링 등)는 "레거시"로 분리.
- 자금: KHIDI 정부지원과제 + Bonroi 개인사업자. PO 혼자 운영.

## 2. 핵심 전략 결정 (왜)
- **"병원 매칭 마켓플레이스" 아님 → "연속 케어 컨시어지"**: 제휴 병원이 면력한방병원 3곳(진단·면역·재활, 수술 X) + 협진 대학병원 4곳(수술·항암)뿐. 100개 중 1개 고르는 게 아니라 **진단→수술 연계→면역·재활을 쭉 잇는** 모델. 그래서 홈·AI챗·치료여정의 "매칭" 표현을 "케어 경로/상담 배정"으로 톤다운함. (`/care-journey` 페이지가 이 스토리)
- **매칭 엔진 코드는 보존**하되 환자 전면엔 안 붙임 (미래 확장용).

## 3. 디자인 (DESIGN.md 가 헌법)
- **"기본 톤"만 표준** (Airbnb 스타일: 흰 배경·teal-600·시스템폰트·rounded-xl). ※ 예전 "legacy 모드"를 **2026-07-01에 "기본 톤"으로 개명** — "legacy(옛날꺼)"라는 이름이 "premium으로 올려야지"라는 정반대 오해를 반복 유발해서. 이제 디자인은 하나뿐, 모드 토글 없음.
- **Premium 톤 폐기**: 검은배경·금색·serif·필름그레인 = "럭셔리 호텔" 느낌이라 PO·대표가 거부. 정부과제 성격과 안 맞음. **되살리기·재활용 금지 — 폐기된 옛 실험이지 업그레이드 아님.**
- PO가 가장 싫어하는 것: **"AI가 만든 느낌"** (큰 컬러원+큰아이콘, 똑같은 카드 반복, 이모지 도배, 의미없는 영문카피).
- 공개 페이지(/treatments·상세·/telemedicine·/faq·/hospitals/immune·404·500) 전부 기본 톤으로 재구성 완료. 옛 premium은 `*Premium.jsx` 잔재로만 존재(비활성, import 금지).

## 4. 주요 기능 현황 (라우트는 CLAUDE.md 참조)
- **통합 문의 퍼널 `/inquiry`**: 진입 시 AI Agent / Human Agent / Inquiry Form 선택. `/intake`·`/consult/start`는 여기로 통합(redirect). Human Agent = WhatsApp·Telegram·WeChat·LINE 4채널 — **실제 동작(2026-07-02 실측 정정): WhatsApp만 라이브(코드 폴백 wa.me, #73), 나머지 3채널은 env(`NEXT_PUBLIC_MESSENGER_*_URL`) 미설정이라 카드 자체가 숨김 처리**("준비 중" 표시 아님 — 미완성 인상 안 줌, 1채널뿐이면 picker 생략 직행).
- **원격협진(LiveKit 영상)**: 코디가 `/admin/consultations`에서 상담 생성(문의에서 환자 선택+의사/코디 드롭다운) → 게스트 초대 링크 → `/consultation/[id]` 영상. LiveKit 키는 Vercel에 설정됨(작동). 예약시각 KST 입력·KST+UTC 병기.
- **회원관리**: `/admin/staff`(의사·코디 — 역할부여·임시비번·소프트 비활성), `/admin/users`(환자 — 상담이력·소프트 ban). 계정은 어드민에서 생성(이메일 형식이면 가짜 `doc1@healo.local` 도 가능, 메일 수신 불필요).
- **어드민 메뉴**: 운영현황 / 환자여정 / 제휴자원·RAG / AI품질·시스템 / 레거시도구 (피벗 반영 재편).
- 보안: inquiries/chat_threads/consultation_sessions 는 **service_role 전용 RLS + PII 암호화** → 반드시 서버 API 경유.

## 5. 지금 막혀있거나 PO 결정 대기
- **서비스명 변경**: HEALO 상표권 문제 → 새 이름 정해야 함(미정). 정하면 도메인 등록 + Madrid 출원.
- **메신저 URL 4개**: Vercel env(`NEXT_PUBLIC_MESSENGER_*_URL`)에 넣어야 채널 활성. Telegram 봇·WhatsApp 비즈니스는 PO가 가입.
- **병원 사진 전체**: 주워온 이미지(immunehospital 배너·시술컷, unsplash, 세브란스 위키미디어) **전부 제거** → "이미지 준비 중" 플레이스홀더(`_coming-soon.svg`)로 대체. PO가 직접 제공하는 실사진만 적용 원칙. **성동만 PO 제공 항공샷 적용됨**(`immunehospital-seongdong/1.jpg`).
  - **폴더 규칙**: `public/images/hospitals/<slug>/1~5.jpg` (1=메인 썸네일, 2~5=서브 갤러리). 상세페이지 그리드가 메인1+서브4 자동 정렬. 폴더 8개 생성됨(README.md 참조).
  - **연결 위치**: 마곡·신촌·광명·이대서울·이대목동·고려대구로·세브란스 = **DB**(hospitals 테이블 thumbnail_image/gallery_images/images) / 성동 = **정적**(partnerHospitals.js). PO가 폴더에 사진 넣으면 → DB(SQL) 또는 정적 코드에서 해당 경로로 연결해야 반영됨.
  - 면력 의료진 헤드샷·`/hospitals/immune` 전용 페이지(Photos.js)는 immunehospital.com 공식 사용권 이미지라 미변경(PO가 원하면 교체).
- **고려대구로 "수술 성공률"** 문구: 출처 불명이라 톤다운 유지 중.

## 6. 다음 작업 (KNOWN_ISSUES.md 참조)
- **P1 — portal 데이터 서버 API 이관**: coordinator/inbox·patient/messages·coordinator/messages·알림뱃지가 service_role 테이블을 client로 직접 조회 → 빈 데이터. 단 portal 미활성(메뉴 미연결·코디계정 없음)이라 손님 영향 없음. portal 본격 활성화 직전 일괄 수정 권장.
- **환자 여정 통합 뷰**: ✅ 1단계 완료 — 문의 폼 이메일 필수화(전화 선택) → `/admin/users` 환자 상세에 "과거 문의"를 **이메일로 매칭**해 표시(가입 전 게스트 문의↔계정 통합). 동일인 식별 키 = **이메일**(PO 결정). inquiries.email은 AES암호화(IV랜덤)라 복호화 후 비교(파일럿 규모; 대량화 시 이메일 해시 컬럼 권장). 다음: 상담·견적·비자까지 한 타임라인으로 확장 가능.
- 의사/코디 portal, 비자·견적 admin 감독 뷰(읽기전용 미러) 등.

## 6-1. 공신력 데이터 인용 (콘텐츠 신뢰·SEO)
- **인용 중인 통계**: 한국 암 5년 생존율 **72.9%**(2018–2022, 국립암센터 국가암등록통계) / 2024 외국인환자 **117만명**(KHIDI) / 러시아 누적 16,622·카자흐 14,475명(KHIDI 2009–2024).
- **사용 위치**: `/care-journey`("숫자로 보는 한국 암치료" 섹션, 6개 언어), `/ru/for-russian-patients`·`/kk/for-kazakh-patients`(통계 밴드). 모두 출처 각주 표기.
- **주의**: 한방=암 "치료/완치" 근거로 쓰지 말 것. 통합종양학 문헌은 "보조·삶의질·부작용 관리" 프레임으로만. 통계는 매년 신규 발표 시 갱신.

## 6-1-b. 심층 리서치 결과 (2026-06-11) — `docs/archive/DEEP_RESEARCH_2026_06_11.md` 필독
- **법**: 의료해외진출법 개정(2026.5.26 공포, ~2027.5 시행) — 외국인환자 비대면진료 합법화. 단 진료 주체=유치의료기관 소속 의사 (HEALO는 플랫폼/유치업자 역할로 구조 명확화). 유치업자 등록 확인 + 변호사 자문 + KHIDI 지원시스템 위탁 문의 필요.
- **즉시 5건**: Gemini spend cap 설정 / 모델 별칭 핀(5배 비용 폭탄 방지) / 유치업자 등록 확인 / AI챗 국외이전 고지 / Vercel Pro 전환.
- **카자흐어 통역 해결책 확정**: Gemini 3.5 Live Translate 카자흐 지원 확인 (백업: Gladia). PoC 대기.
- **결제 원칙**: 러시아 직접 결제 불가 → 병원 직접청구 + 카자흐 허브.
- **데드라인**: Supabase 구형 API 키 마이그레이션 (2026년 말 키 제거).
- Supabase 리전 = 서울 확정 (국외이전 부담 최소).

## 6-2. 트렌드 스캔 루틴 (`/trend`)
- PO가 아무 세션에서 **`/trend`** 입력 → 최근 신뢰도 높은 기술·시장 소식 중 HEALO 적용 가능한 "보석"만 선별 보고 (`.claude/commands/trend.md`에 기준 정의). 주 1회 권장. 적용은 PO 승인 후에만.
- 후보 메모: **Gemini 3.5 Live Translate** (2026-06-09 발표) — LiveKit 공식 연동, 분당 $0.023, 음성+자막 동시. 카자흐어 지원 확인 + PoC 1~2일 후 도입 판단 (Gemini 유료 전환·토큰 방어와 묶어서).

## 7. 일하는 방식 (반드시)
- 출시 전 **self-QA**(CLAUDE.md): "빌드 통과 ≠ 동작". DB 기능은 RLS·암호화·데이터흐름 직접 검증. 검증 못 한 건 솔직히 말함.
- 빌드: `npx next build --webpack` (Turbopack 금지). main 푸시 = Vercel 자동 배포.
- 큰 변경은 계획 먼저 보여주고 승인받기. "겸사겸사" 다른 거 건들지 말기.
