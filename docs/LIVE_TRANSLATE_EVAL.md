# Gemini 3.5 Live Translate — 평가 메모 & 통역 파이프라인 진단

> 작성: 2026-06-21 (주간 트렌드 스캔에서 파생). 배포·코드변경 없음 — 의사결정용 메모.

## 1. 트렌드 보석 — Gemini 3.5 Live Translate

- **무엇**: 구글이 2026-06-09 공개한 **실시간 음성→음성 통역** 모델(`gemini-3.5-live-translate-preview`). 70+ 언어, 말하는 도중 몇 초 뒤따라가며 연속 통역(턴제 아님), 억양·속도·피치 보존. **Live API(WebSocket)** 로 제공, **LiveKit에 기본 연동 지원**.
- **출처**:
  - 구글 블로그: https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-live-3-5-translate/
  - Live API 통역 문서: https://ai.google.dev/gemini-api/docs/live-api/live-translate
- **상태**: public **preview**. 단가 **미공개**(우리 비용통제 원칙상 리스크).

## 2. 우리 현재 통역 구조 (실측 — 코드 기준)

화상방(`app/consultation/[id]/page.jsx`)의 자막·통역은 **2경로**:

| 경로 | 트리거 | STT 방식 | 카자흐어 | 특징 |
|---|---|---|---|---|
| ① 브라우저 STT | 기본(크롬에서 동작) | Web Speech API (`useSpeechRecognition.js`) → 텍스트 → `/translate-realtime`(Gemini 번역) | **❌ 미지원 → ru-RU 폴백** | 무료·저지연. 크롬 전용. |
| ② 서버 STT | ①이 실패/미지원/조용한 사망 시 (`forceServerStt`) | 오디오 조각 → Gemini 멀티모달이 **전사+번역 1회** (`/[id]/stt`) | **✅ 지원** | 모든 브라우저 OK. 조각(clip) 단위라 약간 끊김. |

**핵심**: 카자흐어 통역은 경로 ②(서버 STT, Gemini 멀티모달)로 **이미 가능**하다. 즉 "카자흐어 음성통역"은 Live Translate 없이도 이미 된다 — 단 조각(clip) 단위라 연속 스트리밍은 아님.

## 3. Live Translate가 우리한테 주는 실제 증분

이미 Gemini 멀티모달로 전사+번역을 하므로, Live Translate의 추가 가치는 **"카자흐어 지원"이 아니라**:

1. **연속 스트리밍 통역**(조각 왕복 → 끊김 없는 몇 초 지연) — UX 향상.
2. 억양/피치 보존된 **음성 출력**(자막뿐 아니라 TTS 통역) 가능성.

→ 즉 **"있으면 좋은 품질 업그레이드"이지, 없는 기능을 메우는 게 아님.**

### 추천: 🟡 나중에 / 관망

- preview(정식 아님) + **단가 미공개**(비용통제 원칙 위반 소지) + **라이브 검증 필요**(화상방 2명+ 오디오 — 자동 테스트 불가).
- 8/27 중간평가 "ICT 체계·외국인 통역"에 어필 소재는 되지만, **이미 작동하는 서버 STT로 충분히 시연 가능**하므로 평가 전 무리한 교체는 비권장.
- **재검토 트리거**: ①Live Translate 정식(GA) 전환 + 단가 공개 ②서버 STT 조각 끊김에 대한 PO/사용자 불만 발생 시.

## 4. 🔴 이번에 발견한 진짜 구멍 — 크롬 카자흐어 사용자 라우팅

- **증상**: 크롬을 쓰는 **카자흐어 사용자**는 경로 ②(카자흐어 지원)로 **안 넘어가고** 경로 ①의 **러시아어 폴백(ru-RU)** 에 머문다.
- **원인**: `useServerStt` 분기(`page.jsx:1139`)가 **"브라우저 STT가 실패했거나 미지원일 때만"** 서버 STT로 전환한다. 그런데 크롬은 카자흐어를 `ru-RU`로 폴백해 **"지원"으로 판정**(`useSpeechRecognition.js`의 `LANG_MAP.kz = 'ru-RU'`, `stt.isSupported = true`) → 서버 STT 전환 조건에 안 걸림. 결과적으로 카자흐 환자의 발화를 **러시아어 인식기로 처리**(부정확) 후 번역.
- **영향**: 카자흐스탄은 **우리 1순위 타깃 시장**이라 통역 정확도 직결. KHIDI 평가(외국인환자 유치)와도 연결.
- **수정(완료, 2026-06-21)**: 화자 언어가 **브라우저가 진짜로 지원하지 않는 언어(kz)** 면 처음부터 경로 ②(서버 STT)로 강제. `useSpeechRecognition.js`에 `isBrowserSttNative(lang)` 헬퍼 추가(폴백 집합 `{kz}`), `page.jsx`가 `myLang`이 폴백 전용이면 `forceServerStt` ON + 브라우저 STT 시작 안 함. 순수 분기 로직 단위테스트 `sttRouting.test.ts`(6개)로 잠금.
- **검증**: vitest 285 통과(+6) / tsc 0. **라이브 실동작(화상방 카자흐어 발화)은 PO 동석 검증 필요**(자동 불가).

## 5. 측정에 대한 솔직한 메모

- 당초 "카자흐어 통역 품질 측정 스크립트"를 만들려 했으나, 위 진단대로 **카자흐 텍스트 번역은 Gemini가 이미 잘 처리**(측정해도 새 정보 적음). 측정이 필요한 진짜 절반 = **음성인식 정확도**인데 이는 **실제 오디오 + 라이브**가 있어야 해 자동 측정 불가(화상방 2명+ — 우리가 늘 막히는 지점).
- 따라서 무의미한 AI 퀘타 소모를 피하려 **자동 측정 스크립트는 보류**. 실측이 필요하면 PO 동석 라이브에서 ②서버 STT 카자흐 통역을 1회 녹화·검수하는 게 정확하다.

---

## 6. 2026-08-06 실측 — 「지어내기」 A/B 대조 (PO 지시로 실행)

**왜 다시 봤나**: 위 3절에서 *"이미 작동하는 서버 STT로 충분"* 이라 판정했는데, 2026-08-03 실서비스 실측에서 **그 서버 받아쓰기가 없는 말을 15~17% 지어내는 것**이 확인됐다. 판정 근거가 흔들렸으므로 두 길을 같은 소리에 대고 직접 비교했다.

**방법**: `e2e/fixtures/audio/ko-patient-speech.wav`(정답 대본 확정)에서 조각 13개를 만들었다 — 온전한 문장 2 / 잘린 파편 3 / **완전 무음 2** / **잡음만 2** / 아주 작은 소리 2 / 잡음 섞인 1 / 뒤에 무음 붙은 1.
- **A(지금 길)** = `app/api/khidi/consultation/[id]/stt/route.ts` 와 같은 모델(`gemini-flash-latest`)·같은 프롬프트(DOMAIN_PRIMING + NO_INVENTION)·`temperature 0`, 조각당 3회 = 39회
- **B(새 길)** = `gemini-3.5-live-translate-preview`, Live API 스트림, ko→ru, `output_audio_transcription`, 조각당 2회 = 26회

⚠️ **숫자를 두 층으로 갈라 읽어야 한다** — 상담방(`page.jsx`)에는 이미 **소리 크기 문턱**(`rms > 0.014`, 0.3초 이상 + 블롭 4KB 초과)이 있어 조용한 조각은 서버에 가지도 않는다. 그 문턱을 안 세고 «83%»라고 쓰면 실사용을 과장하는 것이다(초안에서 실제로 그렇게 쓸 뻔했다).

**층 1 — 실사용 조건**(문턱을 통과하는 조각 8개: A1·A2·B1·B2·B3·D2·F1·G1)

| 항목 | A (지금 길) | B (새 길) |
|---|---|---|
| 없는 말을 «무에서» 만듦 | 1/24 = 4% | **0/16 = 0%** |
| 내용 오염(숫자·시술명 바뀜) | **7/24 = 29%** | 3/16 = 19% |
| 못 알아들었을 때 행동 | **그럴듯한 말을 만든다**(B3: 3/3 「EMR 수술」) | **아무 말도 안 한다**(B3: 2/2 빈값) |
| 들은 말을 빠뜨림 | 0회 | **3회**(`위암` 누락 등) |

**층 2 — 조용한 구간**(완전 무음 2 + 잡음만 2). 대부분 문턱이 먼저 막지만, **8/03 실서비스 실측에선 15~17%가 실제로 새어나왔다** — 문턱이 충분하지 않다는 증거.

| | A | B |
|---|---|---|
| 없는 말 창작 | **10/12 = 83%** | **0/8 = 0%** |

**A가 완전 무음/잡음에서 만들어낸 문장**(전부 아무도 말한 적 없음):
`환자분의 경부 CT 결과에서는` · `거기는 이제 항암 화학요법을 해야 된다라고 이야기를 했다 그러더라고요` · `прогрессирование заболевания` · `지금 3시 한 7분 정도 되는데 한 3시 반까지 진행하겠습니다` · `안녕하세요. 한국보건산업진흥원` · `I'll do my best for you.`

**해석(단정 아님)**: A는 프롬프트에 「지어내지 마라」가 이미 두 곳 다 들어 있는데도 안 지킨다. 8/03 에 프라이밍 재료를 걷어내는 방식으로 한 번 손봤지만 비율은 안 줄었다(17%→15%, 표본 작음). **프롬프트 튜닝으로 닫히는 구멍이 아닐 가능성이 높다** — B는 프롬프트가 아예 없는 전용 통역 모델이라 지어낼 재료 자체가 없다. 다만 「모델 내부」는 확인 불가이므로 인과로 단정하지 않는다.

**B의 대가**: `위암`(рак желудка)이 3회 통째로 빠졌고, `두 달`이 `две недели`(두 주)로 2회 바뀌었다. **틀린 정보를 «넣지»는 않지만 정보를 «잃는다».** 의료 기록에서 어느 쪽이 덜 위험한지는 PO 판단 영역.

### 곁다리로 나온 진짜 고장 — 작게 말하면 자막이 통째로 안 나간다

작게 말한 문장(`E1`·`E2`, rms 0.0065~0.0069)을 **모델은 6번 다 정확히 받아썼다.** 그런데 상담방 문턱(`rms > 0.014`)이 먼저 막아 **서버에 가지도 못한다.** 반대로 순수 잡음 `D2`(rms 0.021)는 문턱을 **통과한다.** 즉 지금 문턱은 **말을 버리고 잡음을 통과시키는** 쪽으로 치우쳐 있다.
→ 이 문턱 값은 2026-07-20 실회의 로그(한국어 문장 59%가 끊김)를 근거로 «이 값이 하한»이라고 주석에 못 박혀 있다. **크기(rms) 하나로 말/잡음을 가르는 방식 자체의 한계**로 보이며, 값을 조정하는 것만으론 양쪽을 동시에 못 잡는다. 별건으로 다뤄야 한다.

**못 잰 것**: ①실제 사람 2명 통화 ②카자흐어 발화(카자흐어 하는 사람 필요) ③실사용 지연시간 — B 숫자에 내가 넣은 대기시간이 섞여 있어 지연 비교에 쓰면 안 된다.

**재현**: 조각 생성기·실행기는 세션 작업폴더(`make_clips.py` / `run_ab.py`). 문장 경계 자동감지는 문장 «안»의 짧은 쉼에서 갈라져 실패한다 — `0.95~4.85s`(문장1) / `6.25~9.45s`(문장2) 로 고정해야 한다.

---

## 7. 2026-08-06 (같은 날, 뒤) — **§6 의 판정을 뒤집는 재측정**: 자막은 «원문 받아쓰기»로 재야 한다

**계기**: PO — *"통역 기능은 아직 잘 쓰고 있지 않고 «자막» 기능을 많이 쓰고 있어."* §6 은 새 모델의 **번역문 자막**(`output_audio_transcription`)만 보고 *"들은 말을 빠뜨린다"* 고 적었다. **그건 자막 기능의 기준이 아니었다.**

**발견**: Live Translate 세션은 **원문 자막(`input_audio_transcription`)을 따로 준다** — 그리고 우리 에이전트(`agents/live-translate/src/gemini_session.py`)는 **출력쪽만 켜 놓아 이걸 안 받고 있다.** 실측 결과 원문 자막은 **설정을 안 켜도 이미 나온다**(기본 동작).

| 항목 (같은 조각 13개) | A: 지금 길 (39회) | B: 새 길의 **원문 자막** (26회) |
|---|---|---|
| 없는 말을 «무에서» 만듦 | **10/39 = 26%** | **0/26 = 0%** |
| 정확히 받아씀 | 21/39 = 54% | **20/26 = 77%** |
| 잘못 알아들음 | 6/39 = 15% | 4/26 = 15% (**비김**) |
| 아무 말도 안 함(놓침) | 0회 | 2회(가운데만 잘린 토막) |

**§6 정정**: *"새 길이 「위암」을 빠뜨린다"* → **틀렸다.** 원문 자막엔 `안녕하세요. 위암 수술을 받은 지 두 달 되었습니다.` 가 2/2 그대로 나온다. 빠진 건 **번역문**이었다(`две недели`=두 주 오류 포함). **누락은 받아쓰기가 아니라 번역 단계의 문제**다.

**그래서 자막 용도의 조합**: 새 모델에서 **원문 자막만** 받고 **번역은 지금 쓰는 텍스트 번역을 그대로** 쓴다 → 지어내기 0% + 원문 정확도 유지 + 번역 품질 그대로. **음성 통역 트랙은 안 켜도 된다.**

### 실사용 실측 — 자막이 실제로 얼마나 쓰이나 (DB, 2026-08-06 조회)

`consultation_translations` 기준. **시험 아닌 실제 회의 8건에서 2,748줄.**

| 날짜 | 줄 수 | 방향 |
|---|---|---|
| 08-04 | **414** | ru>ko, **kz>ko 112 · kz>ru 84**, ko>ru, en>* |
| 08-03 | 434 (261+173) | ru>ko 중심 |
| 07-31 | 531 | ru>ko 529 |
| 07-29 | 424 | ru>ko 316 |
| 07-27 | 551 | ru>ko 549 |
| 07-24 | 210 · 07-23 5 · 07-20 179 | ru>ko / ko>ru |

- **압도적으로 `ru → ko`** — 러시아어 발화를 한국어 자막으로 본다.
- **08-04 에 카자흐어가 처음 대량 등장(196줄 = 그 회의의 47%).** 카자흐어는 브라우저에 인식기가 없어 **반드시 서버 받아쓰기 길**로 간다 = §6·§7 에서 «지어낸다»고 잰 바로 그 길.
- ⚠️ **`ru>ko` 가 어느 길로 갔는지는 DB 로 증명 못 한다** — 두 길이 같은 테이블에 같은 모양으로 저장하고 구분 칸이 없다(`confidence` 는 양쪽 다 안 채움, 전 구간 0건). 브라우저 길일 «가능성이 높다»까지만 말할 수 있다.

---

## 8. 2026-08-06 — 실행한 것 (PO 지시 1·2번)

### 1번. 통역봇에 «원문 자막» 받기 + 「소리 끄면 싸지나」 실측

- **켰다**: `agents/live-translate/src/gemini_session.py` 에 `input_audio_transcription` 을 명시하고, 원문 자막을 같은 스트림에 **종류 표시(`kind=source`)** 를 달아 발행한다. 통역문은 `kind=translation`.
  - 프론트(`LiveTranslateBridge.jsx`)는 **원문을 화면에 안 띄운다** — 사용자는 「상대 말 → 내 언어」만 본다는 우리 자막 모델대로. 원문까지 띄우면 같은 발화가 두 줄로 겹친다.
  - 🔸 **아직 안 한 것: 원문을 상담 기록에 저장하는 경로.** 지금은 받아서 버린다. 통역봇 자체가 기본 꺼짐이라 실사용 영향 0이며, 저장을 붙일 자리는 브릿지의 `kind=source` 분기 한 곳이다.
- **소리 끄기 = 안 된다(실측).** `response_modalities=["TEXT"]` 로 바꿔도 **통역 음성이 그대로 온다**(AUDIO 492KB · TEXT 504KB — 오히려 같거나 더 많음). 즉 **자막만 받는다고 싸지지 않는다.**
  - 곁다리: TEXT 로 요청했을 때 원문 자막이 `위암` → **`이암`** 으로 틀렸다(AUDIO 모드에선 2/2 정확). 표본 1회라 단정 못 하지만 **TEXT 모드를 쓸 이유가 없다**는 근거는 된다.
  - ⚠️ **정확한 청구 토큰은 못 쟀다** — `usage_metadata` 가 메시지 단위로 오고 누적값이 아니라, 세션 총량을 이 방법으론 못 구했다. 「싸지지 않는다」는 **받은 오디오 바이트 수**로 판단한 것이다.

### 2번. 자막 한 줄에 「어느 길로 왔는지」 표시

- **DB**: `consultation_translations.stt_engine` 추가([migrations/20260806_translation_stt_engine.sql](../migrations/20260806_translation_stt_engine.sql), 적용 완료). 옛 3,310줄은 전부 `null` = 미상 — **소급 추정은 안 했다**(추측을 데이터로 만들면 안 된다).
- **값 정의·검증 단일 SoR**: `src/lib/consultation/sttEngine.ts`. 클라이언트가 보내는 값이라 **서버에서 아는 값만 통과**시킨다(모르면 저장 안 함). 시험 `sttEngine.test.ts` 로 잠금.
- **채우는 곳 3군데**: `/[id]/stt`(→`server_gemini`, 라우트가 정의상 확정) · `/translate-realtime`(클라이언트가 알려주는 값, 기본 `browser_webspeech`) · `/[id]/translate`(맞장구 사전, 기본 `backchannel_dict`).

**이제 이 한 줄로 실사용 비중을 잰다** (다음 실회의 뒤 실행):

```sql
select stt_engine, count(*) 줄수, round(100.0*count(*)/sum(count(*)) over (), 1) 비율
from consultation_translations
where created_at >= '2026-08-07' group by 1 order by 2 desc;
```

`server_gemini` 비율이 곧 **「지어내는 길에 노출된 자막의 몫」** 이다. 지금까진 이걸 셀 수가 없어 추정만 했다.

### 실검증 (2026-08-06) — 「지금 확인할 수 있는 건 다」 (PO 지시)

**① 길 표시가 실제로 저장되나 — ✅ 5/5 기대값 일치.**
로컬 서버(3241)에 실제 라우트로 태워 확인(`admin@test.com`, 비밀번호 없이 세션 발급 = `scripts/dev-login-as.mjs`). 시험용 상담 `c0c88ce4…`(is_test):

| 보낸 값 | 저장된 값 | 판정 |
|---|---|---|
| `server_gemini` | `server_gemini` | ✅ 그대로 |
| `'; drop table --` | `browser_webspeech` | ✅ **거름 동작** |
| (안 보냄) | `browser_webspeech` | ✅ 기본값 |
| 맞장구 경로, 안 보냄 | `backchannel_dict` | ✅ |
| 맞장구 경로, `whisper-v9` | `backchannel_dict` | ✅ **거름 동작** |

🔸 **곁다리 발견**: `translate-realtime` 의 출처 허용목록은 **`localhost:3000`·`3001` 만** 박혀 있다(`route.ts` 상단 `ALLOWED_ORIGINS`). 다른 포트에서 띄운 개발 서버는 실시간 번역이 **403** 으로 통째로 막힌다 — 병렬 세션이 3241·3251·3097 등을 쓰므로 **로컬에서 자막을 시험할 때마다 조용히 막힌다.** (형제 라우트 `api/translate-text` 는 아무 localhost 나 허용 — 두 라우트가 서로 다르다.) 보안 허용목록이라 **이번엔 안 건드렸다.**

**② 통역봇 실동작 — ✅ 처음으로 확인됨.** (그동안 「실 2인 통화 미검증」으로 남아 있던 항목)
로컬에서 통역 에이전트를 띄우고(`lk` 설정의 화상 열쇠 사용) `lk dispatch create` → 참가자 2명(`lang=ko` 발화자 / `lang=ru` 청취자)이 실제 방 `verify-src-caption-0806` 에 입장, 정답 대본 음성을 흘렸다.

- 통역쌍 생성: `session up: doctor-ko -> ru` ✅ (POSTMORTEMS #100 의 판정 기준)
- **원문 자막이 실제로 나온다**: `source caption <- doctor-ko` ✅
- **종류 표시(`kind`)가 실제로 붙어 온다**: 받은 줄 전부 `source`/`translation` 로 갈렸고 **표시 없는 줄 0건** → 프론트의 가르기가 실제로 동작한다.
- 내용도 맞다 — 원문 `안녕하세요. 위암 수술을 받은 지 두 달 되었습니다. 회복 기간이 얼마나 걸리는지`, 통역 `Здравствуйте. После операции по поводу рака желудка прошло два месяца. Сколько времени занимает восстановление?` (**`위암`·`두 달` 둘 다 살아 있다** — 조각 실험에서 「두 주」로 틀렸던 것이 여기선 정확)

**🔴 ② 에서 새로 나온 진짜 문제 — 입장 직후 말하면 앞부분이 통째로 유실된다**

첫 시도에서 원문이 `든지 두 달 되었습니다…` 로 시작했다 — **`안녕하세요. 위암 수술을 받은 지` 가 통째로 없다.**
- 원인 후보(통역 세션 준비 지연)를 **반증 검사**했다: *그게 원인이면, 발화 앞에 무음을 충분히 두면 앞부분이 살아나야 한다.* → 앞에 **무음 5초**를 붙여 다시 흘리니 **문장이 전부 나왔다.** 가설 통과.
- 실측 시간: 참가자 입장~`session up` 약 2초, 첫 자막까지 약 5초.
- ⚠️ **한계**: 이 시험의 참가자는 입장 **0.15초 만에** 말을 시작했다(사람은 보통 몇 초 뒤 말한다). 그러니 「실사용에서 반드시 유실된다」고는 말할 수 없다 — 정확한 서술은 **「입장 직후 곧바로 말하면 앞 몇 초가 유실된다」**. 실제 상담의 첫인사(`안녕하세요, 저는 ○○입니다`)가 여기 걸릴 수 있어 **별건으로 남긴다**.
