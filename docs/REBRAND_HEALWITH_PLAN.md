# 리브랜딩 계획 — HEALO → healwith

> 2026-06-16 작성. **서비스명 최종 결정: `healwith`** (PO 확정).
> 이 문서는 "틀"이다. 실제 전역 치환은 **PO가 ①로고 선택 ②아래 결정 3건 확인** 후 실행한다.
> (큰 변경은 계획 먼저 — CLAUDE.md 규칙)

## 0. 로고 먼저
- 후보 6종 프리뷰: ~~`/brand/logos.html`~~ — **2026-08-31 삭제**. 리브랜딩이 끝나 시안 비교가 필요 없어졌는데
  `public/` 에 있어 실서비스 주소(`healwith.co.kr/brand/logos.html`)로 그대로 열리고 있었다
  (`headers.html`·`headers2.html` 도 같이 지웠다). 필요하면 `git show HEAD~1:public/brand/logos.html` 로 꺼내라.
- 확정된 자산은 그대로 남아 있다: `public/brand/wordmark*.svg` 4종
- PO가 번호 고르면 → favicon / 헤더 로고 / OG 이미지 / 앱 아이콘 일괄 제작 후 적용.

## 1. 범위 (healo 등장 = 484파일·2045곳, 대소문자 무시)

디렉토리별 분포:
| 위치 | 건수 | 분류 |
|---|---|---|
| `app/` | 816 | 🟢 제품 UI (치환 대상) |
| `src/` | 684 | 🟡 일부 UI + 일부 코드 식별자 (선별) |
| `components/` | 39 | 🟢 제품 UI |
| `docs/` | 296 | ⚪ 내부 히스토리 (선택) |
| `coo/` `scripts/` `e2e/` `migrations/` | ~110 | ⚪ 내부/테스트 (선별·대부분 보류) |
| `public/` | 6 | 🟢 메타/매니페스트 |

## 2. 🟢 치환한다 (사용자에게 보이는 브랜드 텍스트)
- 화면 카피·헤더·푸터·타이틀, **i18n 6개 언어 문자열**, 이메일 템플릿(`src/emails`), 메타데이터/OG/SEO, manifest, alt 텍스트.
- 표기 규칙(확정 필요 → §4): 기본 `HEALO`/`Healo`/`healo` → **`healwith`** (브랜드는 소문자 표기).

## 3. 🔴 건드리지 않는다 (자동 치환에서 제외 — 깨짐/법적)
- **`healo-khidi`** (61곳): Vercel 프로젝트명·저장소·배포 URL·repo 슬러그 = **인프라**. 도메인/프로젝트 마이그레이션은 별건(아래 §4 결정).
- **`@healo.local`** 류 가짜 계정 이메일: 이미 발급된 의사·코디 계정 로그인 ID. 바꾸면 기존 로그인 깨짐 → **유지**(신규만 새 도메인).
- **DB 실데이터**: 슬러그·식별자 등.
- **KHIDI 공식 제출 문서 / 제안서**(`docs/KHIDI_*`, `_proposal/`): 과거 제출명이 'HEALO'면 **기록 보존**(임의 개명 금지, 필요 시 "舊 HEALO" 병기).

## 4. ⚠️ PO 확인 필요 (실행 전 3가지)
1. **표기/대소문자**: 로고대로 전부 소문자 `healwith`? (제목·문장 첫머리도 소문자 유지? 아니면 문장에선 `Healwith`?)
2. **도메인/인프라**: `healwith.com`(또는 `.co`/`.kr`) 신규 도메인 등록할지 → Vercel 도메인 연결·Madrid 상표출원 별도 트랙. (현 `healo-khidi.vercel.app`은 당분간 유지 가능)
3. **docs 히스토리**: 내부 핸드오프 문서(296곳)도 일괄 변경할지, 히스토리라 그대로 둘지.

## 5. 실행 방식 (승인 후)
- 🟢 영역만 대상으로 **스코프 지정 치환 스크립트** + 사전 dry-run diff를 PO에게 보여주고 실행.
- 제외 패턴: `healo-khidi`, `@healo.local`, `*.vercel.app`, import 경로·식별자.
- 빌드(`npx next build --webpack`) + i18n 6개 언어 스폿체크 + 프리뷰 배포 후 머지.
- 작업 로그 1줄 → `docs/KHIDI_중간보고_베이스.md` §4.
