# 세션 작업 배분판 — 주제별 첫 프롬프트 (2026-07-29 작성)

> **PO가 새 창을 열고 여기서 프롬프트를 복사해 붙여넣으면 그 세션이 그 주제만 맡는다.**
> 영역(만지는 파일)이 겹치지 않게 갈랐다 — 2026-07-27~28 에 「내 커밋이 남의 브랜치로」 사고가 **두 번** 났고
> 같은 영역 3중복도 있었다(`docs/PARALLEL_SESSIONS.md` 참조).
>
> ⚠️ **이 문서는 「그날의 판」이라 빨리 낡는다.** 작업이 끝나면 그 줄을 지우고, 판이 바뀌면 새로 짜라.
> 단일 SoR(맥락·왜)은 여전히 `docs/PROJECT_CONTEXT.md` 다 — 이 문서는 「누가 뭘 맡나」만 담는다.

---

## 영역 배분표 — 한눈에

| # | 주제 | 급한가 | 만지는 곳(영역) | 브라우저 필요 |
|---|---|---|---|---|
| 1 | 스토어 앱 화면 수리 본판 합류 (#1127) | 🔴 | `src/index.css` · `app/styles/healo-tokens.css` · 모바일 UI · `src/lib/notifications` | ✕ |
| 2 | 앱 등록 마무리 (애플·구글 콘솔) | 🔴 | Play Console · App Store Connect · `docs/APP_STORE_LISTING.md` | **○ 필수** |
| 2-A | 🍎 **애플만** — 「심사에 추가」 제출까지 (**5관문**, 아이패드 스크린샷이 진짜 구멍) | 🔴 | App Store Connect 화면 + 스크린샷 업로드 | **○ 필수** |
| 3 | 광고 착지 페이지 합류 (#1128) | 🔴 | `app/ru/**` · `app/kk/**` | ✕ |
| 4 | 측정(GA4)이 진짜 도는지 실증 + 광고 꼬리표 | 🔴 | `docs/GA4_SETUP.md` · `src/lib/ga.ts` | **○ 필수** |
| 5 | 배포 창구 — **전제 정정됨(창구는 돌았다)** + 잔재 6개 박제·삭제 | 🟠 | `.github/workflows/**` | ✕ |
| ~~6~~ | ~~방치된 문서 신청서 3건 합치기~~ ✅ **2026-07-29 완료** (#1126·#1129 합침 / #1111 은 #1145 로 대체 후 닫음) | — | — | — |
| 7 | 국내병원(hospital) 자동 확인 구멍 마무리 | 🟡 | `e2e/**` | ✕ |
| 8 | KHIDI 8/27 중간평가 준비 | 🟡 | `docs/KHIDI_*` · `app/admin/khidi/**` | ✕ |

**🚫 지금 건드리면 안 되는 것**: `claude/cloud-hospital-business-model-ft66ln`(B2B 병원 템플릿 #1132) — **다른 세션이 지금 활발히 작업 중**이다.

---

## 1. 스토어 앱 화면 수리 본판 합류 (#1127) 🔴

> 먼저 `docs/PROJECT_CONTEXT.md` 최상단 읽어. **[#1127](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1127) 을 본판에 합치는 게 이 세션의 일**이다 — 60개 파일·38커밋이 **10시간 넘게 서랍에 갇혀 있다.** 내용은 스토어 앱 실기기 버그 수리(키보드 뜨면 로그인 불가 · 투명한데 눌리는 삭제 버튼 6곳 · 44px 터치타겟이 아이콘을 밀어내던 부류 · 하단 안전영역 7곳)와 폰 알림 배선이다.
> **왜 급한가**: 구글 비공개 테스트 14일 동안 **앱 파일을 2번 이상 업데이트해야 프로덕션 승인률이 올라간다**(어제 조사분). 올릴 내용이 바로 이거고, 지금은 아무 데도 안 나가 있다.
> **할 일**: ①`git worktree add` 로 **딴 폴더에 격리**해서 그 작업본을 열어라(현재 브랜치 바꿔치기 금지) ②본판 최신을 끌어와 충돌 해소 — **`git checkout origin/main -- .gitattributes` 를 먼저** 하고 합쳐라(핸드오프 문서가 `merge=union` 이라 그래야 안 터진다) ③**반성문 번호가 겹치는지 반드시 확인**(`git show origin/main:docs/POSTMORTEMS.md` 로 최대 번호 확인 — 이 브랜치는 `#145` 를 쓰는데 본판도 `#145` 를 쓰고 있다. 겹치면 내 쪽을 최대+1 로 옮기고 참조 전부 갱신) ④`node scripts/check-rules.mjs`·`node scripts/check-content-consistency.mjs` 통과 확인 ⑤자동 검사 초록이면 합쳐라.
> 🚫 **함정**: 전역 CSS(`src/index.css`·`app/styles/healo-tokens.css`)를 만지므로 **다른 세션과 동시에 화면 작업하지 마라.** · 문서 자동 병합(union) 은 **같은 줄을 두 번 남긴다** — 합친 뒤 `docs/PO_PREFERENCES.md`·`docs/PROJECT_CONTEXT.md` 에 중복 줄이 생겼는지 확인해라.
> **끝나면**: 합친 것·못 합친 것을 `PROJECT_CONTEXT.md` 최상단에 중간 저장.

---

## 2. 앱 등록 마무리 (애플·구글 콘솔) 🔴 — **브라우저 있는 로컬 세션만**

> 먼저 `docs/PROJECT_CONTEXT.md` 최상단 「앱스토어 4차」 블록을 읽어. **앱 등록의 남은 관문은 전부 콘솔 클릭이라 브라우저 없는 세션은 못 한다.**
> **🍎 애플**: ①App Store Connect → 사용자 및 액세스에서 **직원 아셀(`binmin96@icloud.com`)이 초대를 수락했는지** 확인 ②수락했으면 TestFlight **내부 그룹**에 추가(내부 테스터는 베타 심사와 무관하게 즉시 설치된다) ③아셀에게 아이폰 설치·실행 요청 → 열리면 `/api/push/test` 로 푸시 수신 확인 ④**심사용 데모 계정**을 만들어 앱 정보에 입력(로그인 필요 앱이라 없으면 애플이 못 들어가서 반려. 비밀번호는 저장소에 적지 마라) ⑤개인정보 라벨 「게시」 ⑥**「심사에 추가」 제출**(버튼 이미 활성).
> **🤖 구글**: ①게시 개요에서 **비공개 테스트 검토가 승인됐는지**(「검토 중인 변경사항」이 사라졌는지) ②승인됐으면 **테스터 12명 채우기**(PO 결정 필요 — 크몽 규랩스 5,000원/16명 추천, 비교표는 「앱스토어 3차」 블록) → **여기서 14일 시계 시작** ③**스토어 등록정보**(마지막 1/11) — 문구는 `docs/APP_STORE_LISTING.md` §1·§3 그대로, 피처 그래픽은 `node scripts/play-feature-graphic.mjs ko|en|ru` 로 만든다(PO 승인 후 업로드).
> 🚫 **함정**: **비공개 테스트 주소는 `/app/<앱ID>/closed-testing`** — `tracks/` 를 붙이면 「예기치 않은 오류」가 나는데 **구글 장애가 아니라 없는 주소다** · **Play Console 은 URL 을 손으로 찍으면 튕긴다**(메뉴 클릭으로만 이동) · **크롬 프로필은 `u/2`**(`u/0`·`u/1` 은 다른 계정) · **PO 계정은 안드로이드폰이라 iOS 검증 불가**(아이폰은 아셀) · **내부 테스트는 12명 카운트에 안 들어간다**(비공개 테스트만) · 출시 노트 **자리표시자를 그대로 두면 그 문구가 발행된다**.
> 🚫 **앱 화면 버그는 이 세션 소관 아님** — 1번 세션이 한다. 중복 착수 금지.

---

## 2-A. 🍎 애플 — 「심사에 추가」 제출까지 끝내기 🔴 — **크롬 있는 로컬 창만** (2026-07-29 갱신)

> 📌 **관문이 5개로 늘었다** — 오늘 다른 세션이 **제출을 막던 진짜 원인**을 찾았다: **아이패드 13형 스크린샷 누락**. 그게 없으면 애플이 「심사에 추가」를 아예 안 열어준다.
>
> ⚠️ **겹침 주의**: `work/appstore-tablet-shots` 세션이 **스크린샷 만드는 도구(코드)** 를 담당한다. **그 스크립트를 고치지 마라.** 이 세션은 **만들어서 올리고 제출하는 것**만 한다.
>
> **① 아셀 초대 수락 확인 — 이게 나머지를 다 막고 있다**
> App Store Connect → 「사용자 및 액세스」에서 아셀(`binmin96@icloud.com`) 상태 확인. 「대기 중/초대됨」이면 **재발송 누르고 PO 에게 「아셀에게 메일 확인 요청」 한 줄만** 남긴 뒤 ②로 넘어가라(여기서 세션을 끝내지 마라 — ②④⑤는 아셀과 무관하게 진행된다).
> 수락돼 있으면 → TestFlight → **내부 그룹에 추가**. 내부 테스터는 베타 심사를 안 거치고 즉시 설치된다.
>
> **② 아이패드 13형 스크린샷 — 제출을 막던 진짜 구멍**
> 규격 `2064×2752`. 만드는 도구는 **아직 본판에 없고 `work/appstore-tablet-shots` 작업본에만 있다** — 거기서 가져와 돌려라:
> ```
> node scripts/appstore-screenshots.mjs ko,en,ru
> ```
> 산출물은 `appstore-assets/screenshots/ios-ipad-13/`. **영어판 4장을 App Store Connect 에 업로드**하면 「심사에 추가」가 열린다. ⚠️ 브라우저 실행 파일을 못 찾으면 `PLAYWRIGHT_CHROMIUM_PATH` 로 경로를 주면 된다(`docs/APP_STORE_LISTING.md` §0).
>
> **③ 아이폰 실기기 확인** (①이 풀린 뒤에만)
> 아셀에게 「TestFlight 로 healwith 설치·실행하고 화면 찍어달라」 요청(러시아어/영어 문구를 만들어 PO 에게 전달). **앱이 열리는 걸 봐야 「iOS 됨」이라 쓸 수 있다.** 열리면 `/api/push/test` 로 푸시 수신까지.
>
> **④ 심사용 데모 계정 — 새로 만들지 마라**
> ✅ **`patient@test.com` 을 그대로 쓴다.** 2026-07-29 실측: 살아 있고 **05:12 KST 로그인 성공**, 정지·삭제 없음. **구글 Play 에 이미 같은 계정을 데모로 등록**해뒀다. `@test.com` 이라 KHIDI 집계에서 자동 제외된다. **아이디만 넣고 비밀번호는 저장소·문서 어디에도 적지 마라.**
>
> **⑤ 마지막 두 칸 + 제출**
> 개인정보 라벨 **「게시」**(작성은 끝났는데 게시를 안 눌렀다) → 가격(무료)·배포 국가 확인 → **「심사에 추가」 제출.**
>
> **PO 에게 물을 것 딱 1개(버튼으로, 제출 직전에)**: 지금 **「자동으로 버전 출시」** — 심사 통과 순간 앱스토어에 바로 공개된다. 시점을 고르려면 「수동으로 버전 출시」로 바꿔야 한다. **PO 판단이라 마음대로 바꾸지 마라.**
>
> 🚫 **함정**
> - **PO 계정(`bonroi2296@gmail.com`)은 안드로이드폰이라 iOS 검증 원천 불가.** 아이폰은 아셀 것뿐이다(2026-07-28 에 이걸 착각해 PO 계정을 내부 테스터에 넣었다 뺐다).
> - **애플 개인 계정이라 판매자명에 PO 실명이 노출**되고, **가이드라인 5.1.1**(의료성 앱에 법인 계정 요구)로 반려될 수 있다. 반려되면 항소 논지는 **「우리는 진단·처방을 하지 않는 중개」**.
> - **웹뷰 반려(4.2) 대비 심사 노트는 이미 써 있다.** 지우거나 다시 쓰지 마라.
> - **수출 규정·EU 거래자·한국 전자상거래 정보는 전부 끝났다**(EU 는 2026-07-27 애플 승인). 다시 건드리지 마라.
>
> **끝나면**: **①~⑤ 중 어디까지 갔는지 번호로** `PROJECT_CONTEXT.md` 최상단에 중간 저장. 「됐어요」로 뭉개지 마라.

---

## 3. 광고 착지 페이지 합류 (#1128) 🔴

> 먼저 `docs/PROJECT_CONTEXT.md` 최상단 읽어. **[#1128](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1128) 을 본판에 합치는 게 이 세션의 일**이다 — 러시아어·카자흐어 광고 착지 페이지에 **왓츠앱 바로가기**를 붙이고 구글 의료광고 정책에 걸리는 **세포치료 직링크를 걷어낸** 변경인데, 15시간째 안 합쳐져 있다.
> **왜 급한가**: **광고가 이미 돌고 있다.** 카자흐스탄은 왓츠앱을 인구의 83%가 쓰는데 지금 착지 페이지는 웹폼만 내민다. 그리고 세포치료 링크는 **광고 심사 반려 사유**다.
> **할 일**: ①격리 폴더에서 `feat/ru-ads-landing` 을 열어 본판 최신을 끌어와 충돌 해소 ②자동 검사 초록 확인 후 합침 ③합친 뒤 **PO 에게 물어야 할 판단거리 2개**를 버튼으로 정리 — (ㄱ)`НК-клеточная Терапия`(NK세포치료)가 암종 상세에 러시아어로 노출되는데 구글이 제한하는 표현이다, 지울지 말지는 PO·병원 판단 (ㄴ)착지 페이지의 「24시간 내 답변」 약속을 1인 운영에서 지킬 수 있는지(위탁계약서는 5영업일).
> 🚫 **함정**: 광고 성과는 **GA 가 아니라 우리 DB(실제 문의 건수)** 로 봐라 — 쿠키 동의를 안 누른 사람은 GA 에 0으로 잡힌다. `[RU-ADS]`·`[KZ-ADS]` 표식이 그래서 들어간 것이다.
> 🚫 **측정(GA4) 쪽은 4번 세션 소관** — 건드리지 마라.

---

## 4. 측정(GA4)이 진짜 도는지 실증 + 광고 꼬리표 🔴 — **브라우저 필요**

> 먼저 `docs/KNOWN_ISSUES.md` 의 GA4 항목들을 읽어. **어제 GA4 를 대대적으로 고쳤는데(수집 요청이 보안 헤더에 막히던 구멍 · AI 상담 2배 집계 · 옛 주소 광고 출처 증발 7곳) 「진짜 신호가 나가는지」는 아무도 눈으로 못 봤다.**
> 🚧 **0순위 — 2026-07-30 확정 사실부터 읽어라**: PO PC 에는 **AdGuard**(윈도 프로그램)가 깔려 있어
> ①gtag.js 를 **500 으로 바꿔치고**(응답 본문에 `/* Blocked by AdGuard */`) ②`google-analytics.com`·
> **`analytics.google.com`(= GA4 콘솔 화면)** 을 DNS 에서 `0.0.0.0` 으로 막는다. **시크릿창으로는 안 걸러진다.**
> → **검증은 차단기 없는 기기·회선에서** 하거나 AdGuard 예외를 먼저 넣어라. 모르고 덤비면 멀쩡한 코드를
> 반나절 뒤진다(실제로 그랬다). 상세 = `docs/GA4_SETUP.md` §2-1 · 반성문 #161 속편.
> **1순위(이것부터)**: `https://healwith.co.kr/?ga_debug=1` 로 열고 쿠키 **「모두 허용」**(실제 버튼 글자) → GA4 관리 → **DebugView**. 몇 초 안에 `page_view`·`inquiry_*` 가 뜨면 확정. **이게 유일한 실증 통로다** — 뜰 때까지 「검증됨」이라고 쓰지 마라. ⚠️ 어제 수정분은 **오늘 오후 3시 배포 창구가 나간 뒤**에 확인해야 한다(그 전엔 옛날 코드가 떠 있다).
> **2순위**: 광고 링크에 **꼬리표(UTM)** 가 붙어 있는지 확인. 없으면 GA4 가 「직접 방문」으로 뭉뚱그려 **어느 광고가 문의를 만들었는지 영영 못 본다**(지난 데이터는 소급 불가). 형식: `?utm_source=yandex&utm_medium=cpc&utm_campaign=ru_lung_2608`. **이름을 한 번 정하면 절대 바꾸지 마라**(`yandex`/`Yandex` 를 섞으면 다른 출처 2개로 센다).
> **3순위**: GA4 콘솔에서 **맞춤 측정기준(custom dimension) `platform`·`lang` 을 이벤트 범위로 1회 등록** — 안 하면 보고서에 안 뜬다. 이게 있어야 「앱에서 몇 명이 문의했나」에 답할 수 있다.
> **PO 결정 2건을 버튼으로 정리**: ①동의 모드(Consent Mode v2) 켤지 — 비교표는 `docs/GA4_SETUP.md` §5 ②얀덱스 메트리카 — 코드·보안 헤더는 이미 준비 끝이라 **카운터 번호만 받으면 1분**이다. 러시아에선 구글보다 얀덱스가 크다.
> 🚫 **함정**: 🛑 **`cancer_type`(암종) 같은 건강 값으로 「잠재고객」을 만들지 마라** — 구글·얀덱스 정책 위반이고 **계정 정지**로 이어진다. 전환 측정은 괜찮고 리타게팅만 안 된다. · 이벤트에 심어둔 해석은 **가설이지 사실이 아니다**(예: 「AI 상담 2마디에서 멈춤」은 실패일 수도 **한 번에 답을 얻은 것**일 수도 있다). 숫자만 보고 고치지 마라.

---

## 5. 배포 창구 — 🔴 **큰 전제가 틀렸다. 착수 전에 이 칸을 먼저 읽어라** (2026-07-29 실측)

### 📌 정정 — 「창구가 하루도 못 돌았다」는 **틀렸다**

2026-07-28 핸드오프는 *「3시 배포 창구가 구조적으로 절대 못 도는 상태」*, *「2026-07-28 프로덕션 배포 0건」* 이라고 적었다. **실행 이력을 직접 조회해보니 아니다.**

| 실측 (GitHub Actions `daily-deploy.yml` 실행 이력 5건) | 결과 |
|---|---|
| 2026-07-28 **08:43 UTC (= 17:43 KST) · 예약 실행** | ✅ **성공** — 이게 `production` 브랜치를 밀어 실서비스 배포를 냈다 |
| 07-28 06:24 UTC · 손으로 실행 | ✅ 성공 |
| 07-28 06:18 / 05:39 UTC · 손으로 실행 | ❌ 실패 (옆문 수리 전) |
| 07-28 05:32 UTC · 손으로 실행 | ✅ 성공 |

`production` 브랜치 끝 = `2026-07-28 17:42` 커밋([#1118](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1118))으로 **예약 실행 성공 시각과 맞는다.**

- **진짜 증상은 「못 돈다」가 아니라 「늦게 돈다」다.** 예약은 `cron: "0 6 * * *"`(= KST 15:00)인데 실제 발화는 **17:43 KST — 2시간 43분 지연**. GitHub 예약 실행은 부하가 몰리면 밀리는 게 알려진 동작이라, **「안 돌았다」로 오판하기 쉽다.**
- ⚠️ **그래서 반성문(「만든 자동화를 한 번도 안 돌려봤다」)은 쓰지 마라 — 전제가 틀렸다.** 쓸 게 있다면 **「중간 신호(워크플로 실행 이력)를 안 보고 결론냈다」** 쪽이다.

### 잔재 브랜치 6개 — **전수 대조 끝, 전부 잔재로 판정** (2026-07-29)

`fix/deploy-side-door` · `fix/deploy-side-door2` · `fix/deploy-window-not-merge-window` · `fix/deploy-window-skipcheck` · `work/fix-deploy-window` · `fix/prod-build-lock`

- **판정 근거(추측 아님)**: 각 브랜치의 의미 있는 커밋이 본판의 어느 합치기로 들어갔는지 하나씩 대조 — 옆문([#1107](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1107)) · 머지 자유화+창구([#1099](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1099)) · `--grep` 수리([#1100](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1100)) · 얕은 복제 수리([#1110](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1110)) · 벤더 API 실값([#1094](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1094)) · 규칙 다이어트([#1109](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1109)) · 말투 훅. `batch-merge.yml` **본판에서 삭제된 것 확인**.
- **자동 저장 커밋 7개도 개별 확인** — `session-orient.sh` 에 넣은 13줄은 **한 줄씩 대조해 본판에 전부 있음**, `PROJECT_CONTEXT.md` 61줄짜리 2건은 **`docs/archive/PROJECT_CONTEXT_handoffs.md` 에 보존됨**(핸드오프 4개 초과분이 창고로 밀린 것이라 본문에서 안 보였던 것뿐). 스크립트·워크플로 변경은 본판 쪽이 더 최신.
- ⬜ **남은 일 = 박제 후 삭제.** 로컬에 `attic/2026-07-29/<브랜치명>` 태그 6개를 만들었으나 **원격 푸시가 403 으로 막혔다**(태그 푸시 권한이 없는 세션이었다 — 작업본 푸시는 되는데 태그만 막힌다). **태그를 밀 수 있는 세션이나 PO 가 마무리해라.** 내용은 위처럼 전부 본판에 있으니 **박제 없이 지워도 유실은 없지만**, 관례(박제 먼저)를 반쪽으로 깨지 않으려고 삭제하지 않았다.

### 그래서 이 세션이 실제로 할 일

1. **오늘(2026-07-29) 예약 실행이 도는지 지켜봐라** — 예정 15:00 KST, 어제 기준 **최대 3시간까지 밀릴 수 있다.** 「15시에 안 떴다」로 성급히 고장 판정하지 마라.
2. **돌고 난 뒤 Vercel 에서 실서비스 최신 반영본 시각을 직접 확인**해라. 워크플로 초록 ≠ 배포됨.
3. **잔재 6개 박제+삭제** (위 ⬜).

## ~~6. 방치된 문서 신청서 3건 합치기~~ ✅ 완료 (2026-07-29)

- **[#1126](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1126)** 공공 포털 등재 — 합침. ➡️ **PO 몫 남음**: 문서에 든 메일 초안 2건을 한국관광공사 의료관광안내센터(`medicaltourism@knto.or.kr` · ☎ 02-752-2102~3)에 보낼지 결정.
- **[#1129](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1129)** CloudHospital 검토 — 충돌 풀고 합침.
- **[#1111](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1111)** SEO 취향 3건 — 그대로 합치면 규칙 다이어트가 되돌아가서 **[#1145](https://github.com/bonroi2296-tech/HEALO_KHIDI/pull/1145) 로 다시 짜서 합치고 원본은 닫았다.** 새로운 것 1건은 **말투 훅 8번**으로 승격, 재탕 2건은 원문만 보관(삭제 0건).

---

## 7. 국내병원(hospital) 자동 확인 구멍 마무리 🟡

> `docs/KNOWN_ISSUES.md` 의 「`hospital`(국내병원) 역할만 자동 확인 경로가 아예 없다」 항목을 읽어. 배선은 어제 끝났고(`e2e/auth.setup.ts` 에 역할 추가 · `e2e/hospital-portal.spec.ts` 신설 · `.env.example` 에 키 12개 등재) **두 가지가 남았다.**
> **① PO 손**: `E2E_HOSPITAL_EMAIL`·`E2E_HOSPITAL_PASSWORD` 를 GitHub Actions 비밀값에 등록. **등록 전까지 이 검사는 조용히 건너뛴다**(= 검사가 없는 것과 같다). 이걸 PO 에게 먼저 요청해라.
> **② 원인 미규명**: 「`hospital@test.com` 으로 로그인하면 첫 화면으로 되돌아간다」는 증상의 **원인을 아직 못 찾았다.** 한때 `hospitals.is_active=false` 를 의심했는데 **문지기(`checkHospitalAuth`)는 그 값을 안 본다** — 반증 검사에서 걸러졌다. 문지기가 보는 `hospital_users.is_active` 는 두 계정 다 `true` 다. **재현부터 해라.**
> 🚫 **함정**: 「X 가 원인이다」라고 말하기 **전에** *X 가 진짜 원인이면 무엇이 참이어야 하는가* 를 한 줄 적고 그걸 먼저 확인해라(`CLAUDE.md` 실측 5번). 어제 이 항목에서 정확히 그 검사가 오답을 걸러냈다.

---

## 8. KHIDI 8/27 중간평가 준비 🟡

> `docs/rules/KHIDI.md` 와 `docs/KHIDI_중간보고_베이스.md` 를 읽어. **8/27 중간평가에서 70점 이상이어야 잔금(30%)을 받는다.** 남은 기간이 한 달이 안 된다.
> **할 일**: ①**정량지표 2개가 실제로 자동 집계되는지 실측** — 외국인환자 유치 **12건** / 사전상담·사후관리 **120건**. `/admin/khidi/conversion` 이 그걸 센다 = **그 화면이 곧 점수다.** 지금 숫자가 몇인지 DB 로 직접 확인하고, 0 이면 **「0 이 정상인 경우」를 먼저 배제**해라(어제 「사전상담 0건」은 실제로 「0 이 정상」 판정이 났다). ②**만족도 90점** — 파이프는 뚫렸는데 **실환자 표본이 없다**. 표본을 어떻게 채울지 PO 와 정해야 한다. ③**접근성 32화면 「0건」 증거를 최신 날짜로 갱신** — 저장소에 커밋된 증거는 2026-07-28 CI 산출물이고 그 뒤 화면이 여러 번 바뀌었다. 제출 전이라면 `Audit (live)` 워크플로를 한 번 더 돌리고 결과 파일도 같이 커밋해라.
> 🚫 **함정**: **8월에 급조하지 마라** — 지표는 「그날 만드는 것」이 아니라 「그때까지 쌓인 것」이다. 지금 숫자가 부족하면 **부족하다고 지금 보고**해야 PO 가 손을 쓴다. · 돈 숫자·실적 숫자를 단정하기 전에 **그 숫자를 가장 정확하게 아는 화면**을 열어라(요약 화면의 합계로 결론내지 마라).
