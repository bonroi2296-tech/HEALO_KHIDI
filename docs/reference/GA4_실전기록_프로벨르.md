# GA4 붙이기 실전 기록 — 프로벨르 자사몰 (2026-07-29)

> 📌 **이건 «남의 사업 기록»이 아니라 «우리가 나중에 쓸 참고자료»다.**
> PO 방침(2026-08-03): *«프로벨르는 이번 프로젝트랑 상관 없어. **GA4 만** 우리꺼 광고 하기 전에
> 연습 삼아서 하고 있던거지»* — 즉 **프로벨르 = 우리 GA4 의 예행연습**이었다.
> 그러니 **버리지 말고, 우리 서비스에 GA4·광고를 붙일 때 이 문서를 먼저 펴라.**
>
> **여기서 그대로 재사용되는 것** (프로벨르에서 실제로 데어본 것들):
> - **이중 집계** — 플랫폼 자체 연동과 태그관리자를 둘 다 켜면 같은 주문이 두 번 잡혀 매출이 2배로 뜬다
> - **작업공간 게시 함정** — 기본 작업공간에 남의 미게시 변경이 남아 있으면 게시할 때 같이 나간다
> - **「실패한 클릭」도 잡힌다** — 옵션 안 고르고 버튼 누른 것까지 장바구니로 집계됐다(실측)
> - **내부 트래픽 제외** — 사무실 공인 IP 로 우리 접속 빼기 (`curl https://api.ipify.org` · `ipconfig` 는 내부 주소라 못 씀)
> - **데이터 보존 기본값 2개월** — 14개월로 올려야 하고 **소급이 안 된다**
> - **필터·테스트 이벤트는 소급 삭제 불가** — 테스트한 날 숫자는 영영 조금 부풀어 있다
> - **계정 헷갈림** — 소유 계정이 아니면 「아무것도 없다」로 오진한다
> - **봇 비율** — 프로벨르 방문자의 53% 가 봇이었다. 우리 숫자도 그대로 믿지 마라
>
> 🔑 **계정 관계 바로잡기(2026-08-03 PO 확인)**: 힐위드 GA4 가 프로벨르 «밑에» 있는 게 아니다.
> **같은 구글 계정(bonroi2296)으로 둘을 각각 만들어 둔 것**뿐이고 서로 별개 속성이다.
> 로그인 계정이 같아서 화면에 나란히 보일 뿐 — 코드 주석이 「(Probelle 계정)」이라 적혀 있어
> 오해를 부르던 것을 같이 고쳤다(`src/lib/ga.ts`).
>
> 우리 쪽 실제 설정 절차는 [`docs/GA4_SETUP.md`](../GA4_SETUP.md).
> 아래는 프로벨르에서 실제로 한 작업의 원문 기록이다(원래 `docs/PROJECT_CONTEXT.md` 안에 있던 것을
> 2026-08-03 에 여기로 옮겼다 — **우리 프로젝트 「진행상황」이 아니라 「참고자료」라서**).

---

## 🔻 세션 종료 핸드오프 — 2026-07-29 [프로벨르·별건] 자사몰 GA4 연동 완료 — 저장소 코드 변경 0

> ⚠️ **이 세션은 healwith 코드를 한 줄도 안 건드렸다.** 대상은 본로이의 다른 브랜드 **프로벨르 자사몰(probelle.co.kr, 카페24)**. 여기 적는 이유는 「왜 그렇게 했는지」가 코드·깃에 안 남기 때문. 상세는 어시 기억 `probelle-ga4-state`.
> 📌 2026-07-28 자 「[Probelle 쇼핑몰] 측정 상태 진단만 (작업 착수 전 보류)」 핸드오프의 **후속·완료편**이다.

**1. 이번 세션 한 일**

| 무엇 | 결과 |
|---|---|
| **원인 규명** | GA4 속성(`G-SRCG9RGWJG`)도 태그관리자 컨테이너(`GTM-5NV3J8PK`)도 이미 있었는데 **컨테이너 안 태그 16개 중 GA4 태그가 0개**라 7개월간 수집 0. 전부 대행사(랩빗)가 넣은 메타(페북) 픽셀이었다. |
| **연동 (게시 3회)** | 버전 4 = Google 기본 태그 + 전자상거래 4종(상품조회·장바구니·주문서·구매완료) + 맞춤JS 변수 4개 / 버전 5 = 장바구니 「실패한 클릭」 차단 예외 / 버전 6 = 구매완료 상품정보를 주문완료 화면에서 직접 읽도록 개선 |
| **실주문 검증** | 무통장입금으로 테스트 주문 1건(`20260729-0000017`) → `purchase` 79,300원·주문번호·상품까지 실측 확인 후 **카페24에서 주문 취소**(입금 안 함 → 지출 0원) |
| **내부 트래픽 제외** | 사무실 공인 IP `218.153.240.144` → 규칙 「본로이 사무실」 + 데이터 필터 `Internal Traffic` 을 **테스트 → 활성** |
| **서치콘솔 연결** | `probelle.co.kr` ↔ 스트림 「프로벨르 자사몰」 링크 생성 |
| **마케터용 매뉴얼** | 9쪽 워드·PDF(실제 화면 캡처 5장 + 설명 그림 3장). 바탕화면 `프로벨르_GA4_매뉴얼` 폴더 |
| 저장소 | **합치기 신청서(PR) 0건 · 코드 변경 0건** (문서 2개만) |

**2. 왜 그렇게 했는지**

- **카페24 자체 구글 애널리틱스 연동은 일부러 안 켰다** — 태그관리자로 이미 붙였는데 카페24 쪽까지 켜면 **같은 주문이 두 번 잡혀 매출이 2배로 부풀려진다**. 실측으로 전송이 이벤트당 1회뿐임을 확인해 이중 집계 없음을 확정했다.
- **새 작업공간을 따로 판 이유** — 기본 작업공간(Default Workspace)에 **대행사 계정(`datanugget@weirdsector.co.kr`)의 5개월 묵은 미게시 변경 9건**이 남아 있었다. 거기서 게시하면 의도 모를 그 9건이 같이 실서비스로 나간다.
- **홈페이지(스킨) 코드를 안 건드린 이유** — 대행사 메타 픽셀이 쓰던 변수·발동조건(상품번호·가격·장바구니 목록·주문번호)이 컨테이너에 이미 다 있어 **그대로 재사용**하면 됐다.
- **테스트 주문을 무통장입금으로 한 이유** — 카드 결제는 실제로 돈이 나가고 환불에 며칠 걸린다. 무통장입금은 주문만 생기고 **입금을 안 하면 지출 0원**이며, 주문완료 화면까지 도달해 `purchase` 를 실측할 수 있다.
- **마케터 접근을 「별도 계정 초대」가 아니라 「`bonroi2296` 계정 공유」로 간 것은 PO 결정** — 어시가 「메일함·드라이브까지 열린다 · 변경 기록이 안 남는다」로 두 번 말렸으나 PO 재확인(*"코딱지만한 회사에서 그렇게 구분해야해?"*). **다시 권하지 마라.** 대신 매뉴얼 0장을 「초대 메일 수락」 → 「전달받은 구글 계정으로 로그인」으로 고치고, 「관리」 메뉴 주의 문구를 넣었다.

**3. 안 끝났거나 보류**

- ⏸ **네이버페이·카카오페이 결제가 주문완료 화면을 거치는지 미확인** — 안 거치면 GA4 매출이 실제보다 **적게** 잡힌다. 대행사 메타 픽셀은 이 경우 「결제 버튼 클릭」 시점을 구매로 치는데 그건 반대로 **과대집계**다. 풀리려면: 네이버페이 실주문 1건이 들어와야 한다.
- ⏸ **검색어 표 · 회원가입 완료 집계** — 만들 수 있으나 **마케터 회신 대기**. 미리 만들면 안 쓰는 것만 늘어난다(매뉴얼 마지막 장에 마케터가 그대로 복사해 요청할 문장을 넣어뒀다).
- ⏸ **마케터에게 계정 정보 전달** — 계정 정보는 어시가 다루지 않는다. **PO 손 필요.**

**4. 주의·함정**

- 🧨 **태그관리자는 「게시」하면 그 작업공간을 없앤다.** 추가 작업할 때마다 새 작업공간을 다시 만들어야 한다(⋮ → 작업공간 관리 → +).
- 🧨 **기본 작업공간에 대행사 미게시 변경 9건이 여전히 남아 있다.** 거기서 작업해 게시하면 그게 같이 나간다.
- 🧨 **클릭 발동조건은 「실패한 클릭」도 잡는다** — 옵션을 안 고르고 장바구니 버튼을 누르면 **빈 `add_to_cart` 가 나갔다**(실측). 예외 규칙으로 막고 재실측으로 확인했다. 대행사 픽셀은 코드 안에서 `length>0` 으로 막고 있었다.
- 🧨 **GA4 데이터 필터는 소급 적용이 안 되고 구글이 「취소 불가」라 경고한다.** 2026-07-29 오전에 어시가 만든 테스트 이벤트는 **이미 들어가서 안 지워진다** → 2026-07-29 자 숫자만 조금 부풀어 있다.
- ⚠️ **공인 IP 는 바뀔 수 있다**(일반 인터넷 회선). 바뀌면 필터가 **아무도 안 거르게** 될 뿐 엉뚱한 손님을 자르진 않는다. 숫자가 이상하면 IP 부터 다시 확인. 조회는 `curl https://api.ipify.org` — `ipconfig` 는 **내부 주소**라 못 쓴다.
- ⚠️ **계정을 헷갈리면 「아무것도 없다」로 오진한다** — GA4·태그관리자 둘 다 `bonroi2296@gmail.com`(authuser=2) 소유다. 크롬 기본 로그인은 `seokmin.moon88` 이고 그쪽엔 태그관리자 계정이 0개다.
- ⚠️ **로그인된 화면을 파일로 캡처하려면 크롬을 `--app=` 모드로 새 창에 띄워라.** 그냥 새 창으로 찍으면 **PO 북마크바와 작업표시줄이 통째로 찍힌다**(실제로 한 번 찍혀서 지웠다). 어시 도구의 스크린샷은 파일로 남지 않는다.
- ⚠️ **태그 품질 「주의 필요」는 무시해도 되는 경고였다** — 태그 안 된 5개가 전부 결제·쿠폰 처리용 중계 주소와 관리자 화면이다. 오히려 태그를 붙이면 유령 조회수가 생긴다. **같은 화면의 「교차 도메인 측정을 위한 도메인 추가」는 누르지 마라**(카페24 관리자 활동이 손님 데이터에 섞인다).
- 🧨 **작업 도중 다른 세션이 `HEALO_KHIDI` 본 폴더의 브랜치를 갈아탔다** — 그래서 이 핸드오프는 **별도 작업본(`HEALO_worktrees/probelle-ga4`)에서 썼다**. 본 폴더에서 문서를 쓰려다 파일이 통째로 달라져 한 번 헛손질했다. **같은 폴더 동시작업 금지는 실제로 지켜야 한다.**

**5. 다음 세션이 먼저 할 일**

1. ⚠️ **직전 미검증분 먼저 확인 2건** — ①**2026-07-31 이후** 프로벨르 GA4 일반 보고서(실시간이 아닌)에 **어제 날짜 숫자가 실제로 채워졌는지**. 설치 당일엔 「실시간」만 나오고 나머지는 하루~이틀 뒤라 이번 세션에선 **끝까지 못 봤다.** ②네이버페이 결제 경로(실주문 대기).
2. **healwith 본업으로 복귀** — 이 세션은 별건이다. 바로 아래 핸드오프들의 남은 일이 그대로 살아 있다.
3. **프로벨르 재개 신호가 오면 처음부터 다시 파악하지 마라** — 어시 기억 `probelle-ga4-state` 에 계정·ID·함정·조회법이 전부 있다.
4. 마케터 회신이 오면 검색어 표·회원가입 집계부터(둘 다 30분 이내 작업).

**6. 검증 상태**

- **저장소: 합치기 신청서 0건 · 자동 검사 해당 없음**(코드 변경이 없어 돌릴 것이 없다). 이 세션이 만든 변경은 `docs/PROJECT_CONTEXT.md`·`docs/PO_PREFERENCES.md` 2개뿐.
- ✅ **실측으로 확인한 것**(사이트에서 전송되는 데이터를 직접 뜯어봄): `page_view` · `view_item`(79,300원/상품번호 33/카테고리 28) · `add_to_cart`(79,300원) · `begin_checkout`(79,300원) · `purchase`(주문번호 `20260729-0000017`·79,300원·배송비 0·KRW) · `view_search_results`(검색어 「글리신」). GA4 실시간 화면에서 주요 이벤트 `purchase` 1건·구매자 1명 확인.
- ✅ **주문 취소 확인** — 카페24 전체 주문 조회에서 「취소 1 / 실결제금액 0원」.
- ✅ **매뉴얼 9쪽 전부 눈으로 확인**(워드 → PDF → 이미지). 빈 페이지·잘린 표·깨진 글자 없음. 첫 판에서 발견해 고친 것: 빈 페이지 1개 · 제목만 남고 표가 넘어간 것 · 「⋯」 글자 깨짐 · 그림 번호 어긋남.
- ❌ **검증 못 함 1** — **다음 날 일반 보고서에 숫자가 채워지는지.** 설치일에는 확인이 물리적으로 불가(구글 처리 지연 하루~이틀).
- ❌ **검증 못 함 2** — **네이버페이·카카오페이 결제 경로.** 실주문이 없어 못 쟀다.
- 📝 **정정 1건(내 오판)** — 어시가 「`purchase` 가 주요 이벤트로 안 켜져 있다」고 보고했으나, 실제로는 **이미 켜져 있고 해제가 불가능한 항목**이었다(별표가 회색인 건 「해제 불가」 표시). 아무것도 바꾸지 않았다.
- 📝 **매뉴얼 설명 검수에서 내 안내 4곳이 틀린 것을 자가적발** — 구매 여정이 4단계가 아니라 **5단계**(결제 시작이 빠져 있었다) · 유입경로는 드롭다운을 바꿔야 개별 출처가 보임 · 기본 기간이 **어제까지**라 오늘이 안 들어감 · 검색어는 수집되지만 기본 화면에 표가 없음. **전 화면을 직접 클릭해 대조한 뒤에야 드러났다.**

**7. 다음 세션 첫 프롬프트**

---

### 그 세션이 다음 세션에 남긴 부탁 (원문 — PROJECT_CONTEXT 에서 같이 빼냄)

> 먼저 `docs/PROJECT_CONTEXT.md` 최상단 읽어. 직전 세션은 프로벨르(별건)였고 healwith 코드 변경은 0이야. ①2026-07-31 이후면 프로벨르 GA4 일반 보고서에 어제 숫자가 채워졌는지부터 확인해줘(설치일엔 못 봤음) ②그다음 healwith 본업으로 돌아와서 아래 핸드오프들의 남은 일 이어가 ③프로벨르 얘기가 다시 나오면 기억 `probelle-ga4-state` 부터 읽어 — 처음부터 다시 파악하지 마.
