-- 2026-06-24: 코디 운영 단순화 — 계정 없는 시드 병원 비활성화 (PO 결정)
-- 현재는 TEST 병원(국내 의료기관) 1곳이 전체를 컨트롤. 고려대구로·면력한방·세브란스·이대 등
-- 시드 데이터는 실제 파트너 계정이 없어 코디 화면(케이스·상담 모달)에 떠 혼선을 줬다.
-- 가역: 나중에 실제 병원 추가 시 해당 행을 is_active=true 로 되돌리면 된다.
-- (이미 prod 에 적용됨 — 이 파일은 재현/기록용. 멱등.)
UPDATE hospitals
SET is_active = false
WHERE name <> 'TEST 병원 (국내 의료기관)';
