NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Supabase Security
# ⚠️ 중요: 최소 32자 이상의 강력한 랜덤 문자열 (DB에 저장하지 않음)
# ⚠️ 경고: 키 분실/변경 시 기존 데이터 복호화 불가 (백업 필수)
# 생성 방법:
#   - openssl rand -hex 32
#   - node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 프로덕션 환경에서는 키 누락/길이 부족 시 서버 시작 실패
SUPABASE_ENCRYPTION_KEY=your-32-character-minimum-random-encryption-key-here

# 토큰 회전 API 보호 (POST /api/inquiries/rotate-token)
# inquiryId + adminSecret로 public_token 재발급 시 사용
INTERNAL_ADMIN_SECRET=your-internal-admin-secret-here
